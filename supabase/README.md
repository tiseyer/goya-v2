# Supabase Setup

1. Go to your Supabase project SQL editor
2. Run the contents of `migrations/001_profiles.sql`
3. After your first signup, update `seed.sql` with your email and run it to get admin role
4. Create a storage bucket named `avatars` (set to public) for avatar uploads
