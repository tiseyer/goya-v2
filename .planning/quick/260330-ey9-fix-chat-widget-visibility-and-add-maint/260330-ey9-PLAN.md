---
type: quick
tasks: 3
autonomous: true
files_modified:
  - app/components/chat/ChatWidget.tsx
  - app/components/chat/ChatWidgetLoader.tsx
  - app/api/chatbot/config/route.ts
  - app/admin/settings/page.tsx
  - app/admin/settings/components/MaintenanceTab.tsx
  - lib/chatbot/chat-actions.ts
  - supabase/migrations/20260362_chatbot_maintenance_mode.sql
---

<objective>
Fix chat widget not appearing on frontend pages and add a Maintenance tab to /admin/settings that consolidates maintenance-related controls including a new Chatbot Maintenance toggle.

Purpose: The chat widget is invisible on all frontend pages despite being mounted in layout.tsx. Maintenance-related settings (Maintenance Mode, Email Sandbox) are buried in the General tab. A new Maintenance tab will group these logically and add chatbot maintenance mode control.

Output: Visible chat widget on non-admin pages, new Maintenance tab in admin settings with chatbot maintenance toggle.
</objective>

<context>
@app/layout.tsx — Mounts ChatWidgetLoader for non-admin pages (line 102)
@app/components/chat/ChatWidgetLoader.tsx — Dynamic import wrapper (ssr: false)
@app/components/chat/ChatWidget.tsx — Fetches /api/chatbot/config, checks is_active, renders FloatingButton + ChatPanel
@app/api/chatbot/config/route.ts — Returns { is_active, name, avatar_url } from chatbot_config table (service role)
@app/admin/settings/page.tsx — Settings page with General/Analytics/Email Templates/Health tabs
@supabase/migrations/20260356_chatbot_config.sql — chatbot_config table, is_active defaults to false
@lib/chatbot/chat-actions.ts — getCurrentUserId (checks supabase auth), getAnonymousId

Key interfaces:
- ChatWidget checks `config.is_active` — returns null if false (line 84)
- /api/chatbot/config uses getSupabaseService() (service role, bypasses RLS)
- chatbot_config.is_active defaults to false in migration seed
- Admin settings Tab type: 'general' | 'analytics' | 'email-templates' | 'health'
- GeneralTab contains: General info section, Maintenance Mode block (lines 380-483), Email Sandbox block (lines 486-567), DeployEnvironmentCard, Danger Zone
- Admin role checked via profile?.role === 'admin' pattern (see Header.tsx)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix chat widget visibility and add admin preview mode</name>
  <files>
    app/api/chatbot/config/route.ts
    app/components/chat/ChatWidget.tsx
    lib/chatbot/chat-actions.ts
  </files>
  <action>
  The chat widget is not visible on frontend pages. Investigate and fix the root cause. The most likely issue is that `is_active` is `false` in the chatbot_config table (the migration seeds it as false). The fix has two parts:

  **Part A — Investigate the config endpoint:**
  1. Use curl or the dev server to hit `/api/chatbot/config` and check what it returns.
  2. If `is_active` is false, the widget correctly hides itself (line 84 of ChatWidget.tsx: `if (loading || !config || !config.is_active) return null`).
  3. The real fix: update `/api/chatbot/config/route.ts` to ALSO return a `chatbot_maintenance_mode` boolean (from site_settings table) and an `is_admin` boolean.
  4. Query site_settings for key `chatbot_maintenance_mode` alongside the chatbot_config query. Return `{ is_active, name, avatar_url, chatbot_maintenance_mode }`.

  **Part B — Add admin-awareness to ChatWidget:**
  1. In `lib/chatbot/chat-actions.ts`, add a new server action `getCurrentUserRole()` that returns the user's role string or null (query the `members` table using the user ID from supabase auth, select the `role` column).
  2. In `ChatWidget.tsx`, call `getCurrentUserRole()` alongside the existing identity resolution.
  3. Change the visibility logic (line 84):
     - If `is_active` is true AND `chatbot_maintenance_mode` is false: show widget for everyone (normal mode).
     - If `is_active` is true AND `chatbot_maintenance_mode` is true: show widget ONLY for admins. When shown to admins in this state, pass a `maintenanceBadge={true}` prop concept — render a small amber "Maintenance" badge on the FloatingButton (positioned top-right of the button, text-[10px], bg-amber-500 text-white px-1.5 py-0.5 rounded-full).
     - If `is_active` is false: show widget ONLY for admins (admin preview mode). Show a small "Preview" badge on the FloatingButton (same style but bg-blue-500).
     - If not admin and widget hidden: return null as before.
  4. Update the config interface to include `chatbot_maintenance_mode: boolean`.
  5. The FloatingButton badge can be implemented inline in ChatWidget.tsx by wrapping FloatingButton in a relative div and adding an absolute-positioned span badge.

  **Important:** Do NOT modify the chatbot_config table or its is_active column. The chatbot_maintenance_mode is stored in site_settings (key-value), not in chatbot_config.
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
  - /api/chatbot/config returns chatbot_maintenance_mode boolean from site_settings
  - ChatWidget shows for admins even when is_active=false (preview badge) or chatbot_maintenance_mode=true (maintenance badge)
  - ChatWidget hides for non-admins when chatbot_maintenance_mode=true or is_active=false
  - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Maintenance tab and migrate settings from General tab</name>
  <files>
    app/admin/settings/components/MaintenanceTab.tsx
    app/admin/settings/page.tsx
    supabase/migrations/20260362_chatbot_maintenance_mode.sql
  </files>
  <action>
  **Part A — Create migration for chatbot_maintenance_mode:**
  Create `supabase/migrations/20260362_chatbot_maintenance_mode.sql`:
  ```sql
  INSERT INTO site_settings (key, value, description)
  VALUES ('chatbot_maintenance_mode', 'false', 'When true, hides the chat widget for non-admin users')
  ON CONFLICT (key) DO NOTHING;
  ```
  Then run `npx supabase db push` to apply.

  **Part B — Extract MaintenanceTab component:**
  Create `app/admin/settings/components/MaintenanceTab.tsx` as a new file. Move the following from `GeneralTab` in `page.tsx`:
  1. All maintenance mode state and logic (mmEnabled, mmScheduled, mmStart, mmEnd, mmMessage, mmLoading, mmSaving, showConfirm, toast + saveMmSettings, handleEnableToggle, the confirmation dialog)
  2. All email sandbox state and logic (sbEnabled, sbRecipient, sbLoading, sbSaving + saveSbSettings)
  3. The maintenance mode UI block (lines 380-483 of page.tsx)
  4. The email sandbox UI block (lines 486-567)

  Add a NEW third section — "Chatbot Maintenance" — after Email Sandbox:
  - Same card style as the other sections (white rounded-xl border shadow-sm)
  - Header: "Chatbot Maintenance" with status dot (amber if active, emerald if inactive)
  - Toggle: "Enable Chatbot Maintenance Mode"
  - Description: "Hide the chat widget from regular users. Admins will still see it with a maintenance badge."
  - When enabled, show an info banner (amber): "Chatbot is hidden from regular users. Admin users can still see and test the widget."
  - Save button same style as other sections
  - Load and save `chatbot_maintenance_mode` from site_settings (same pattern as maintenance_mode_enabled)
  - Add `chatbot_maintenance_mode` to the initial data load useEffect alongside the other keys

  The MaintenanceTab needs to import: `supabase` from `@/lib/supabase`, and reuse the shared UI components (Section, Toggle, Toast, etc.). Since those are defined locally in page.tsx, either:
  - Option A (preferred): Import `supabase` and redefine the needed UI helpers (Toggle, Toast, Section) locally in MaintenanceTab.tsx. Copy them from page.tsx — they are small utility components.
  - The datetime helpers (utcToDatetimeLocal, datetimeLocalToUtc) also need to be copied to MaintenanceTab.tsx.

  **Part C — Update page.tsx:**
  1. Add import: `import MaintenanceTab from './components/MaintenanceTab'`
  2. Update Tab type: `'general' | 'analytics' | 'email-templates' | 'health' | 'maintenance'`
  3. Add to TABS array after health: `{ key: 'maintenance', label: 'Maintenance' }`
  4. Add tab render: `{tab === 'maintenance' && <MaintenanceTab />}`
  5. Clean up GeneralTab: Remove ALL maintenance mode state/logic/UI and email sandbox state/logic/UI. GeneralTab should only contain:
     - The "General" Section with ReadOnlyField items (App Name, Version, Environment)
     - The DeployEnvironmentCard
     - The Danger Zone section
  6. Remove the confirmation dialog and toast from GeneralTab (they move to MaintenanceTab)

  **Styling consistency:** Match the existing card and toggle patterns exactly. Use the same color tokens (#1B3A5C, #00B5A3, #E5E7EB, #374151, #6B7280).
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
  - New Maintenance tab appears in /admin/settings after Health tab
  - Maintenance Mode block renders in Maintenance tab (not General tab)
  - Email Sandbox block renders in Maintenance tab (not General tab)
  - New Chatbot Maintenance block with toggle, status dot, info banner, save button
  - General tab only shows General info, Deploy Environment, and Danger Zone
  - chatbot_maintenance_mode stored/loaded from site_settings
  - Migration file created and pushed
  - TypeScript compiles without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  1. Chat widget visibility fix — widget should now appear on frontend pages (e.g., /events) when is_active=true in chatbot_config. Admins see it even when is_active=false (with "Preview" badge) or when chatbot_maintenance_mode=true (with "Maintenance" badge).
  2. New Maintenance tab in /admin/settings with three sections: Maintenance Mode, Email Sandbox, Chatbot Maintenance.
  3. General tab cleaned up — only shows General info, Deploy Environment, Danger Zone.
  </what-built>
  <how-to-verify>
  1. Visit /events (or any non-admin frontend page) — check if chat widget bubble appears in bottom-right corner.
  2. Visit /admin/settings — confirm "Maintenance" tab appears after "Health".
  3. Click "Maintenance" tab — verify Maintenance Mode, Email Sandbox, and Chatbot Maintenance sections all render correctly with toggles.
  4. Click "General" tab — confirm only General info, Deploy Environment, and Danger Zone remain (no maintenance or email sandbox blocks).
  5. In Maintenance tab, toggle Chatbot Maintenance on, save, then visit a frontend page as a non-admin — widget should be hidden. As admin, widget should show with amber "Maintenance" badge.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- /api/chatbot/config returns { is_active, name, avatar_url, chatbot_maintenance_mode }
- Chat widget visible on frontend pages when is_active=true
- Admin sees widget with badge when is_active=false or chatbot_maintenance_mode=true
- /admin/settings has 5 tabs: General, Analytics, Email Templates, Health, Maintenance
- Maintenance tab contains all three maintenance sections
- General tab is clean (no maintenance/sandbox blocks)
</verification>

<success_criteria>
- Chat widget is visible and functional on all non-admin pages when chatbot is active
- Admins can preview the widget even when it is disabled or in maintenance mode
- Maintenance-related settings are consolidated in a dedicated Maintenance tab
- Chatbot maintenance mode toggle works end-to-end (toggle -> save -> widget visibility changes)
</success_criteria>
