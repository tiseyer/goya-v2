---
phase: quick
plan: 260331-my2
type: execute
wave: 1
depends_on: []
files_modified:
  - app/events/[id]/page.tsx
  - app/academy/[id]/page.tsx
  - app/members/[id]/page.tsx
autonomous: true
requirements: [HERO-LEFT-ALIGN, HERO-SPACING, HERO-TITLES, HERO-STRUCTURE]
must_haves:
  truths:
    - "All hero text on event, course, and member detail pages is left-aligned"
    - "Hero elements have generous vertical spacing between back link, pills, title, and meta"
    - "H1 titles are larger (text-4xl md:text-5xl) on all three detail pages"
    - "Event and course detail heroes share the same structural order: back link, pills, title, meta"
    - "Member profile hero is left-aligned with same spacing improvements but retains avatar layout"
  artifacts:
    - path: "app/events/[id]/page.tsx"
      provides: "Event detail hero with left-align, spacing, larger title, meta line"
    - path: "app/academy/[id]/page.tsx"
      provides: "Course detail hero with left-align, spacing, larger title, meta line"
    - path: "app/members/[id]/page.tsx"
      provides: "Member profile hero with left-align, spacing improvements"
  key_links: []
---

<objective>
Fix hero layout on three detail pages (event, course, member profile) to be left-aligned with increased spacing between elements, larger titles, and consistent structure between event and course heroes.

Purpose: Visual polish — heroes currently feel cramped and inconsistently structured across detail pages.
Output: Updated hero sections in three page files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/events/[id]/page.tsx
@app/academy/[id]/page.tsx
@app/members/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix event and course detail page heroes</name>
  <files>app/events/[id]/page.tsx, app/academy/[id]/page.tsx</files>
  <action>
## Event detail page (`app/events/[id]/page.tsx`)

### Gradient hero (no-image variant, lines ~85-119):
Update spacing classes on the PageContainer children to create breathing room:
- Back link: keep as-is at top, change `mb-4` to `mb-4` (already good, 16px gap)
- Pills row: change `mb-4` to `mb-3` (12px gap before title)
- H1 title: change `text-4xl sm:text-5xl` to `text-4xl md:text-5xl` (already correct size, just ensure md breakpoint), keep `mb-3`
- Date meta line: keep `text-primary-200 text-sm font-medium` (8px natural gap from mb-3 above)

Already left-aligned — no changes needed for alignment in gradient hero.

### Image hero (lines ~46-82):
- Back link (line 62-69): change `mb-5` to `mb-4` (16px gap)
- Pills row (line 70): change `mb-3` to `mb-3` (keep 12px gap)
- H1 title (line 79): change `text-3xl sm:text-4xl` to `text-4xl md:text-5xl` to match gradient hero size
- Add a date meta line AFTER the H1 (inside the PageContainer, after the h1 closing tag):
  ```
  <p className="text-white/70 text-sm font-medium mt-2">{dateFormatted}</p>
  ```
  This ensures the image hero has the same structure as the gradient hero: back link > pills > title > meta date.

Both hero variants are already left-aligned (no text-center or items-center on the text axis).

## Course detail page (`app/academy/[id]/page.tsx`)

Update the hero section (lines ~68-115) inside the PageContainer:
- Back link (line 81-89): change `mb-2` to `mb-4` (16px gap to pills)
- Pills row (line 92): change `mb-2` to `mb-3` (12px gap to title)
- H1 title (line 110): change `text-3xl sm:text-4xl` to `text-4xl md:text-5xl`, change `mb-2` to `mb-2` (8px gap to meta)
- Short description (line 111-113): this serves as the meta line — keep as-is

Add an instructor/duration meta line AFTER the short description (or replace it as the meta line). Insert after the short_description paragraph:
```tsx
<div className="flex flex-wrap items-center gap-x-4 gap-y-1 text-white/70 text-sm mt-1">
  {course.instructor && (
    <span className="flex items-center gap-1.5">
      <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" /></svg>
      {course.instructor}
    </span>
  )}
  {course.duration && (
    <span className="flex items-center gap-1.5">
      <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>
      {course.duration}
    </span>
  )}
  {course.lesson_count && (
    <span className="flex items-center gap-1.5">
      <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 6h16M4 10h16M4 14h16M4 18h16" /></svg>
      {course.lesson_count} {course.lesson_count === 1 ? 'lesson' : 'lessons'}
    </span>
  )}
</div>
```

Note: Check if `instructor`, `duration`, `lesson_count` exist on the Course type. If `instructor` or `lesson_count` are not on the type, only render the fields that exist. Do NOT add fields that don't exist on the type — just use what's available (title, duration, access, category, level are confirmed). Only render the meta line items for fields that actually exist on the Course type.

Both pages are already left-aligned — the key changes are spacing increases and title size bumps.
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Event gradient hero: back link mb-4, pills mb-3, title text-4xl md:text-5xl, date meta below
    - Event image hero: same spacing pattern, title bumped to text-4xl md:text-5xl, date meta added
    - Course hero: back link mb-4, pills mb-3, title text-4xl md:text-5xl, meta info line with available fields
    - Both pages share the same hero element order: back link > pills > H1 > meta
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix member profile page hero</name>
  <files>app/members/[id]/page.tsx</files>
  <action>
Update the member profile hero section (lines ~60-119).

### Left-align the layout:
- The outer flex container (line 77): change `items-center sm:items-end` to `items-start sm:items-end` so mobile is left-aligned too
- The info div (line 97): change `text-center sm:text-left` to `text-left` (always left-aligned)
- The pill wrapper (line 98): change `justify-center sm:justify-start` to `justify-start`
- The location div (line 109): change `justify-center sm:justify-start` to `justify-start`

### Increase spacing:
- Back link (line 67-75): keep `mb-4` (16px gap — already good)
- Role pill wrapper (line 98): change `mb-3` to `mb-3` (keep 12px — matches event/course pill-to-title gap)
- H1 title (line 104): already `text-3xl sm:text-4xl lg:text-5xl` which is large enough. Change to `text-4xl md:text-5xl` for consistency with the other two pages. Keep `mb-2` (8px gap to location meta)
- Location meta (line 108-116): change `mt-2` to `mt-1` (tighten to match the 8px pattern from title to meta)

### Do NOT change:
- Hero background color or height
- Avatar size or ring styling
- The overall flex-col sm:flex-row layout pattern (avatar beside info on desktop)
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - All hero text is left-aligned on all screen sizes (no text-center on mobile)
    - Title is text-4xl md:text-5xl matching other detail pages
    - Spacing between elements is consistent with event/course heroes
    - Avatar + info layout preserved, just aligned left on mobile
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- All three detail page heroes are left-aligned (no text-center classes remain in hero sections)
- All three pages use text-4xl md:text-5xl for H1
- Event and course heroes share the same element order: back link, pills, title, meta
- No overview pages (Dashboard, Events overview, Academy overview, Add-Ons) were modified
</verification>

<success_criteria>
- Three files modified, zero TypeScript errors
- Heroes are visually left-aligned with spacious vertical gaps
- Titles are noticeably larger than before
- Event and course heroes are structurally consistent
</success_criteria>

<output>
After completion, create `activity/quick-tasks/quick-task_fix-hero-layout-detail-pages_31-03-2026.md`
</output>
