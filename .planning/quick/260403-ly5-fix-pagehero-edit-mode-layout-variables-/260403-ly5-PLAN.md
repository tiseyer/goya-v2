---
phase: quick
plan: 260403-ly5
type: execute
wave: 1
depends_on: []
files_modified: [app/components/PageHero.tsx]
autonomous: true
requirements: [quick-fix]

must_haves:
  truths:
    - "Variable pills render as a fixed left-edge panel when editing, not inside the hero flow"
    - "Save/Cancel buttons render fixed top-right, not inside the hero"
    - "Hero maintains normal height in edit mode with only 3 inline inputs (pill, title, subtitle)"
    - "Pencil icon becomes X icon when editing — clicking X cancels edit"
  artifacts:
    - path: "app/components/PageHero.tsx"
      provides: "Refactored edit mode layout with fixed panels"
  key_links:
    - from: "fixed left panel"
      to: "insertVariable()"
      via: "onClick on variable buttons"
      pattern: "insertVariable"
---

<objective>
Fix PageHero edit mode layout so variables and action buttons do not overlap hero content.

Purpose: Currently the variable pills and Save/Cancel buttons are positioned inside the hero section (absolute bottom), causing overlap and readability issues. Move variables to a fixed left-edge panel and Save/Cancel to fixed top-right.
Output: Updated PageHero.tsx with clean edit mode layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/components/PageHero.tsx
@lib/hero-variables.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor PageHero edit mode layout — fixed panels for variables and buttons</name>
  <files>app/components/PageHero.tsx</files>
  <action>
Refactor the edit mode UI in `app/components/PageHero.tsx` with these 3 changes:

**1. Replace `editToolbar` with two separate fixed-position elements:**

Remove the entire `editToolbar` const (lines 179-224) and its usage in both variant returns. Replace with two new elements rendered when `editing === true`:

A) **Variables panel** — fixed vertical panel on left edge of screen:
```tsx
{editing && (
  <div className="fixed left-0 top-1/2 -translate-y-1/2 z-50 flex flex-col gap-1 p-2 bg-black/30 backdrop-blur-sm rounded-r-xl">
    <span className="text-white/50 text-xs mb-1">Variables</span>
    {HERO_VARIABLES.map(v => (
      <button
        key={v.key}
        type="button"
        onClick={() => insertVariable(v.key)}
        title={v.description}
        className="text-xs bg-white/20 hover:bg-white/40 text-white px-2 py-1 rounded font-mono whitespace-nowrap transition-colors"
      >
        {v.key}
      </button>
    ))}
  </div>
)}
```

B) **Save/Cancel buttons** — fixed top-right:
```tsx
{editing && (
  <div className="fixed top-4 right-4 z-50 flex gap-2">
    <button type="button" onClick={handleCancel} className="px-4 py-2 text-sm bg-white/20 hover:bg-white/30 text-white rounded-lg backdrop-blur-sm transition-colors">
      Cancel
    </button>
    <button type="button" onClick={handleSave} disabled={saving} className="px-4 py-2 text-sm bg-white text-[#345c83] hover:bg-white/90 rounded-lg font-medium transition-colors disabled:opacity-50">
      {saving ? 'Saving...' : 'Save'}
    </button>
  </div>
)}
```

Place both of these inside each variant's `<section>` element, right after the opening tag (before any background divs). They are `fixed` positioned so placement in DOM just needs to be within the component.

**2. Change pencil icon to X icon when editing:**

Update the `adminControl` const. Currently it renders `null` when `editing` is true (line 174). Change the logic so that when `editing === true`, instead of null it renders an X (close) button that calls `handleCancel`:

Replace the ternary in `adminControl`:
- When `saved`: show the green "Saved" badge (no change)
- When `editing`: show an X button that calls `handleCancel` (same position/style as pencil but with X icon)
- When neither: show pencil button (no change)

X icon SVG: `<path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M6 18L18 6M6 6l12 12" />`

The X button should use the same styling as the pencil button (same size, same variant-aware colors).

**3. Keep hero content area clean:**

The hero section height and content area remain unchanged. The 3 inline inputs (pill, title, subtitle) already replace display text when editing — that behavior stays. No buttons or variable pills render inside the hero flow anymore. The `{editToolbar}` references in both variant returns must be removed.

**Summary of removals:**
- Delete the `editToolbar` const entirely (lines 179-224)
- Delete `{editToolbar}` from dark variant return (line 302)
- Delete `{editToolbar}` from light variant return (line 371)

**Summary of additions:**
- Fixed left variables panel (inside each section, rendered when editing)
- Fixed top-right Save/Cancel (inside each section, rendered when editing)
- X icon in adminControl when editing (replaces the null case)
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Variable pills render in a fixed left-edge panel (not inside hero flow)
    - Save/Cancel buttons render fixed top-right (not inside hero flow)
    - Hero maintains normal height in edit mode — only pill/title/subtitle inputs inline
    - Pencil icon becomes X when editing; clicking X cancels
    - tsc --noEmit passes with 0 errors
    - No overflow in edit mode
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with 0 errors
- Visual: edit mode shows clean hero with inline inputs only, variables on left edge, buttons top-right
</verification>

<success_criteria>
PageHero edit mode no longer has overlapping content. Variables accessible on left panel, Save/Cancel accessible top-right, hero content is readable with inline-only editing inputs.
</success_criteria>

<output>
After completion, create `.planning/quick/260403-ly5-fix-pagehero-edit-mode-layout-variables-/260403-ly5-SUMMARY.md`
</output>
