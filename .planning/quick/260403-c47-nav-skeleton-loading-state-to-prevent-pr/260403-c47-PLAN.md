---
phase: quick
plan: 260403-c47
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/Header.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Desktop nav shows skeleton pill placeholders while auth is loading"
    - "Desktop nav renders all links at once (no progressive pop-in) after auth resolves"
    - "Mobile nav shows skeleton placeholders when opened during auth loading"
    - "No layout shift between skeleton and resolved nav states"
  artifacts:
    - path: "app/components/Header.tsx"
      provides: "Nav skeleton loading state for both desktop and mobile"
  key_links:
    - from: "authLoading state"
      to: "skeleton vs real nav rendering"
      via: "conditional render in desktop and mobile nav sections"
      pattern: "authLoading.*skeleton|animate-pulse"
---

<objective>
Add skeleton loading placeholders to the navigation bar so nav items don't progressively pop in as auth state resolves.

Purpose: Eliminate the visual jank where logged-out nav items flash first, then switch to logged-in nav items when auth resolves.
Output: Updated Header.tsx with skeleton loading states for desktop and mobile nav.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/components/Header.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add nav skeleton loading to desktop and mobile nav</name>
  <files>app/components/Header.tsx</files>
  <action>
  Modify the desktop nav section (around lines 1027-1053) and mobile nav section (around lines 1153-1198) to show skeleton placeholders during auth loading.

  **Desktop nav (inside the `hidden lg:flex` nav element):**
  - When `authLoading` is true, render 4 skeleton pill divs instead of any nav links:
    ```tsx
    {authLoading ? (
      <>
        {[80, 64, 72, 64].map((w, i) => (
          <div key={i} className={`h-[34px] rounded-lg bg-slate-100 animate-pulse`} style={{ width: `${w}px` }} />
        ))}
      </>
    ) : (
      <>
        {isLoggedIn && (
          <Link href="/dashboard" ...>Dashboard</Link>
        )}
        {(isLoggedIn ? NAV_LINKS_LOGGED_IN : NAV_LINKS_LOGGED_OUT).map(({ href, label }) => (
          <Link key={label} href={href} ...>{label}</Link>
        ))}
      </>
    )}
    ```
  - The skeleton pills use `bg-slate-100 animate-pulse rounded-lg h-[34px]` to match the nav item dimensions (px-3 py-2 text-sm).
  - Widths of 80, 64, 72, 64 pixels approximate natural nav text widths and avoid uniform rectangles.
  - Keep the existing `gap-0.5` on the parent nav — skeletons inherit that spacing.

  **Mobile nav (inside the `navOpen &&` conditional):**
  - When `authLoading` is true, render 4 skeleton bars instead of nav links:
    ```tsx
    {authLoading ? (
      <>
        {[128, 96, 112, 96].map((w, i) => (
          <div key={i} className="h-10 rounded-lg bg-slate-100 animate-pulse" style={{ width: `${w}px` }} />
        ))}
      </>
    ) : (
      <>
        {isLoggedIn && (
          <Link href="/dashboard" ...>Dashboard</Link>
        )}
        {(isLoggedIn ? NAV_LINKS_LOGGED_IN : NAV_LINKS_LOGGED_OUT).map(...)
      </>
    )}
    ```
  - Mobile skeleton bars are wider (128, 96, 112, 96) to match the larger mobile nav link sizing.

  **Important:** Do NOT modify the right-side auth area (lines 1056-1107) — it already has proper skeleton loading.
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
  - Desktop nav shows 4 skeleton pills during authLoading, then all real links at once when resolved
  - Mobile nav shows 4 skeleton bars during authLoading, then all real links at once when resolved
  - No progressive pop-in of nav items
  - TypeScript compiles with 0 errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Nav skeleton loading state that prevents progressive item appearance during auth resolution</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the app in browser
    2. Open DevTools Network tab, throttle to "Slow 3G" to make loading visible
    3. Hard refresh the page while logged in
    4. Observe: desktop nav should show grey pulsing pills first, then all nav items appear at once
    5. Check mobile: click hamburger during loading — should see skeleton bars, not real items
    6. Verify no layout shift when skeletons transition to real nav items
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with 0 errors
- Desktop nav: skeleton pills visible during auth loading, all items appear simultaneously after
- Mobile nav: skeleton bars visible during auth loading, all items appear simultaneously after
- Right-side auth area unchanged and still working
</verification>

<success_criteria>
Nav items no longer progressively pop in. Instead, skeleton placeholders show during auth loading, and all nav items render at once when auth state resolves.
</success_criteria>

<output>
After completion, create `.planning/quick/260403-c47-nav-skeleton-loading-state-to-prevent-pr/260403-c47-SUMMARY.md`
</output>
