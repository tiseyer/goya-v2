---
phase: quick
plan: 260403-d8m
type: execute
wave: 1
depends_on: []
files_modified:
  - app/admin/events/[id]/edit/page.tsx
  - app/admin/events/components/EventForm.tsx
autonomous: true
requirements: [QUICK-TASK]
must_haves:
  truths:
    - "View Event button visible next to Edit Event heading on admin edit page"
    - "View Event button visible next to Save/Cancel buttons at bottom of form"
    - "Clicking either button opens /events/{id} in a new tab"
    - "Buttons use outline/secondary style with external link icon"
  artifacts:
    - path: "app/admin/events/[id]/edit/page.tsx"
      provides: "Top View Event button next to heading"
    - path: "app/admin/events/components/EventForm.tsx"
      provides: "Bottom View Event button next to Save/Cancel"
  key_links:
    - from: "View Event button"
      to: "/events/[id]"
      via: "Link or anchor with target=_blank"
---

<objective>
Add a "View Event" button to the admin event edit page in two locations: next to the page heading and next to the Save/Cancel action buttons. The button opens the public event detail page in a new tab.

Purpose: Let admins quickly preview the public-facing event page while editing.
Output: Two View Event buttons with outline style and external link icon.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/admin/events/[id]/edit/page.tsx
@app/admin/events/components/EventForm.tsx
</context>

<interfaces>
<!-- Key types and contracts the executor needs -->

From app/admin/events/[id]/edit/page.tsx (line 37-43):
- Page receives `params: Promise<{ id: string }>` and extracts `id`
- Event data fetched as `data` cast to `Event`
- Heading is at line 70: `<h1>Edit Event</h1>` inside a `<div className="mb-6">`

From app/admin/events/components/EventForm.tsx (line 46-56):
```typescript
interface Props {
  event?: Event;
  userRole?: string;
  currentUserId?: string;
  currentUserName?: string;
  currentUserAvatar?: string | null;
}
```
- `event.id` is available (used at line 226)
- Actions bar is at lines 553-566: flex row with Save button + Cancel Link
- Component is 'use client' and already imports `Link` from 'next/link'

Public event page route: `/events/[id]` (file: app/events/[id]/page.tsx)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add View Event button next to Edit Event heading</name>
  <files>app/admin/events/[id]/edit/page.tsx</files>
  <action>
In the heading section (lines 68-72), change the `<div className="mb-6">` to use a flex layout with the heading on the left and a View Event link on the right.

Replace the current heading block with:
```
<div className="mb-6 flex items-start justify-between">
  <div>
    <h1 className="text-2xl font-bold text-[#1B3A5C]">Edit Event</h1>
    <p className="text-sm text-[#6B7280] mt-0.5 truncate max-w-lg">{(data as Event).title}</p>
  </div>
  <a
    href={`/events/${id}`}
    target="_blank"
    rel="noopener noreferrer"
    className="inline-flex items-center gap-1.5 px-4 py-2 border border-goya-border text-sm font-medium text-foreground-secondary rounded-lg hover:bg-slate-50 transition-colors"
  >
    View Event
    <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14" />
    </svg>
  </a>
</div>
```

Use a plain `<a>` tag (not Next.js Link) since this opens in a new tab to a different route section. The `id` variable is already destructured at line 38.
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>View Event button appears top-right of the Edit Event heading, styled as outline/secondary with external link icon, linking to /events/{id} in a new tab.</done>
</task>

<task type="auto">
  <name>Task 2: Add View Event button next to Save/Cancel in EventForm</name>
  <files>app/admin/events/components/EventForm.tsx</files>
  <action>
In the Actions section (lines 552-566), add a View Event link after the Cancel link, but ONLY when in edit mode (when `event` exists and has an `id`).

Replace the actions div (lines 553-566) with:
```
<div className="flex items-center gap-3 pt-2">
  <button
    type="submit" disabled={saving}
    className="px-6 py-2.5 bg-primary text-white text-sm font-semibold rounded-lg hover:bg-primary-dark transition-colors disabled:opacity-60"
  >
    {saving ? 'Saving...' : isEdit ? 'Save Changes' : 'Create Event'}
  </button>
  <Link
    href="/admin/events"
    className="px-6 py-2.5 border border-goya-border text-foreground-secondary text-sm font-medium rounded-lg hover:bg-slate-50 transition-colors"
  >
    Cancel
  </Link>
  {isEdit && event?.id && (
    <a
      href={`/events/${event.id}`}
      target="_blank"
      rel="noopener noreferrer"
      className="inline-flex items-center gap-1.5 px-5 py-2.5 border border-goya-border text-foreground-secondary text-sm font-medium rounded-lg hover:bg-slate-50 transition-colors ml-auto"
    >
      View Event
      <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14" />
      </svg>
    </a>
  )}
</div>
```

The `ml-auto` pushes the View Event button to the right, visually separating it from the Save/Cancel group. The `isEdit && event?.id` guard ensures the button only shows on the edit form (not the create form). Uses a plain `<a>` tag for the new-tab link.
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>View Event button appears after Cancel in the bottom action bar (edit mode only), styled as outline/secondary with external link icon and ml-auto alignment, linking to /events/{id} in a new tab.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with 0 errors
2. Navigate to `/admin/events/{any-event-id}/edit` — View Event button visible top-right of heading
3. Scroll to bottom — View Event button visible after Cancel, pushed right
4. Click either button — opens `/events/{id}` in a new browser tab
5. Navigate to `/admin/events/new` — no View Event button in the bottom actions
</verification>

<success_criteria>
- Both View Event buttons render on the admin event edit page
- Buttons use outline/secondary styling consistent with the Cancel button
- External link icon (arrow-out-of-box) visible on both buttons
- Links open /events/{event-id} in a new tab
- Button does NOT appear on the create event form
- TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/quick/260403-d8m-add-view-event-button-to-admin-event-edi/260403-d8m-SUMMARY.md`
</output>
