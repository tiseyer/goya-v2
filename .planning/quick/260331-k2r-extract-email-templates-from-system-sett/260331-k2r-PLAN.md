---
phase: quick-260331-k2r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/admin/settings/email-templates/page.tsx
  - app/admin/settings/page.tsx
  - app/admin/components/AdminShell.tsx
autonomous: true
requirements: [QUICK-K2R]
must_haves:
  truths:
    - "Email Templates is a standalone page at /admin/settings/email-templates"
    - "System Settings page has only General, Health, Maintenance tabs"
    - "Sidebar Email Templates link goes to /admin/settings/email-templates"
    - "Only Email Templates highlights in sidebar when on that page, not System"
    - "All email template functionality unchanged"
  artifacts:
    - path: "app/admin/settings/email-templates/page.tsx"
      provides: "Standalone email templates page"
    - path: "app/admin/settings/page.tsx"
      provides: "System settings with 3 tabs only"
    - path: "app/admin/components/AdminShell.tsx"
      provides: "Updated sidebar nav with correct href"
  key_links:
    - from: "AdminShell sidebar"
      to: "/admin/settings/email-templates"
      via: "href in Settings group children"
    - from: "app/admin/settings/email-templates/page.tsx"
      to: "EmailTemplatesList component"
      via: "import from ../components/EmailTemplatesList"
---

<objective>
Extract the Email Templates tab from System Settings into a standalone page under /admin/settings/email-templates, update the sidebar navigation link, and fix the highlighting so only the correct item is active.

Purpose: Cleaner navigation — Email Templates is important enough to be its own page rather than buried as a tab in System Settings.
Output: Standalone email templates page, cleaned-up settings page, correct sidebar behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/admin/settings/page.tsx
@app/admin/settings/components/EmailTemplatesList.tsx
@app/admin/components/AdminShell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create standalone Email Templates page and clean up System Settings</name>
  <files>app/admin/settings/email-templates/page.tsx, app/admin/settings/page.tsx</files>
  <action>
1. Create `app/admin/settings/email-templates/page.tsx` as a 'use client' page component:
   - Import `EmailTemplatesList` from `../components/EmailTemplatesList`
   - Render a page header with title "Email Templates" and subtitle "Manage email template content and delivery settings"
   - Render `<EmailTemplatesList />` below the header
   - Use the same padding/spacing pattern as the current settings page (`p-6 lg:p-8`, `mb-6` for header)
   - Match the heading style: `text-2xl font-bold text-[#1B3A5C]` for h1, `text-sm text-[#6B7280] mt-0.5` for subtitle

2. Modify `app/admin/settings/page.tsx`:
   - Remove the `EmailTemplatesList` import
   - Remove `'email-templates'` from the `Tab` type (should be `'general' | 'health' | 'maintenance'`)
   - Remove the email-templates entry from the `TABS` array (keep General, Health, Maintenance)
   - Remove the `{tab === 'email-templates' && <EmailTemplatesList />}` render line
   - Remove the `useEffect` that syncs tab from URL search params (no longer needed for email-templates deep-link)
   - Actually keep the useEffect and useSearchParams — they may still be useful for other tab deep-links. Just ensure email-templates is no longer a valid tab value.
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - /admin/settings/email-templates/page.tsx exists and renders EmailTemplatesList
    - /admin/settings page.tsx has only 3 tabs: General, Health, Maintenance
    - No TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Update sidebar navigation and fix highlighting</name>
  <files>app/admin/components/AdminShell.tsx</files>
  <action>
1. In the `NAV_ITEMS` array, find the Settings group children. Change the Email Templates entry:
   - FROM: `{ href: '/admin/settings?tab=email-templates', label: 'Email Templates', paths: [...] }`
   - TO: `{ href: '/admin/settings/email-templates', label: 'Email Templates', paths: [...] }`
   (Keep the same SVG paths, just change the href)

2. Fix the sidebar highlighting logic. The current `isChildActive` check at line ~290 has a special case for `/admin/settings` to avoid both System and Email Templates highlighting. With the new href structure (`/admin/settings/email-templates` is a sub-path of `/admin/settings`), the existing `pathname.startsWith(childPath)` logic would cause System to highlight when on Email Templates.

   Update the `isChildActive` logic (around line 290) to use exact match for `/admin/settings` (System):
   ```
   const isChildActive = (() => {
     if (childPath === '/admin/settings') {
       return pathname === '/admin/settings';
     }
     return pathname.startsWith(childPath);
   })();
   ```
   This ensures:
   - On `/admin/settings` -> only "System" highlights
   - On `/admin/settings/email-templates` -> only "Email Templates" highlights
   - Other children continue to use startsWith matching

3. Also update `getAutoOpenGroups` — it uses `pathname.startsWith(childPath)` which will correctly open the Settings group for both `/admin/settings` and `/admin/settings/email-templates`, so no change needed there. Verify this is the case.
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Sidebar Email Templates links to /admin/settings/email-templates (not query param)
    - On /admin/settings only System is highlighted
    - On /admin/settings/email-templates only Email Templates is highlighted
    - Settings group auto-opens for both paths
    - No TypeScript errors
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. Navigate to /admin/settings — shows 3 tabs (General, Health, Maintenance), no Email Templates tab
3. Navigate to /admin/settings/email-templates — shows standalone Email Templates page with template list
4. Sidebar: on /admin/settings, only "System" is highlighted
5. Sidebar: on /admin/settings/email-templates, only "Email Templates" is highlighted
6. Email template Edit buttons still navigate to /admin/email-templates/[key]
</verification>

<success_criteria>
- Email Templates is a standalone page at /admin/settings/email-templates
- System Settings has exactly 3 tabs: General, Health, Maintenance
- Sidebar highlighting is correct for both pages
- All existing email template functionality preserved
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/quick/260331-k2r-extract-email-templates-from-system-sett/260331-k2r-SUMMARY.md`
</output>
