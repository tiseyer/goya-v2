---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/PageHero.tsx
autonomous: true
must_haves:
  truths:
    - "Dot grid pattern is visibly noticeable at normal zoom on dark variant"
    - "On hard refresh, hero text does not flash fallback values before DB data loads"
    - "Hero section background and height are always immediately visible — no layout shift"
    - "Edit mode still works correctly for admin users"
  artifacts:
    - path: "app/components/PageHero.tsx"
      provides: "PageHero with improved dot grid visibility and FOFC prevention"
  key_links:
    - from: "contentReady state"
      to: "opacity transition on text content"
      via: "conditional className"
      pattern: "contentReady.*opacity"
---

<objective>
Fix two visual issues in PageHero: (1) make the dot grid background pattern more visible by increasing its opacity, and (2) prevent the flash of fallback content (FOFC) on page load by hiding text until API data resolves or a timeout fires.

Purpose: Polish the hero sections so they look intentional (visible dot grid) and load cleanly (no content flash).
Output: Updated `app/components/PageHero.tsx` with both fixes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/components/PageHero.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Increase dot grid visibility and add FOFC prevention</name>
  <files>app/components/PageHero.tsx</files>
  <action>
Two changes in `app/components/PageHero.tsx`:

**Fix 1 — Dot grid opacity (dark variant only):**
Line 253 currently has `className="absolute inset-0 opacity-[0.04]"`. Change to `opacity-[0.08]` (2x increase). Do NOT change dot size (1px), spacing (28px), or color (white). The light variant has no dot grid — leave it as-is.

**Fix 2 — FOFC prevention:**
Add two new state variables at the top of the component (near existing useState calls):
```tsx
const [contentReady, setContentReady] = useState(!pageSlug); // true immediately if no slug (no fetch needed)
const [showFallback, setShowFallback] = useState(!pageSlug);
```

Update the existing `useEffect` that fetches `/api/page-hero/${pageSlug}` (lines 51-61):
- Add a timeout: `const timer = setTimeout(() => setShowFallback(true), 800);`
- In the `.then()` success handler, after setting rawPill/rawTitle/rawSubtitle, call `setContentReady(true)` and `clearTimeout(timer)`
- In the `.catch()` handler, call `setContentReady(true)` and `clearTimeout(timer)`
- Return cleanup: `return () => clearTimeout(timer);`

Derive a visibility flag: `const isVisible = contentReady || showFallback;`

For BOTH dark and light variants, wrap the text content area (the inner div containing pill, h1, subtitle — NOT the outer section) with a conditional opacity class. Specifically, on the inner content div (the one with `className="relative h-full flex flex-col items-center..."`) add:
```
className={`relative h-full flex flex-col items-center justify-start pt-8 text-center px-4 max-w-3xl mx-auto transition-opacity duration-200 ${isVisible ? 'opacity-100' : 'opacity-0'}`}
```

Apply this to BOTH the dark variant content div (line 269) and the light variant content div (line 352).

Important: The outer `<section>` element must NOT have any opacity change — it stays fully visible with background, height, and decorative elements always showing. Only the text content fades in.

Do NOT change any edit mode behavior. The `contentReady` starts as `true` when `pageSlug` is falsy, so non-fetchable heroes render immediately.
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
- Dot grid opacity is 0.08 (was 0.04) on dark variant
- Text content starts opacity-0 when pageSlug is set, fades in on fetch resolve or after 800ms timeout
- Hero section background/height always fully visible
- TypeScript compiles with zero errors
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with 0 errors
2. Visual check: dot grid visible at normal zoom on dark hero pages (e.g. Events, Academy)
3. Hard refresh on a page with PageHero — no flash of fallback text, text fades in smoothly
4. Hero height stays stable throughout load (no layout shift)
5. Admin edit mode still works (pencil, edit fields, save/cancel)
</verification>

<success_criteria>
- TypeScript compiles clean
- Dot grid noticeably more visible (opacity doubled)
- No FOFC on page load — text hidden until data ready or 800ms elapsed
- Zero layout shift during hero load
</success_criteria>

<output>
After completion, create `.planning/quick/260403-n8u-pagehero-dot-grid-visibility-no-flash-of/260403-n8u-SUMMARY.md`
</output>
