---
phase: quick
plan: 260401-l88
subsystem: admin-ui
tags: [sidebar, navigation, tabs, admin-shell]
tech-stack:
  added: []
  patterns: [useRouter-for-group-navigation, pill-tab-style]
key-files:
  modified:
    - app/admin/components/AdminShell.tsx
    - app/admin/emails/page.tsx
decisions:
  - Emails moved to first child of Settings group — reduces top-level nav clutter and groups all config under one parent
  - toggleGroup accepts full NavGroup item to access children[0].href directly — cleaner than passing label+lookup
  - Group click never collapses — expansion state is managed by pathname useEffect; manual collapse removed to avoid confusing UX
metrics:
  duration: ~8m
  completed: 2026-04-01
  tasks_completed: 2
  files_modified: 2
---

# Quick Task 260401-l88: Fix 4 Admin Navigation/UI Issues Summary

**One-liner:** Moved Emails into Settings group, group click now expands+navigates, removed parent active highlight, and replaced underline tabs with pill-style tabs on email page.

## Tasks Completed

| # | Task | Commit | Files |
|---|------|--------|-------|
| 1 | Fix sidebar: Emails in Settings, expand+navigate, no parent highlight | 1bdcac4 | app/admin/components/AdminShell.tsx |
| 2 | Restyle email page tabs to pill style | c964378 | app/admin/emails/page.tsx |

## Changes Made

### Task 1 — AdminShell.tsx (3 sub-fixes)

**Fix 1: Emails moved into Settings group**
- Removed standalone `type: 'link'` Emails entry from NAV_ITEMS
- Removed the `{ type: 'divider' }` that followed it (between Shop and Settings)
- Added Emails as `children[0]` of the Settings group (before System)

**Fix 3: Group click = expand + navigate**
- Added `useRouter` import from `next/navigation`
- Changed `toggleGroup(label: string)` to `toggleGroup(item: NavGroup)`
- New behavior: always adds group to openGroups (never removes), then `router.push(item.children[0].href)`
- Group button onClick updated from `toggleGroup(item.label)` to `toggleGroup(item)`

**Fix 4: Remove parent group active highlight**
- Removed `isAnyChildActive ? 'bg-primary/10 text-primary font-semibold'` ternary from group button className
- Group button always uses `'text-slate-500 hover:text-primary-dark hover:bg-primary-50'`
- `strokeWidth={isAnyChildActive ? 2 : 1.75}` retained as subtle visual indicator

### Task 2 — emails/page.tsx

- Tab container: `flex gap-1 border-b border-slate-200 mb-6` → `flex gap-1 bg-slate-100 rounded-lg p-1 mb-8 w-fit`
- Active tab: `text-primary` + absolute underline span → `bg-white text-[#1B3A5C] shadow-sm`
- Button: added `rounded-md cursor-pointer` to base classes
- Removed `{activeTab === tab.key && <span className="absolute bottom-0 ..." />}` entirely

## Deviations from Plan

None — plan executed exactly as written.

## Self-Check: PASSED

- app/admin/components/AdminShell.tsx: EXISTS (modified)
- app/admin/emails/page.tsx: EXISTS (modified)
- Commit 1bdcac4: FOUND
- Commit c964378: FOUND
- `npx tsc --noEmit`: PASSED (no output = clean)
