---
phase: quick
plan: 260401-l88
type: execute
wave: 1
depends_on: []
files_modified:
  - app/admin/components/AdminShell.tsx
  - app/admin/emails/page.tsx
autonomous: true
must_haves:
  truths:
    - "Emails appears as first child inside the Settings group, not as standalone nav item"
    - "No divider exists between Shop group and Settings group"
    - "Clicking a collapsed group expands it AND navigates to its first child"
    - "Clicking an already-expanded group navigates to its first child (does not collapse)"
    - "Only the active child link has highlight styling, parent group label has no bg highlight"
    - "Email page tabs use pill-style matching Settings page tabs"
  artifacts:
    - path: "app/admin/components/AdminShell.tsx"
      provides: "Sidebar nav with Emails inside Settings group, click-to-navigate groups, child-only highlighting"
    - path: "app/admin/emails/page.tsx"
      provides: "Email management page with pill-style tab bar"
  key_links:
    - from: "AdminShell.tsx group click"
      to: "router.push(firstChild.href)"
      via: "useRouter navigation on group button click"
      pattern: "router\\.push"
---

<objective>
Fix 4 admin navigation/UI issues: move Emails into Settings group, change group click behavior to expand+navigate, remove parent highlight on active child, and restyle email tabs to pill style.

Purpose: Consistent admin UI — sidebar groups behave intuitively and tab styles match across pages.
Output: Updated AdminShell.tsx and emails/page.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/admin/components/AdminShell.tsx
@app/admin/emails/page.tsx
@app/admin/settings/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix sidebar — move Emails, group click behavior, parent highlighting</name>
  <files>app/admin/components/AdminShell.tsx</files>
  <action>
Three changes in AdminShell.tsx:

**1. Move Emails into Settings group (Fix 1):**
- Remove the standalone Emails `type: 'link'` entry (lines 100-107) from NAV_ITEMS
- Remove the divider after it (line 108, the `{ type: 'divider' }` between Emails and Settings)
- Add Emails as the FIRST child of the Settings group, before System:
  ```
  { href: '/admin/emails', label: 'Emails', paths: ['M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z'] }
  ```

**2. Group click = expand + navigate to first child (Fix 3):**
- Import `useRouter` from `next/navigation` (already available in the module)
- Get `const router = useRouter()` inside AdminShell component
- Replace the `toggleGroup` function. New behavior:
  - If group is NOT open: add to openGroups AND router.push(firstChildHref)
  - If group IS open: just router.push(firstChildHref) — do NOT collapse
  - Groups auto-collapse is not needed; they already auto-open via the existing useEffect
- Update the group button's onClick: pass the full group item so toggleGroup can access children[0].href
  - Change signature to `toggleGroup(item: NavGroup)` where `item` is the group
  - Inside: `const firstHref = item.children[0].href;`
  - If not open: `next.add(item.label)` + `router.push(firstHref)`
  - If open: just `router.push(firstHref)`

**3. Remove parent group highlight when child is active (Fix 4):**
- In the group button className array, replace the `isAnyChildActive` ternary:
  - Remove: `isAnyChildActive ? 'bg-primary/10 text-primary font-semibold'`
  - Replace with: always use the non-active style `'text-slate-500 hover:text-primary-dark hover:bg-primary-50'`
  - BUT keep the icon strokeWidth logic: `strokeWidth={isAnyChildActive ? 2 : 1.75}` so the icon is slightly bolder when a child is active (subtle visual cue without background highlight)

Constraint: Run `npx tsc --noEmit` before considering done.
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Emails no longer appears as standalone nav item; it is first child in Settings group
    - Only one divider remains (after Inbox), the one before Settings is gone
    - Clicking collapsed group expands and navigates to first child
    - Clicking expanded group navigates to first child without collapsing
    - Parent group button has no bg-primary/10 highlight when child is active
    - TypeScript compiles cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: Restyle email page tabs to pill style</name>
  <files>app/admin/emails/page.tsx</files>
  <action>
Replace the underline-style tab bar in EmailsPageContent with the pill-style tabs matching Settings page:

**Replace the tab bar container** (the div with `border-b border-slate-200`):
- Old: `<div className="flex gap-1 border-b border-slate-200 mb-6">`
- New: `<div className="flex gap-1 bg-slate-100 rounded-lg p-1 mb-8 w-fit">`

**Replace each tab button className:**
- Old active: `'text-primary'` + absolute positioned underline span
- New active: `'bg-white text-[#1B3A5C] shadow-sm'`
- Old inactive: `'text-slate-500 hover:text-slate-700'`
- New inactive: `'text-slate-500 hover:text-slate-700'` (same)
- Add `rounded-md` to button base classes

**Full button className replacement:**
```
className={`px-4 py-2 rounded-md text-sm font-medium transition-all ${
  activeTab === tab.key
    ? 'bg-white text-[#1B3A5C] shadow-sm'
    : 'text-slate-500 hover:text-slate-700'
}`}
```

**Remove the underline span entirely:**
- Delete the conditional `{activeTab === tab.key && (<span className="absolute bottom-0 ..." />)}` block

Also add `cursor-pointer` to the button classes for consistency.

Constraint: Run `npx tsc --noEmit` before considering done.
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Email page tabs use pill-style container (bg-slate-100 rounded-lg p-1 w-fit)
    - Active tab shows white bg with shadow, no underline
    - Inactive tabs show slate text with hover
    - Visual appearance matches Settings page tab bar
    - TypeScript compiles cleanly
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. Visual check: Emails appears inside Settings group in sidebar
3. Visual check: Clicking Settings group expands and navigates to /admin/emails (first child)
4. Visual check: Only active child highlighted, not parent group label
5. Visual check: Email page tabs match Settings page pill style
</verification>

<success_criteria>
- All 4 fixes implemented and TypeScript compiles cleanly
- Sidebar: Emails is first child of Settings group, no extra divider
- Sidebar: Group click expands + navigates, never collapses on click
- Sidebar: Parent group has no highlight background when child active
- Emails page: Pill-style tabs matching Settings page
</success_criteria>

<output>
After completion, create `.planning/quick/260401-l88-fix-admin-nav-email-position-tab-styling/260401-l88-SUMMARY.md`
Also create `activity/quick-tasks/quick-task_fix-admin-nav-email-tab-styling_01-04-2026.md`
Branch: feature/nav-fixes, merge to develop when complete
</output>
