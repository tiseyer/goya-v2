---
phase: quick
plan: 260403-c2q
type: execute
wave: 1
depends_on: []
files_modified: [app/events/page.tsx]
autonomous: true
requirements: [quick-fix]
must_haves:
  truths:
    - "Sidebar filter boxes have dark-appropriate borders on the events page"
    - "No white/light borders visible against dark background"
  artifacts:
    - path: "app/events/page.tsx"
      provides: "Events page with corrected sidebar border colors"
  key_links: []
---

<objective>
Fix Events sidebar filter box border colors for dark mode. The four sidebar containers (Calendar, Event Type, Format, Category) use `border-slate-200/80` which appears as a light/white line against the dark page background.

Purpose: Visual polish — remove jarring light borders on dark-themed events page.
Output: Updated border classes on four sidebar boxes plus one inner divider.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/events/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update sidebar filter box border colors</name>
  <files>app/events/page.tsx</files>
  <action>
In `app/events/page.tsx`, update the four sidebar filter container divs and one inner divider. All are inside the `<aside>` block starting around line 417.

1. Lines 420, 434, 464, 505 — Four boxes currently have:
   `bg-white rounded-2xl border border-slate-200/80 shadow-sm p-5`
   Change to:
   `bg-white dark:bg-white/5 rounded-2xl border border-slate-200/80 dark:border-white/10 shadow-sm p-5`

2. Line 494 — Inner location filter divider currently has:
   `border-t border-slate-100`
   Change to:
   `border-t border-slate-100 dark:border-white/10`

3. Also fix the text colors inside these boxes for dark mode — the label text at lines 435, 465, 506 uses `text-slate-400` which is fine for dark, and the inactive button text uses `text-slate-600 hover:bg-slate-50` which needs dark variants. Update each inactive button className to add `dark:text-slate-300 dark:hover:bg-white/5`.

Do NOT touch any other elements on the page (event cards, mobile filters, hero, etc.).
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>All four sidebar filter boxes use dark:border-white/10 and dark:bg-white/5 instead of showing light borders on dark background. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with 0 errors
- Visual: sidebar filter boxes show subtle dark borders (white/10) against dark background
- No light/white borders visible on any of the four filter containers
</verification>

<success_criteria>
Events page sidebar filter boxes display correctly on dark background with no jarring light borders.
</success_criteria>

<output>
After completion, create `.planning/quick/260403-c2q-fix-events-sidebar-filter-box-border-col/260403-c2q-SUMMARY.md`
</output>
