---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/_skip_20260403_course_instructors_attendees.sql
  - lib/types.ts
  - app/admin/courses/components/CourseForm.tsx
  - app/admin/courses/[id]/edit/page.tsx
  - app/admin/courses/components/AttendeePicker.tsx
  - app/academy/[id]/page.tsx
  - app/settings/my-courses/page.tsx
autonomous: true
requirements: [courses-parity]
must_haves:
  truths:
    - "Course admin form uses same FormSection styling as EventForm"
    - "Admins can pick instructors, organizers, and attendees on courses"
    - "Visibility toggles control what shows on frontend course detail"
    - "Course detail page shows instructor and organizer sidebar widgets"
    - "Course detail page shows attendees section in main content"
    - "Organizers and admin/moderator see Edit Course button on course detail"
    - "My Courses page shows courses where user is an organizer"
  artifacts:
    - path: "supabase/migrations/_skip_20260403_course_instructors_attendees.sql"
      provides: "course_instructors and course_attendees tables, visibility columns, organizer_ids column"
    - path: "app/admin/courses/components/CourseForm.tsx"
      provides: "Restyled form with FormSection, InstructorPicker, OrganizerPicker, AttendeePicker"
    - path: "app/academy/[id]/page.tsx"
      provides: "Sidebar widgets, attendees section, edit button"
  key_links:
    - from: "CourseForm.tsx"
      to: "course_instructors table"
      via: "supabase insert/delete on save"
    - from: "academy/[id]/page.tsx"
      to: "course_instructors + courses.organizer_ids"
      via: "supabase select queries"
    - from: "my-courses/page.tsx"
      to: "courses.organizer_ids"
      via: ".or() filter including cs organizer_ids"
---

<objective>
Bring courses to feature parity with events: add instructor/organizer/attendee management to the admin form, restyle the form to match EventForm's FormSection pattern, add frontend sidebar widgets and attendees section, add edit permissions for organizers, and expand My Courses query.

Purpose: Courses currently have a text-only instructor field and no organizer/attendee management. Events already have all of this. This task mirrors the event patterns for courses.
Output: Migration file, updated CourseForm, updated course detail page, updated My Courses query.
</objective>

<context>
@lib/types.ts (Course interface — lines 123-148)
@app/admin/events/components/EventForm.tsx (FormSection pattern, InstructorPicker/OrganizerPicker/AttendeePicker usage)
@app/admin/courses/components/CourseForm.tsx (current form to restyle)
@app/admin/courses/[id]/edit/page.tsx (server component that renders form + lessons + audit)
@app/academy/[id]/page.tsx (frontend course detail — needs sidebar widgets + edit button)
@app/events/[id]/page.tsx (reference — sidebar widgets for instructors/organizers, edit button, attendees)
@app/settings/my-courses/page.tsx (My Courses query to expand)
@app/components/OrganizerPicker.tsx (reusable picker — max 5, current user slot 1)
@app/components/InstructorPicker.tsx (reusable picker — max 5, debounced search)
@app/admin/events/components/AttendeePicker.tsx (reference for course AttendeePicker)
@supabase/migrations/_skip_20260403_event_instructors.sql (reference migration pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database migration + type update</name>
  <files>supabase/migrations/_skip_20260403_course_instructors_attendees.sql, lib/types.ts</files>
  <action>
Create migration file `supabase/migrations/_skip_20260403_course_instructors_attendees.sql` following the exact pattern from `_skip_20260403_event_instructors.sql`:

1. **course_instructors table** (mirrors event_instructors):
   - `course_id uuid NOT NULL REFERENCES courses(id) ON DELETE CASCADE`
   - `profile_id uuid NOT NULL REFERENCES profiles(id) ON DELETE CASCADE`
   - `created_at timestamptz NOT NULL DEFAULT now()`
   - `PRIMARY KEY (course_id, profile_id)`
   - Enable RLS
   - Policy "Anyone can read course instructors" FOR SELECT USING (true)
   - Policy "Organizers and admins can manage course instructors" FOR ALL USING same pattern as event_instructors but referencing courses table and course_instructors.course_id

2. **course_attendees table** (mirrors event_attendees pattern):
   - `course_id uuid NOT NULL REFERENCES courses(id) ON DELETE CASCADE`
   - `profile_id uuid NOT NULL REFERENCES profiles(id) ON DELETE CASCADE`
   - `created_at timestamptz NOT NULL DEFAULT now()`
   - `PRIMARY KEY (course_id, profile_id)`
   - Enable RLS
   - Policy "Anyone can read course attendees" FOR SELECT USING (true)
   - Policy "Organizers and admins can manage course attendees" FOR ALL USING same pattern as instructors policy

3. **Add columns to courses table:**
   - `ALTER TABLE courses ADD COLUMN IF NOT EXISTS show_organizers boolean NOT NULL DEFAULT true;`
   - `ALTER TABLE courses ADD COLUMN IF NOT EXISTS show_instructors boolean NOT NULL DEFAULT true;`
   - `ALTER TABLE courses ADD COLUMN IF NOT EXISTS show_attendees boolean NOT NULL DEFAULT false;`
   - `ALTER TABLE courses ADD COLUMN IF NOT EXISTS organizer_ids uuid[] NOT NULL DEFAULT '{}';`
   - `ALTER TABLE courses ADD COLUMN IF NOT EXISTS created_by uuid REFERENCES profiles(id);`

4. **Update Course interface** in `lib/types.ts` — add after `updated_at`:
   - `created_by: string | null;`
   - `show_organizers: boolean;`
   - `show_instructors: boolean;`
   - `show_attendees: boolean;`
   - `organizer_ids: string[];`

Run `npx supabase db push` after creating the migration.
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Migration file exists, db push succeeds, Course type includes new fields, tsc passes</done>
</task>

<task type="auto">
  <name>Task 2: Admin form restyle + instructor/organizer/attendee pickers</name>
  <files>app/admin/courses/components/CourseForm.tsx, app/admin/courses/[id]/edit/page.tsx, app/admin/courses/components/AttendeePicker.tsx</files>
  <action>
**A. Update CourseForm props** to match EventForm pattern:
- Change Props interface to: `{ course?: Course; categories: CourseCategoryRow[]; userRole?: string; currentUserId?: string; currentUserName?: string; currentUserAvatar?: string | null; }`

**B. Add FormSection helper** inside CourseForm.tsx (copy from EventForm):
```
function FormSection({ title, description, children }: { title: string; description?: string; children: React.ReactNode }) {
  return (
    <div className="bg-white rounded-xl border border-goya-border shadow-soft p-6">
      <div className="mb-5">
        <h3 className="text-sm font-semibold text-primary-dark">{title}</h3>
        {description && <p className="text-xs text-foreground-secondary mt-1">{description}</p>}
      </div>
      {children}
    </div>
  );
}
```

**C. Replace all 3 card sections** with FormSection:
- Section 1: `<FormSection title="Basic Info" description="Core course details">`
- Section 2: `<FormSection title="Content" description="Descriptions and visual appearance">`
- Section 3: `<FormSection title="Settings" description="Publishing and duration">`

**D. Update INPUT/LABEL/SELECT constants** to use design tokens:
- INPUT: `w-full px-3 py-2.5 rounded-lg border border-goya-border text-sm text-foreground bg-white focus:outline-none focus:ring-1 focus:ring-primary focus:border-primary transition-colors`
- LABEL: `block text-xs font-semibold text-foreground mb-1.5 uppercase tracking-wide`
- SELECT: same as INPUT with `cursor-pointer`

**E. Remove old text instructor field.** Replace with state variables mirroring EventForm:
- `const [instructorIds, setInstructorIds] = useState<string[]>([]);`
- `const [showInstructors, setShowInstructors] = useState(course?.show_instructors ?? true);`
- `const [organizerIds, setOrganizerIds] = useState<string[]>(() => (course?.organizer_ids ?? []).filter(id => id !== currentUserId));`
- `const [showOrganizers, setShowOrganizers] = useState(course?.show_organizers ?? true);`
- `const [showAttendees, setShowAttendees] = useState(course?.show_attendees ?? false);`
- Keep `instructor` text state for backward compat in payload but do NOT render the text input.

**F. Load existing instructor IDs** on edit (useEffect, same as EventForm):
```
useEffect(() => {
  if (!course?.id) return;
  supabase.from('course_instructors').select('profile_id').eq('course_id', course.id)
    .then(({ data }) => { if (data) setInstructorIds(data.map(r => r.profile_id)); });
}, [course?.id]);
```

**G. Add Instructor, Organizer, and Attendee FormSections** after Settings section, before Actions. Mirror EventForm exactly:
- Import `OrganizerPicker` from `@/app/components/OrganizerPicker`
- Import `InstructorPicker` from `@/app/components/InstructorPicker`
- Import `AttendeePicker` from `./AttendeePicker`
- Each section has a visibility toggle checkbox: `<label className="flex items-center gap-2 cursor-pointer mt-3"><input type="checkbox" checked={showX} onChange={...} className="rounded border-goya-border text-primary focus:ring-primary" /> <span className="text-xs text-foreground-secondary">Show X on course page</span></label>`
- Attendee section only visible in edit mode (`isEdit && course?.id`)

**H. Update handleSubmit** to:
1. Add `show_organizers`, `show_instructors`, `show_attendees`, `organizer_ids` (prepend currentUserId like EventForm) to payload
2. After successful course save (both create and edit), sync course_instructors join table:
   - Delete existing: `await supabase.from('course_instructors').delete().eq('course_id', courseId);`
   - Insert new: `if (instructorIds.length > 0) await supabase.from('course_instructors').insert(instructorIds.map(pid => ({ course_id: courseId, profile_id: pid })));`
3. Keep `instructor` field in payload as null (or first instructor name as text fallback — optional)

**I. Create AttendeePicker** at `app/admin/courses/components/AttendeePicker.tsx`. Copy from `app/admin/events/components/AttendeePicker.tsx` but change:
- All `event_id` references to `course_id`
- All `event_attendees` table references to `course_attendees`
- Props: `{ courseId: string; unlimitedSpots?: boolean; spotsTotal?: number | null; currentUserRole: string; currentUserId: string; }`

**J. Update edit page server component** (`app/admin/courses/[id]/edit/page.tsx`):
1. Pass new props to CourseForm: `userRole={userRole}`, `currentUserId={user!.id}`, `currentUserName={profileRow?.full_name ?? user!.email ?? ''}`, `currentUserAvatar={profileRow?.avatar_url ?? null}`
2. Update profile select to include `full_name, avatar_url, email`: `.select('role, full_name, avatar_url, email')`
3. Move LessonSection ABOVE the actions div but still outside the form — actually LessonSection is already outside the form (it's rendered after CourseForm in the page). Move it between CourseForm and the audit history to keep lessons prominent. Already in correct order.
4. Wrap audit history in a collapsible `<details>` element:
   ```
   <details className="mt-10 max-w-3xl">
     <summary className="text-lg font-bold text-[#1B3A5C] mb-4 cursor-pointer select-none">Course History</summary>
     ...existing timeline JSX...
   </details>
   ```
5. Add a "View Course" link next to the title, similar to how events have a "View Event" button:
   ```
   <Link href={`/academy/${id}`} className="text-sm text-primary hover:text-primary-dark font-medium">View Course</Link>
   ```
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>CourseForm renders with FormSection styling matching EventForm. InstructorPicker, OrganizerPicker, and AttendeePicker sections present. Visibility toggles wired. Save persists instructors to join table and organizer_ids/show_* to courses table. tsc passes.</done>
</task>

<task type="auto">
  <name>Task 3: Frontend sidebar widgets + edit permissions + My Courses query</name>
  <files>app/academy/[id]/page.tsx, app/settings/my-courses/page.tsx</files>
  <action>
**A. Course detail page — data fetching** (`app/academy/[id]/page.tsx`):

Add after the existing lessons query, following the exact pattern from `app/events/[id]/page.tsx`:

1. **Auth + canManage check:**
   ```
   const { data: { user: authUser } } = await supabase.auth.getUser();
   let canManage = false;
   if (authUser) {
     if (course.organizer_ids?.includes(authUser.id)) canManage = true;
     if (!canManage) {
       const { data: authProfile } = await supabase.from('profiles').select('role').eq('id', authUser.id).single();
       if (authProfile?.role === 'admin' || authProfile?.role === 'moderator') canManage = true;
     }
   }
   ```
   Note: `user` variable already exists for enrollment — rename the auth check to `authUser` or reuse `user` (it's the same). Reuse the existing `user` variable and just add the canManage logic.

2. **Fetch organizer profiles** (same pattern as events):
   ```
   let organizers: { id: string; full_name: string | null; avatar_url: string | null; username: string | null }[] = [];
   if (course.organizer_ids && course.organizer_ids.length > 0) {
     const { data: orgProfiles } = await supabase.from('profiles').select('id, full_name, avatar_url, username').in('id', course.organizer_ids);
     organizers = orgProfiles ?? [];
   }
   ```

3. **Fetch instructor profiles** (from join table):
   ```
   let instructors: { id: string; full_name: string | null; avatar_url: string | null; username: string | null }[] = [];
   const { data: instructorRows } = await supabase.from('course_instructors').select('profile_id').eq('course_id', id);
   if (instructorRows && instructorRows.length > 0) {
     const { data: instrProfiles } = await supabase.from('profiles').select('id, full_name, avatar_url, username').in('id', instructorRows.map(r => r.profile_id));
     instructors = instrProfiles ?? [];
   }
   ```

4. **Fetch attendee count + profiles (first 20):**
   ```
   const { count: attendeeCount } = await supabase.from('course_attendees').select('*', { count: 'exact', head: true }).eq('course_id', id);
   let attendees: { id: string; full_name: string | null; avatar_url: string | null; username: string | null }[] = [];
   if (course.show_attendees) {
     const { data: attData } = await supabase.from('course_attendees').select('profile_id').eq('course_id', id).order('created_at', { ascending: true }).limit(20);
     if (attData && attData.length > 0) {
       const { data: attProfiles } = await supabase.from('profiles').select('id, full_name, avatar_url, username').in('id', attData.map(r => r.profile_id));
       attendees = attProfiles ?? [];
     }
   }
   ```

**B. Course detail page — canManage check:**
Use the existing `user` variable. Add canManage logic right after the auth/progress section. Check `course.organizer_ids?.includes(user.id)` OR role is admin/moderator. Reuse the profile fetch if possible, or do a separate one.

**C. Course detail page — RIGHT column widgets:**

Add to the RIGHT column (inside `<div className="sticky top-24">` wrapper, after `CourseEnrollCard`):

1. **Edit button** (above CourseEnrollCard or right above the sticky block):
   ```
   {canManage && (
     <div className="mb-4">
       <Link href={`/admin/courses/${id}/edit`} className="block text-center py-2.5 px-4 bg-primary text-white text-sm font-semibold rounded-xl hover:bg-primary-dark transition-colors">
         Edit Course
       </Link>
     </div>
   )}
   ```

2. **Instructor widget** (after CourseEnrollCard, same JSX as events page lines 270-294):
   - Show when `instructors.length > 0 && course.show_instructors !== false`
   - Same card styling: `bg-white rounded-2xl border border-slate-200/80 shadow-sm p-6 mt-4`
   - Header: `text-[10px] font-bold text-slate-400 uppercase tracking-widest mb-3`
   - Each instructor: Link to `/members/{username ?? id}` with avatar + name
   - Include text fallback for `course.instructor` when no join table instructors exist (backward compat)

3. **Organizer widget** (after instructors, same as events page lines 309-335):
   - Show when `organizers.length > 0 && course.show_organizers !== false`
   - Same styling pattern

**D. Course detail page — LEFT column attendees section:**

Add after "Course Content" card, before closing the left column div:
```
{course.show_attendees && attendees.length > 0 && (
  <div className="bg-white rounded-2xl border border-slate-100 shadow-sm p-6 sm:p-8">
    <div className="flex items-center justify-between mb-4">
      <h2 className="text-lg font-bold text-[#1B3A5C]">Attendees</h2>
      <span className="text-xs text-slate-400 font-medium">{attendeeCount ?? 0} enrolled</span>
    </div>
    <div className="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-3">
      {attendees.map((att) => (
        <Link key={att.id} href={`/members/${att.username ?? att.id}`} className="flex items-center gap-2 group">
          {att.avatar_url ? (
            <img src={att.avatar_url} alt={att.full_name ?? 'Attendee'} className="w-7 h-7 rounded-full object-cover shrink-0" />
          ) : (
            <div className="w-7 h-7 rounded-full bg-primary-light/15 flex items-center justify-center text-primary font-bold text-[10px] shrink-0">
              {(att.full_name ?? '?')[0].toUpperCase()}
            </div>
          )}
          <p className="text-xs font-medium text-primary-dark group-hover:text-primary transition-colors truncate">
            {att.full_name ?? 'Member'}
          </p>
        </Link>
      ))}
    </div>
    {(attendeeCount ?? 0) > 20 && (
      <p className="text-xs text-slate-400 mt-3">and {(attendeeCount ?? 0) - 20} more...</p>
    )}
  </div>
)}
```

**E. My Courses query expansion** (`app/settings/my-courses/page.tsx`):

Change the courses query from:
```
.eq('created_by', user.id).eq('course_type', 'member').neq('status', 'deleted')
```
To:
```
.or(`created_by.eq.${user.id},organizer_ids.cs.{${user.id}}`)
.eq('course_type', 'member')
.neq('status', 'deleted')
```
This shows courses where the user is either the creator OR listed as an organizer.
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Course detail page shows instructor/organizer sidebar widgets when data exists and visibility is enabled. Attendees section shows in main content with 20-person limit. Edit button visible to organizers and admin/moderator. My Courses includes organizer-linked courses. tsc passes.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with 0 errors
2. `npx supabase db push` succeeds (migration applied)
3. Admin course edit page renders with FormSection styling
4. Instructor/Organizer/Attendee pickers functional on admin form
5. Course detail page shows sidebar widgets for instructors and organizers
6. Edit Course button appears for organizers and admin/moderator users
7. My Courses page includes organizer-linked courses
</verification>

<success_criteria>
- Course admin form visually matches EventForm's FormSection pattern
- All three picker components (instructor, organizer, attendee) work on course edit
- Visibility toggles control frontend display of each section
- Frontend course detail mirrors event detail sidebar widget pattern
- Edit permissions check organizer_ids array + admin/moderator role
- My Courses query uses .or() to include organizer-linked courses
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/quick/260403-hb7-courses-parity-with-events-admin-form-fr/260403-hb7-SUMMARY.md`
</output>
