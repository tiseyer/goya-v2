# Quick Task 260404-m1b: Fix test user slot search dropdown clipping

## Task 1 — Portal the search dropdown with position: fixed

**Files:** `app/admin/settings/components/TestUsersTab.tsx`
**Action:**
- Add `createPortal` from `react-dom`
- Add `inputWrapperRef` on the search input wrapper for position tracking
- Add `dropdownRef` for outside-click detection on the portal element
- Calculate dropdown position with `getBoundingClientRect()` on focus and search
- Render results dropdown via `createPortal(..., document.body)` with `position: fixed`, `zIndex: 9999`
- Update outside-click handler to check both containerRef and dropdownRef

**Verify:** Dropdown renders above all containers, no clipping from parent overflow
**Done:** Commit `a74cb3e`
