---
quick_id: 260401-hg8
title: "Fix Vercel build: resolve 4 module-not-found errors in academy page"
mode: quick
date: 2026-04-01
---

# Quick Task Plan: Fix Vercel Build Errors

## Background

The Vercel build fails with 4 "Module not found" errors in `app/academy/[id]/page.tsx`:
1. `./CourseEnrollCard`
2. `./actions`
3. `@/app/components/ui/PageContainer`
4. `@/lib/supabaseServer`

**Root cause:** All 4 files exist locally but are **untracked in git** — they were never committed. The entire `app/` and `lib/` directories show as untracked. The imports are correct; the files just need to be committed and pushed.

## Task 1: Stage and commit the missing source files

**Action:** Add the specific files referenced by the failing imports to git and commit them. Then run `npx tsc --noEmit` to verify no TypeScript errors remain for these files. Push to the `develop` branch.

**Files:**
- `app/academy/[id]/CourseEnrollCard.tsx` (untracked)
- `app/academy/[id]/actions.ts` (untracked)
- `app/components/ui/PageContainer.tsx` (untracked)
- `lib/supabaseServer.ts` (untracked)

**Verify:** `npx tsc --noEmit` completes without errors related to these 4 modules

**Done:** The 4 module-not-found errors are resolved; files are committed and pushed to `develop`

**IMPORTANT:** Only commit the 4 specific files that cause the build errors. Do NOT stage the entire `app/` or `lib/` directory — other untracked files are out of scope for this fix.
