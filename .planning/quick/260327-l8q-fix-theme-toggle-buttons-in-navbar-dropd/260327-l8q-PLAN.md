---
phase: quick
plan: 260327-l8q
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/ThemeToggle.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Theme toggle in navbar dropdown spans full dropdown width as a segmented control"
    - "Each of the three buttons (Light, System, Dark) takes equal 1/3 width"
    - "Active theme button shows highlighted background (bg-primary/15 text-primary)"
    - "Non-active buttons show hover state on mouse-over"
  artifacts:
    - path: "app/components/ThemeToggle.tsx"
      provides: "ThemeInline segmented control"
      contains: "flex-1"
  key_links: []
---

<objective>
Fix the ThemeInline component in the navbar dropdown so the three theme buttons (Light, System, Dark) display as a full-width segmented control instead of small icon-only buttons with gaps.

Purpose: Improve usability and visual consistency of the theme switcher in the profile dropdown.
Output: Updated ThemeInline component with segmented control layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/components/ThemeToggle.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle ThemeInline as full-width segmented control</name>
  <files>app/components/ThemeToggle.tsx</files>
  <action>
Modify the ThemeInline component (lines 102-134) with these specific changes:

1. **Container div** (line 110): Change from:
   `flex items-center justify-center gap-1`
   To:
   `flex w-full bg-surface-muted rounded-lg p-1`
   This makes it a full-width segmented control with a subtle background pill.

2. **Each button** (lines 121-126): Change from:
   `p-2 rounded-lg transition-all duration-150 cursor-pointer`
   To:
   `flex-1 flex items-center justify-center gap-1.5 py-1.5 rounded-md text-xs font-medium transition-all duration-150 cursor-pointer`
   This makes each button take 1/3 width and centers icon+label.

3. **Active state**: Change from `bg-primary/15 text-primary` to `bg-surface text-primary shadow-sm` for a proper segmented control feel (raised selected segment).

4. **Inactive state**: Keep `text-foreground-tertiary hover:text-foreground-secondary` but remove `hover:bg-surface-muted` (the container already has that background).

5. **Add label text** next to each icon inside the button: After `{t.icon}`, add `<span>{t.label}</span>` so each segment shows icon + label (matching the segmented control pattern). The `title` attribute can remain for accessibility.

Do NOT change ThemeCards, persistTheme, or the THEMES array.
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx next lint --file app/components/ThemeToggle.tsx 2>&1 | head -20</automated>
  </verify>
  <done>ThemeInline renders as a full-width segmented control with three equal-width buttons showing icon + label, active state has raised appearance, no lint errors.</done>
</task>

</tasks>

<verification>
- `npx next lint --file app/components/ThemeToggle.tsx` passes with no errors
- Visual: navbar dropdown shows theme toggle spanning full width with Light | System | Dark segments
</verification>

<success_criteria>
- ThemeInline is a full-width segmented control with three equal buttons
- Active theme has visually distinct raised/highlighted appearance
- Each button shows icon + label text
- No changes to ThemeCards or other exports
</success_criteria>

<output>
After completion, create `.planning/quick/260327-l8q-fix-theme-toggle-buttons-in-navbar-dropd/260327-l8q-SUMMARY.md`
Also create `activity/quick-tasks/quick-task_fix-theme-toggle-buttons_27-03-2026.md` per CLAUDE.md instructions.
</output>
