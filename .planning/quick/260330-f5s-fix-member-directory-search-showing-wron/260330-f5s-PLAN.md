---
phase: quick
plan: 260330-f5s
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/members-actions.ts
  - lib/members-data.ts
  - app/members/page.tsx
  - app/members/MapPanel.tsx
  - app/members/[id]/page.tsx
autonomous: true
requirements: [QUICK-FIX]
must_haves:
  truths:
    - "Member directory shows real profiles from Supabase, not mock data"
    - "Search by name, city, country filters real profiles"
    - "Role, country, designation, and style filters work against real data"
    - "Map displays real member locations (members with city/country)"
    - "Individual member profile page no longer falls back to static data"
  artifacts:
    - path: "lib/members-actions.ts"
      provides: "Server action fetching profiles from Supabase"
      exports: ["fetchMembers"]
    - path: "app/members/page.tsx"
      provides: "Client component using real data via server action"
  key_links:
    - from: "app/members/page.tsx"
      to: "lib/members-actions.ts"
      via: "server action call on mount"
      pattern: "fetchMembers"
    - from: "lib/members-actions.ts"
      to: "supabase.from('profiles')"
      via: "getSupabaseService query"
      pattern: "getSupabaseService.*profiles"
---

<objective>
Replace hardcoded mock data in the /members directory page with real Supabase profile queries.

Purpose: The member directory currently shows 330 fake members from `lib/members-data.ts` instead of real registered users. Search returns wrong results because it searches mock data.
Output: Working member directory backed by Supabase `profiles` table with all filters, search, map, and inline profile functioning against real data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@lib/members-data.ts (Member interface and mock data — to be replaced)
@app/members/page.tsx (client component with filters, map, search)
@app/members/MapPanel.tsx (map component expecting Member[])
@app/members/[id]/page.tsx (profile page with static fallback)
@lib/chatbot/chat-actions.ts (example server action pattern)
@lib/supabase/service.ts (getSupabaseService for service-role queries)
@app/admin/users/page.tsx (reference for profiles query patterns)

<interfaces>
From lib/members-data.ts:
```typescript
export type MemberRole = 'Teacher' | 'Student' | 'School' | 'Wellness Practitioner';

export interface Member {
  id: string;
  name: string;
  role: MemberRole;
  country: string;
  city: string;
  coordinates: [number, number]; // [lng, lat]
  bio: string;
  photo: string;
  designations: string[];
  teachingStyles: string[];
  specialties: string[];
  credits: { CE: number; Community: number; Karma: number; Practice: number; };
  social: { website?: string; instagram?: string; youtube?: string; };
  memberSince: string;
  featured?: boolean;
  is_verified?: boolean;
  introduction?: string;
  videoIntroUrl?: string;
}
```

From lib/supabase/service.ts:
```typescript
export function getSupabaseService(): SupabaseClient;
```

Supabase `profiles` fields available for mapping:
- id, full_name, first_name, last_name, email, username
- role (enum: student, teacher, wellness_practitioner, moderator, admin)
- member_type (string: teacher, student, school, wellness_practitioner)
- avatar_url, bio, city, country, location
- designations (string[]), teaching_styles (string[]), wellness_designations (string[])
- is_verified, verification_status, subscription_status
- website, instagram, youtube, facebook, tiktok
- youtube_intro_url, created_at
- teaching_focus_arr (string[]), influences_arr (string[])
- onboarding_completed (boolean)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create server action and update Member interface</name>
  <files>lib/members-actions.ts, lib/members-data.ts</files>
  <action>
1. Create `lib/members-actions.ts` as a `'use server'` module with `import 'server-only'`.

2. Export `fetchMembers()` server action that:
   - Uses `getSupabaseService()` to query `profiles` table
   - Selects: id, full_name, first_name, last_name, avatar_url, bio, introduction, city, country, role, member_type, designations, teaching_styles, wellness_designations, is_verified, verification_status, website, instagram, youtube, youtube_intro_url, created_at, onboarding_completed
   - Filters: only `onboarding_completed = true` (skip incomplete profiles)
   - Orders by `created_at desc`
   - Maps each profile row to the existing `Member` interface shape:
     - `id`: profile.id (UUID string)
     - `name`: full_name, or `${first_name} ${last_name}`, fallback to email prefix
     - `role`: Map using member_type first (if 'school' -> 'School'), then role enum ('teacher' -> 'Teacher', 'student' -> 'Student', 'wellness_practitioner' -> 'Wellness Practitioner'). Skip moderator/admin roles from directory display OR show them as their member_type.
     - `country`: profile.country ?? 'Unknown'
     - `city`: profile.city ?? ''
     - `coordinates`: [0, 0] as placeholder (no lat/lng in profiles table; map will just not pin members without real coords — this is acceptable)
     - `bio`: profile.bio ?? ''
     - `photo`: profile.avatar_url ?? ''
     - `designations`: [...(profile.designations ?? []), ...(profile.wellness_designations ?? [])]
     - `teachingStyles`: profile.teaching_styles ?? []
     - `specialties`: [] (no direct equivalent)
     - `credits`: { CE: 0, Community: 0, Karma: 0, Practice: 0 } (credits system not yet wired)
     - `social`: { website: profile.website, instagram: profile.instagram, youtube: profile.youtube }
     - `memberSince`: extract year from created_at (e.g., '2026')
     - `is_verified`: profile.verification_status === 'verified'
     - `introduction`: profile.introduction ?? undefined (if field exists, otherwise skip)
     - `videoIntroUrl`: profile.youtube_intro_url ?? undefined
   - Returns `{ members: Member[], allDesignations: string[], allTeachingStyles: string[] }` where the designation/style arrays are computed from the fetched members (unique sorted values).

3. Update `lib/members-data.ts`:
   - KEEP the `Member` interface export and `MemberRole` type export (other files import these)
   - REMOVE the hardcoded `members` array (all 330 entries)
   - REMOVE the `allDesignations` and `allTeachingStyles` computed exports
   - Add a comment: `// Member data now fetched via server action in lib/members-actions.ts`
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx tsc --noEmit lib/members-actions.ts 2>&1 | head -20</automated>
  </verify>
  <done>Server action exists, compiles, Member interface preserved in members-data.ts, mock data removed</done>
</task>

<task type="auto">
  <name>Task 2: Wire page components to server action and remove static fallbacks</name>
  <files>app/members/page.tsx, app/members/MapPanel.tsx, app/members/[id]/page.tsx</files>
  <action>
1. Update `app/members/page.tsx`:
   - Keep as `'use client'` component
   - Remove import of `members, allDesignations, allTeachingStyles` from `@/lib/members-data`
   - Keep import of `MemberRole, Member` types from `@/lib/members-data`
   - Add import of `fetchMembers` from `@/lib/members-actions`
   - Add state: `const [allMembers, setAllMembers] = useState<Member[]>([])` and `const [loading, setLoading] = useState(true)`
   - Add state for designations/styles: `const [allDesignations, setAllDesignations] = useState<string[]>([])` and `const [allTeachingStyles, setAllTeachingStyles] = useState<string[]>([])`
   - Add useEffect on mount to call `fetchMembers()` and populate all three state vars, then set loading=false
   - Replace all references to the imported `members` constant with `allMembers` state:
     - `filtered` useMemo: `allMembers.filter(...)` instead of `members.filter(...)`
     - `availableCountries` useMemo: derive from `allMembers` with `[allMembers]` dependency
     - `handleSelect`: `allMembers.find(...)` instead of `members.find(...)`
     - `handleMobileSelect`: same replacement
     - `handleMapMemberClick`: same replacement
     - Count display: `allMembers.length` instead of `members.length`
     - MapPanel props: `allMembers={allMembers}` instead of `allMembers={members}`
   - Add loading state UI: when `loading` is true, show a centered spinner or skeleton in place of the member list. Simple approach: show "Loading members..." text with a subtle animation.
   - Add empty state: when `!loading && allMembers.length === 0`, show "No members found" message.

2. Update `app/members/MapPanel.tsx`:
   - Only change needed: update import of `Member` type from `@/lib/members-data` to still work (it should, since Member type stays in members-data.ts)
   - No functional changes needed — it receives Member[] via props

3. Update `app/members/[id]/page.tsx`:
   - Remove import of `members as staticMembers` from `@/lib/members-data`
   - Remove the `staticMember` fallback logic (lines finding in staticMembers, the STATIC_ROLE_MAP for fallback, and the ternary that builds a fake profile object from static data)
   - Simplify: if `!profileData`, call `notFound()` directly
   - Remove the `STATIC_ROLE_MAP` constant that was only used for the static fallback conversion
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx next build 2>&1 | tail -30</automated>
  </verify>
  <done>
    - /members page loads real profiles from Supabase on mount
    - Search filters against real member names/locations
    - Role, country, designation, style filters work with real data
    - Map receives real member data via props
    - /members/[id] page only uses Supabase profile data, no static fallback
    - Mock data array removed from lib/members-data.ts
    - Build succeeds with no type errors
  </done>
</task>

</tasks>

<verification>
1. `npx next build` completes without errors
2. Visit /members — page loads, shows real profiles (not Jennifer Walsh etc.)
3. Type a real member name in search — results filter correctly
4. Apply role filter — only matching roles shown
5. Click a member — inline profile shows real data
6. Click "Full profile" link — /members/[id] page loads from Supabase
7. Map shows markers for members (those with city data)
</verification>

<success_criteria>
- Member directory shows real Supabase profiles instead of 330 mock entries
- All filters (search, role, country, designation, style) operate on real data
- No references to mock member data remain in page components
- Build passes, no TypeScript errors
- Member interface preserved for MapPanel and other consumers
</success_criteria>

<output>
After completion, create `.planning/quick/260330-f5s-fix-member-directory-search-showing-wron/260330-f5s-SUMMARY.md`
Also create `activity/quick-tasks/quick-task_fix-member-directory-real-data_30-03-2026.md`
</output>
