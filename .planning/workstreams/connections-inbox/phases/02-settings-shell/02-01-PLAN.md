---
phase: 02-settings-shell
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/settings/layout.tsx
  - app/settings/components/SettingsShell.tsx
  - app/settings/page.tsx
  - app/settings/subscriptions/page.tsx
  - app/settings/connections/page.tsx
  - app/settings/inbox/page.tsx
autonomous: false
requirements:
  - SHELL-01
  - SHELL-02
  - SHELL-03
  - SHELL-04

must_haves:
  truths:
    - "Navigating to /settings renders a sidebar with General, Subscriptions, Connections, Inbox in that order"
    - "The active sidebar item is visually highlighted with bg-primary/10 and text-primary"
    - "The layout mirrors AdminShell structure: sticky sidebar + scrollable content area"
    - "Each of the four routes renders without 404"
  artifacts:
    - path: "app/settings/components/SettingsShell.tsx"
      provides: "Sidebar-navigated settings shell component"
      contains: "NAV_ITEMS"
    - path: "app/settings/layout.tsx"
      provides: "Server layout with auth guard wrapping SettingsShell"
      exports: ["default"]
    - path: "app/settings/page.tsx"
      provides: "General settings page (redirect or stub)"
    - path: "app/settings/subscriptions/page.tsx"
      provides: "Subscriptions stub page"
    - path: "app/settings/connections/page.tsx"
      provides: "Connections stub page"
    - path: "app/settings/inbox/page.tsx"
      provides: "Inbox stub page"
  key_links:
    - from: "app/settings/layout.tsx"
      to: "app/settings/components/SettingsShell.tsx"
      via: "import and render as wrapper"
      pattern: "import SettingsShell"
    - from: "app/settings/components/SettingsShell.tsx"
      to: "/settings, /settings/subscriptions, /settings/connections, /settings/inbox"
      via: "NAV_ITEMS href values rendered as Next.js Link components"
      pattern: "href.*settings"
    - from: "app/settings/components/SettingsShell.tsx"
      to: "usePathname"
      via: "Active item detection via pathname.startsWith(item.href)"
      pattern: "pathname.startsWith"
---

<objective>
Build the Settings shell at `app/settings/` with a sidebar-navigated layout that mirrors the Admin Settings pattern from `AdminShell.tsx`.

Purpose: Provide the structural skeleton for user-facing settings pages, using the same visual tokens and component architecture as the admin sidebar so both sections feel consistent.

Output: A working `app/settings/` route with sidebar navigation (General, Subscriptions, Connections, Inbox), active-state highlighting, and stub content pages for each route.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@app/admin/components/AdminShell.tsx
@app/admin/layout.tsx

<interfaces>
<!-- AdminShell.tsx pattern — the reference implementation to mirror -->

Key structural elements to replicate:
- `'use client'` directive (uses usePathname, useState, useEffect)
- `NAV_ITEMS` array with `{ href, label, paths (SVG icon paths) }` shape
- `usePathname()` for active item detection via `pathname.startsWith(item.href)`
- Outer container: `<div className="flex min-h-[calc(100vh-4rem)]">`
- Sidebar: `<aside>` with `sticky top-16 h-[calc(100vh-4rem)]` positioning
- Sidebar styling: `bg-white border-r border-slate-200`, collapsible width
- Active item classes: `bg-primary/10 text-primary font-semibold`
- Inactive item classes: `text-slate-500 hover:text-primary-dark hover:bg-primary-50`
- Nav link structure: `flex items-center gap-3 px-2 py-2.5 rounded-xl`
- Content area: `<div className="flex-1 min-w-0 bg-surface-muted">{children}</div>`
- Toggle button with localStorage persistence

Admin layout.tsx pattern:
```typescript
// Server component, auth guard, wraps children in shell
import { redirect } from 'next/navigation';
import { createSupabaseServerClient } from '@/lib/supabaseServer';
import AdminShell from './components/AdminShell';

export default async function AdminLayout({ children }) {
  const supabase = await createSupabaseServerClient();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) redirect('/sign-in');
  // role check...
  return <AdminShell>{children}</AdminShell>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SettingsShell component and layout</name>
  <files>app/settings/components/SettingsShell.tsx, app/settings/layout.tsx</files>
  <read_first>
    - app/admin/components/AdminShell.tsx (reference implementation — mirror this structure)
    - app/admin/layout.tsx (reference layout — mirror auth guard pattern)
  </read_first>
  <action>
Create `app/settings/components/SettingsShell.tsx` as a client component mirroring AdminShell.tsx with these specific differences:

1. **NAV_ITEMS array** — exactly 4 items in this order:
   - `{ href: '/settings', label: 'General', paths: ['M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z', 'M15 12a3 3 0 11-6 0 3 3 0 016 0z'] }` (gear icon, same as admin Settings)
   - `{ href: '/settings/subscriptions', label: 'Subscriptions', paths: ['M3 10h18M7 15h1m4 0h1m-7 4h12a3 3 0 003-3V8a3 3 0 00-3-3H6a3 3 0 00-3 3v8a3 3 0 003 3z'] }` (credit card icon)
   - `{ href: '/settings/connections', label: 'Connections', paths: ['M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z'] }` (people icon)
   - `{ href: '/settings/inbox', label: 'Inbox', paths: ['M20 13V6a2 2 0 00-2-2H6a2 2 0 00-2 2v7m16 0v5a2 2 0 01-2 2H6a2 2 0 01-2-2v-5m16 0h-2.586a1 1 0 00-.707.293l-2.414 2.414a1 1 0 01-.707.293h-3.172a1 1 0 01-.707-.293l-2.414-2.414A1 1 0 006.586 13H4'] }` (inbox icon)

2. **Active item detection** — for the General item (href `/settings`), use exact match `pathname === '/settings'` instead of `startsWith` to avoid it being active on all sub-routes. For all other items, use `pathname.startsWith(item.href)`.

3. **Same visual structure as AdminShell:**
   - Outer: `<div className="flex min-h-[calc(100vh-4rem)]">`
   - Sidebar: `<aside>` with `sticky top-16 h-[calc(100vh-4rem)]` and collapsible width (64px collapsed / 248px expanded)
   - Collapse toggle with localStorage key `'settings-sidebar-collapsed'` (different key from admin)
   - Same toggle button icon, same transition classes
   - Active: `bg-primary/10 text-primary font-semibold`
   - Inactive: `text-slate-500 hover:text-primary-dark hover:bg-primary-50`
   - Nav links: `flex items-center gap-3 px-2 py-2.5 rounded-xl transition-all duration-150`
   - Content: `<div className="flex-1 min-w-0 bg-surface-muted">{children}</div>`

4. **Omit admin-specific features:** No maintenance banner, no badge counts, no pending schools fetch, no supabase import. The sidebar footer should say "Settings" and "v2.0.0-alpha" (matching admin pattern but with "Settings" label instead of "GOYA Admin").

Create `app/settings/layout.tsx` as a server component:
- Import `redirect` from `next/navigation`
- Import `createSupabaseServerClient` from `@/lib/supabaseServer`
- Import `SettingsShell` from `./components/SettingsShell`
- Auth guard: get user via `supabase.auth.getUser()`, redirect to `/sign-in` if no user
- No role check (settings is for all authenticated users, unlike admin)
- Return `<SettingsShell>{children}</SettingsShell>`
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && grep -c "NAV_ITEMS" app/settings/components/SettingsShell.tsx && grep -c "General\|Subscriptions\|Connections\|Inbox" app/settings/components/SettingsShell.tsx && grep -c "bg-primary/10" app/settings/components/SettingsShell.tsx && grep -c "SettingsShell" app/settings/layout.tsx && grep -c "createSupabaseServerClient" app/settings/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - app/settings/components/SettingsShell.tsx exists and contains `'use client'` directive
    - NAV_ITEMS array has exactly 4 items: General, Subscriptions, Connections, Inbox (grep count for these labels = 4)
    - Active state uses `bg-primary/10 text-primary font-semibold` (exact string present)
    - Inactive state uses `text-slate-500 hover:text-primary-dark hover:bg-primary-50` (exact string present)
    - Sidebar uses `sticky top-16 h-[calc(100vh-4rem)]` (exact string present)
    - Content area uses `bg-surface-muted` class
    - localStorage key is `settings-sidebar-collapsed` (not `admin-sidebar-collapsed`)
    - app/settings/layout.tsx exists and imports SettingsShell
    - layout.tsx calls `createSupabaseServerClient` and checks for user
    - layout.tsx does NOT contain role check (no `profile.role` or `includes('admin')`)
    - General href is exactly `/settings` (not `/settings/general`)
  </acceptance_criteria>
  <done>SettingsShell component renders a collapsible sidebar with 4 nav items using identical visual tokens to AdminShell; layout.tsx wraps children in SettingsShell with auth guard</done>
</task>

<task type="auto">
  <name>Task 2: Create stub pages for all four settings routes</name>
  <files>app/settings/page.tsx, app/settings/subscriptions/page.tsx, app/settings/connections/page.tsx, app/settings/inbox/page.tsx</files>
  <read_first>
    - app/settings/components/SettingsShell.tsx (just created — confirm NAV_ITEMS hrefs)
    - app/admin/settings/page.tsx (first 50 lines — reference the Section component pattern for consistent card styling)
  </read_first>
  <action>
Create 4 stub pages. Each is a simple server component with a heading and placeholder text inside a styled card matching the admin pattern. Use the same card styling seen in admin settings: `bg-white rounded-xl border border-[#E5E7EB] shadow-sm`.

1. **app/settings/page.tsx** (General):
```tsx
export default function SettingsGeneralPage() {
  return (
    <div className="p-6 max-w-4xl">
      <h1 className="text-xl font-semibold text-[#1B3A5C] mb-6">General</h1>
      <div className="bg-white rounded-xl border border-[#E5E7EB] shadow-sm p-6">
        <p className="text-sm text-[#6B7280]">Profile settings will appear here.</p>
      </div>
    </div>
  );
}
```

2. **app/settings/subscriptions/page.tsx** (Subscriptions):
   - Same structure, heading "Subscriptions", placeholder "Subscription details will appear here."

3. **app/settings/connections/page.tsx** (Connections):
   - Same structure, heading "Connections", placeholder "Coming soon."

4. **app/settings/inbox/page.tsx** (Inbox):
   - Same structure, heading "Inbox", placeholder "Coming soon."

All pages are simple server components (no `'use client'`). No imports needed beyond React (implicit in Next.js app router).
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && test -f app/settings/page.tsx && test -f app/settings/subscriptions/page.tsx && test -f app/settings/connections/page.tsx && test -f app/settings/inbox/page.tsx && grep -l "General" app/settings/page.tsx && grep -l "Subscriptions" app/settings/subscriptions/page.tsx && grep -l "Connections" app/settings/connections/page.tsx && grep -l "Inbox" app/settings/inbox/page.tsx && echo "ALL PAGES OK"</automated>
  </verify>
  <acceptance_criteria>
    - app/settings/page.tsx exists and contains heading "General"
    - app/settings/subscriptions/page.tsx exists and contains heading "Subscriptions"
    - app/settings/connections/page.tsx exists and contains heading "Connections"
    - app/settings/inbox/page.tsx exists and contains heading "Inbox"
    - All 4 files use `bg-white rounded-xl border border-[#E5E7EB] shadow-sm` card styling
    - None of the 4 files contain `'use client'` (they are server components)
    - All files export a default function
  </acceptance_criteria>
  <done>All four settings routes render stub content without 404, each inside a styled card consistent with admin settings pattern</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify settings shell in browser</name>
  <files>none</files>
  <action>
Human verifies the settings shell renders correctly in the browser, matching AdminShell visual pattern.
  </action>
  <what-built>Settings shell with sidebar navigation at /settings, mirroring the Admin Settings layout. Four routes: General (/settings), Subscriptions (/settings/subscriptions), Connections (/settings/connections), Inbox (/settings/inbox).</what-built>
  <how-to-verify>
    1. Start dev server if not running: `npm run dev`
    2. Log in as any authenticated user
    3. Navigate to http://localhost:3000/settings
    4. Verify sidebar shows 4 items in order: General, Subscriptions, Connections, Inbox
    5. Verify "General" is highlighted (active state) on /settings
    6. Click "Subscriptions" — verify URL changes to /settings/subscriptions and "Subscriptions" becomes active, "General" is no longer active
    7. Click "Connections" — verify same active-state behavior
    8. Click "Inbox" — verify same active-state behavior
    9. Click the collapse toggle (double-chevron button) — verify sidebar collapses to icon-only mode
    10. Reload page — verify collapsed state persists
    11. Compare visual style with /admin (sidebar should feel visually identical: same colors, spacing, rounded corners)
  </how-to-verify>
  <verify>Human confirms visual and functional correctness</verify>
  <done>User approves the settings shell layout and navigation</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `curl -s http://localhost:3000/settings` does not return 404
2. All 4 nav items render in sidebar in correct order
3. Active state highlighting works via pathname matching
4. Visual tokens match AdminShell (bg-primary/10, text-primary, font-semibold, rounded-xl, etc.)
5. Sidebar collapse/expand works with localStorage persistence
</verification>

<success_criteria>
- /settings renders a sidebar-navigated layout identical in structure to Admin Settings
- Sidebar lists General, Subscriptions, Connections, Inbox in that order
- Active item is highlighted with bg-primary/10 and text-primary
- All 4 routes render stub content (no 404s)
- Layout uses same design tokens as AdminShell.tsx
</success_criteria>

<output>
After completion, create `.planning/phases/02-settings-shell/02-01-SUMMARY.md`
</output>
