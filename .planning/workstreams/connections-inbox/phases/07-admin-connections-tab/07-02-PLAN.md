---
phase: 07-admin-connections-tab
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified: []
autonomous: false
requirements: [ADM-01, ADM-02]

must_haves:
  truths:
    - "Admin sees connections for a user who has connections"
    - "Admin can remove a connection and it disappears"
    - "Tab navigation works via URL params"
  artifacts: []
  key_links: []
---

<objective>
Verify the admin Connections tab works correctly in the browser with real data.

Purpose: Confirm that service role fetch, tab navigation, and remove action all function end-to-end (ADM-01, ADM-02).
Output: Human-verified confirmation or issue report.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/connections-inbox/phases/07-admin-connections-tab/07-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify admin Connections tab end-to-end</name>
  <files>app/admin/users/[id]/page.tsx</files>
  <read_first>
    - .planning/workstreams/connections-inbox/phases/07-admin-connections-tab/07-01-SUMMARY.md
  </read_first>
  <action>
    Present the human verification checklist to the user. Ensure the dev server is running (`npm run dev`). Guide the user through the 12-step browser verification below. Record their feedback.
  </action>
  <what-built>
    Admin user detail page now has Overview and Connections tabs. The Connections tab fetches all connections for the viewed user using service role (bypassing RLS) and displays them with type, status, other party name, and a Remove button with confirmation.
  </what-built>
  <how-to-verify>
    1. Start the dev server if not running: `npm run dev`
    2. Log in as an admin user
    3. Navigate to Admin > Users and click on a user who has connections
    4. Verify you see two tabs: "Overview" and "Connections"
    5. Click the "Overview" tab — confirm existing profile info and onboarding sections display correctly
    6. Click the "Connections" tab — confirm the URL changes to `?tab=connections`
    7. Verify connections are listed with the other party's name, avatar (or initial), status badge, and type label
    8. Click "Remove" on any connection — confirm a "Confirm" / "Cancel" prompt appears
    9. Click "Cancel" — confirm the prompt dismisses
    10. Click "Remove" again, then "Confirm" — confirm the connection row disappears
    11. Refresh the page — confirm the removed connection stays gone
    12. Test deep-link: navigate directly to `/admin/users/{id}?tab=connections` — confirm the Connections tab loads
  </how-to-verify>
  <verify>Human confirms all verification steps pass or reports issues</verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
  <acceptance_criteria>
    - Both tabs render and switch correctly
    - Connections tab shows real data (not empty for users with connections)
    - Remove flow works: button -> confirm -> connection disappears
    - Page refresh after remove confirms persistence
    - Direct URL with ?tab=connections loads the connections tab
  </acceptance_criteria>
  <done>Human approves all 12 verification steps. No visual regressions on Overview tab. Remove action persists across refresh.</done>
</task>

</tasks>

<verification>
Human browser verification — no automated checks needed beyond Plan 01's verification.
</verification>

<success_criteria>
- Human confirms all 12 verification steps pass
- No visual regressions on the Overview tab
- Remove action persists across page refresh
</success_criteria>

<output>
After completion, create `.planning/workstreams/connections-inbox/phases/07-admin-connections-tab/07-02-SUMMARY.md`
</output>
