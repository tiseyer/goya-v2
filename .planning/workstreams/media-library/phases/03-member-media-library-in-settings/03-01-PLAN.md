---
phase: "03"
plan: "01"
type: autonomous
wave: 1
depends_on: []
requirements: [MEMBER-01, MEMBER-02, MEMBER-03, MEMBER-04, MEMBER-05, MEMBER-06]
---

# Phase 03 Plan 01: Member Media Library in Settings

## Objective

Add a read-only Media page to user Settings for teachers, wellness practitioners, and admins. Members can browse their own uploaded files (filtered by uploaded_by = current user) with grid/list toggle, search, and a detail panel. No upload, no delete.

## Context

- @app/settings/components/SettingsShell.tsx — NAV_ITEMS array, role-gated items pattern
- @app/admin/media/MediaPageClient.tsx — reference for component patterns
- @app/admin/media/actions.ts — getMediaItems server action (add uploadedBy filter)
- @app/admin/media/MediaGrid.tsx — reuse directly (presentational)
- @app/admin/media/MediaList.tsx — reuse directly (presentational)
- @app/admin/media/MediaDetailPanel.tsx — adapt (no delete, no "Uploaded by")
- @app/admin/media/MediaToolbar.tsx — reuse directly (no "uploaded by" filter in member version)
- @app/admin/media/mediaUtils.ts — shared helpers
- @app/settings/my-events/page.tsx — server component pattern to follow

## Tasks

### Task 1: Add server action for member media items
**type:** auto

Create `app/settings/media/actions.ts` with a `getMemberMediaItems` server action that:
- Uses `createSupabaseServerClient()` (user-scoped, respects RLS)
- Filters to `uploaded_by = currentUserId` (passed as argument)
- Accepts same params as admin `getMediaItems` minus the `by` filter
- Returns `{ items: MediaItem[], nextCursor: string | null }`

**Imports:** Reuse `MediaItem` type from `@/app/admin/media/actions` (re-export approach — import MediaItem type, don't redefine it).

**Done:** File exists, exports `getMemberMediaItems`, TypeScript compiles.

---

### Task 2: Create MemberFolderSidebar component
**type:** auto

Create `app/settings/media/MemberFolderSidebar.tsx`:
- Simplified version of admin FolderSidebar
- Static folders: "All Files" (null), "Avatars" (bucket key), "Certificates" (bucket key), "Uploads" (bucket key)
- No folder management controls (no create/rename/delete)
- Has collapse/expand toggle (same pattern as FolderSidebar)
- No custom media_folders — just the static bucket list

**Done:** File exists, TypeScript compiles.

---

### Task 3: Create MemberMediaDetailPanel component
**type:** auto

Create `app/settings/media/MemberMediaDetailPanel.tsx`:
- Adapted from `MediaDetailPanel`
- Read-only: no editable fields (no title/alt/caption edit + save button)
- No delete section
- No "Uploaded by" / "By" metadata row
- Shows: preview, file name, type, size, dimensions, upload date, URL with copy button
- Props: `{ item: MediaItem; onClose: () => void }`

**Done:** File exists, TypeScript compiles.

---

### Task 4: Create MemberMediaClient component
**type:** auto

Create `app/settings/media/MemberMediaClient.tsx`:
- Client component, adapted from `MediaPageClient`
- No upload logic (no MediaUploader, no UploadCards, no UploadButton)
- No `by` filter in toolbar (member only sees their own files)
- Calls `getMemberMediaItems` (from Task 1) for data loading, passing `currentUserId`
- Uses `MemberFolderSidebar` (Task 2) and `MemberMediaDetailPanel` (Task 3)
- Reuses `MediaGrid`, `MediaList`, `MediaToolbar` from admin (direct imports)
- Toolbar: remove `by` prop and `onByChange` — pass `by="members"` locked (or simplify toolbar by omitting the "by" select)
- URL param updates go to `/settings/media`
- Empty state message: "No media files yet. Files you upload (like certificates and profile photos) will appear here."

Props: `{ currentUserId: string; folder?: string; view?: string; q?: string; type?: string; date?: string; sort?: string }`

**Done:** File exists, TypeScript compiles.

---

### Task 5: Create settings/media page.tsx
**type:** auto

Create `app/settings/media/page.tsx`:
- Server component following `my-events/page.tsx` pattern
- Uses `createSupabaseServerClient()` to get current user
- Redirects to `/sign-in` if not authenticated
- Checks role, redirects to `/settings` if not in `['teacher', 'wellness_practitioner', 'admin']`
- Reads `searchParams` (folder, view, q, type, date, sort) and passes to `MemberMediaClient`
- Exports `metadata` with title "Media — GOYA Settings"
- Accepts `searchParams` prop (async, Next.js 15 pattern)

**Done:** File exists, TypeScript compiles, page is accessible at `/settings/media`.

---

### Task 6: Add Media nav item to SettingsShell
**type:** auto

Edit `app/settings/components/SettingsShell.tsx`:
- Add "Media" nav item after "My Events" (before "My Courses")
- href: `/settings/media`
- roles: `['teacher', 'wellness_practitioner', 'admin']`
- Icon: photo/image icon (matching media concept) — use `M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z`

**Done:** Nav item visible in sidebar for correct roles, TypeScript compiles.

---

### Task 7: Update teacher docs
**type:** auto

Update `docs/teacher/media-library.md`:
- Document the Member Media page in Settings
- Explain what it shows, role requirement, read-only nature
- Update `last_updated` date to 2026-03-31

**Done:** File updated.

---

### Task 8: Run TypeScript check
**type:** auto

Run `npx tsc --noEmit` from project root. Fix any type errors found.

**Done:** Zero TypeScript errors.

## Verification

- `npx tsc --noEmit` passes with zero errors
- `app/settings/media/page.tsx` exists
- `app/settings/media/MemberMediaClient.tsx` exists
- `app/settings/media/MemberFolderSidebar.tsx` exists
- `app/settings/media/MemberMediaDetailPanel.tsx` exists
- `app/settings/media/actions.ts` exists
- SettingsShell NAV_ITEMS contains Media entry with correct roles
- All MEMBER-01 through MEMBER-06 requirements satisfied

## Success Criteria

- Members with teacher/wellness_practitioner/admin role see "Media" in Settings sidebar
- Navigating to /settings/media shows their own uploaded files only
- No upload button, no delete button visible
- Grid/list toggle and search work
- Detail panel shows file info without "Uploaded by" and without delete
- Empty state shows correct message
- TypeScript compiles cleanly
