---
phase: 07-onboarding-migration
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - middleware.ts
autonomous: true
requirements: [MIGRATE-03]

must_haves:
  truths:
    - "New users who register see onboarding via flow player modal, not the old /onboarding route"
    - "The middleware no longer redirects unonboarded users to /onboarding"
    - "The entire app/onboarding/ directory is deleted"
    - "No broken imports or references to deleted onboarding files remain"
    - "Visiting /onboarding returns a 404 (route no longer exists)"
    - "Admin onboarding preview mode references are cleaned up"
  artifacts:
    - path: "middleware.ts"
      provides: "Updated middleware without onboarding redirect logic"
  key_links:
    - from: "middleware.ts"
      to: "flow player (app/components/providers/ConnectionsProvider)"
      via: "Flow player handles onboarding display via login trigger"
      pattern: "No more /onboarding redirect — flow engine serves onboarding flows"
---

<objective>
Remove the hardcoded onboarding system entirely: delete middleware redirects, delete app/onboarding/ directory, and clean up all references.

Purpose: Complete the migration from hardcoded onboarding to flow-builder-driven onboarding. After Plan 07-01 seeds the templates, this plan removes the old system so the flow player is the sole onboarding path.

Output: Clean codebase with no hardcoded onboarding remnants
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/flow-builder/ROADMAP.md
@.planning/workstreams/flow-builder/STATE.md
@.planning/workstreams/flow-builder/phases/07-onboarding-migration/07-01-SUMMARY.md
@middleware.ts

<interfaces>
<!-- Current middleware onboarding logic to remove (lines 16-19, 90-91, 206-232) -->
From middleware.ts:
```typescript
// These arrays/logic to REMOVE or MODIFY:
const ONBOARDING_GATED_PATHS = ['/dashboard', '/profile', '/connections']
const isOnboardingPath = pathname.startsWith('/onboarding')
// Lines 206-232: The entire onboarding redirect block

// KEEP: PROTECTED_PATHS still needs '/dashboard', '/admin', '/profile', '/connections', '/members'
// REMOVE: '/onboarding' from PROTECTED_PATHS
// REMOVE: ONBOARDING_GATED_PATHS entirely
// REMOVE: isOnboardingPath variable and all its uses
// REMOVE: onboarding_preview_mode cookie check
// REMOVE: onboarding_completed profile query and redirect logic
```

Files to delete (entire directory):
```
app/onboarding/          # Everything — layout, page, components/, steps/, hooks/, lib/
```

Files that reference onboarding to check/clean:
```
app/admin/dashboard/AdminOnboardingTest.tsx  — references onboarding preview
app/admin/users/[id]/ResetOnboardingButton.tsx — keep (resets profile field)
app/admin/users/[id]/page.tsx — may import onboarding components
app/auth/callback/route.ts — may redirect to /onboarding
app/auth/actions.ts — may reference onboarding
app/api/email/onboarding-complete/route.ts — keep (email still useful)
app/layout.tsx — may reference onboarding
app/components/CookieConsent.tsx — may reference onboarding
app/community/page.tsx — may reference onboarding
app/page.tsx — may reference onboarding
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove middleware onboarding redirects</name>
  <read_first>
    - middleware.ts
  </read_first>
  <files>middleware.ts</files>
  <action>
Update middleware.ts to remove all hardcoded onboarding redirect logic. The flow player now handles onboarding display via the login trigger — no route-level redirection needed.

Specific changes:

1. **Remove '/onboarding' from PROTECTED_PATHS** (line 13). The /onboarding route will be deleted, so no need to protect it.

2. **Remove ONBOARDING_GATED_PATHS entirely** (lines 18-20). The flow player displays onboarding as a non-dismissible modal overlay on any page — no need to gate specific paths.

3. **Remove `isOnboardingPath` variable** (line 90) and all references to it.

4. **Update the fast-path condition** (line 97): Remove `!isOnboardingPath` from the condition since the variable no longer exists.

5. **Remove the entire onboarding redirect block** (lines 206-232). This block:
   - Fetches `onboarding_completed` from profile
   - Redirects gated paths to /onboarding if not completed
   - Redirects /onboarding to /dashboard if already completed
   - Handles admin preview mode cookie
   ALL of this is no longer needed.

6. **Simplify the profile query** (line ~196-198): The profile query currently selects `onboarding_completed, role, requires_password_reset`. Remove `onboarding_completed` from the select since it's no longer used in middleware. Keep `role` and `requires_password_reset`.

7. **Remove the `onboarding_preview_mode` cookie check** (line 211) and associated logic.

8. **Keep unchanged:**
   - Password reset interception (lines 200-203)
   - Admin path role check (lines 235-242)
   - Maintenance mode logic
   - Impersonation cookie security
   - The matcher config

The resulting middleware should be shorter and simpler — authenticated users just get their session refreshed and admin role checked. The flow engine handles showing onboarding flows on login.
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx tsc --noEmit middleware.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - No reference to 'onboarding' in middleware.ts (except possibly in comments)
    - ONBOARDING_GATED_PATHS removed
    - isOnboardingPath removed
    - Profile query no longer selects onboarding_completed
    - onboarding_preview_mode cookie logic removed
    - Password reset interception still works
    - Admin path check still works
    - TypeScript compiles clean
  </acceptance_criteria>
  <done>Middleware no longer has any onboarding redirect logic; flow player is the sole onboarding mechanism</done>
</task>

<task type="auto">
  <name>Task 2: Delete app/onboarding/ and clean up references</name>
  <read_first>
    - app/auth/callback/route.ts
    - app/auth/actions.ts
    - app/admin/dashboard/AdminOnboardingTest.tsx
    - app/admin/users/[id]/page.tsx
    - app/layout.tsx
    - app/page.tsx
  </read_first>
  <files>app/onboarding/ (DELETE entire directory), app/auth/callback/route.ts, app/auth/actions.ts, app/admin/dashboard/AdminOnboardingTest.tsx</files>
  <action>
1. **Delete the entire `app/onboarding/` directory:**
   ```bash
   rm -rf app/onboarding
   ```
   This removes: layout.tsx, page.tsx, components/ (OnboardingProvider, OnboardingShell, OnboardingStep, BlockedScreen, CompletionStep, all inputs/), steps/ (all 40+ step files), hooks/ (useOnboardingProgress), lib/ (steps.ts, submitOnboarding.ts, submitOnboardingAction.ts)

2. **Check and update `app/auth/callback/route.ts`:**
   - If it redirects to '/onboarding' anywhere, change to '/dashboard' (the flow player will show the onboarding modal on dashboard load)

3. **Check and update `app/auth/actions.ts`:**
   - If it redirects to '/onboarding', change to '/dashboard'

4. **Check `app/admin/dashboard/AdminOnboardingTest.tsx`:**
   - This likely has a button to set the onboarding_preview_mode cookie and redirect to /onboarding
   - Either delete this file entirely or update it to note that onboarding is now managed via the flow builder
   - If deleting, also remove its import from the admin dashboard page

5. **Check `app/admin/users/[id]/page.tsx`:**
   - If it imports from app/onboarding/, remove those imports
   - The ResetOnboardingButton.tsx should stay (it resets the onboarding_completed profile field, which is still used as a flow condition)

6. **Check `app/layout.tsx`:**
   - Remove any onboarding-specific imports or references

7. **Check `app/page.tsx`, `app/community/page.tsx`, `app/components/CookieConsent.tsx`:**
   - Remove any /onboarding path references if they exist

8. **Keep `app/api/email/onboarding-complete/route.ts`:**
   - This email endpoint is still useful — it can be triggered by a send_email action in the flow templates in the future

9. **Verify no broken imports remain:**
   ```bash
   grep -r "from.*onboarding" app/ lib/ --include="*.ts" --include="*.tsx" | grep -v node_modules | grep -v ".planning"
   ```
   Fix any remaining references.

10. **Run full TypeScript check** to catch any broken imports.
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && ! test -d app/onboarding && echo "PASS: onboarding dir deleted" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - app/onboarding/ directory does not exist
    - No TypeScript compilation errors related to missing onboarding imports
    - Auth callback redirects to /dashboard (not /onboarding)
    - Admin onboarding test button removed or updated
    - No remaining imports from app/onboarding/ in any file
    - ResetOnboardingButton.tsx preserved (it updates profile, not onboarding routes)
    - onboarding-complete email route preserved
  </acceptance_criteria>
  <done>All hardcoded onboarding files removed, no broken references, clean TypeScript compilation</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` compiles clean — no broken imports
2. `grep -r "app/onboarding" app/ lib/ middleware.ts --include="*.ts" --include="*.tsx"` returns no results (except .planning/)
3. `ls app/onboarding/ 2>&1` shows "No such file or directory"
4. middleware.ts has no 'ONBOARDING_GATED_PATHS' or 'isOnboardingPath'
5. Dev server starts without errors: `npx next build 2>&1 | tail -10`
</verification>

<success_criteria>
- Hardcoded onboarding routes completely removed from codebase
- Middleware no longer redirects to /onboarding
- No broken imports or TypeScript errors
- Flow player is the only onboarding mechanism
- Auth callback sends new users to /dashboard where flow player shows onboarding modal
</success_criteria>

<output>
After completion, create `.planning/workstreams/flow-builder/phases/07-onboarding-migration/07-02-SUMMARY.md`
</output>
