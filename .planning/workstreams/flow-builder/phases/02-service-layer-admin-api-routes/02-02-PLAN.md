---
phase: 02-service-layer-admin-api-routes
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/api/admin/flows/route.ts
  - app/api/admin/flows/[id]/route.ts
  - app/api/admin/flows/[id]/steps/route.ts
  - app/api/admin/flows/[id]/steps/[stepId]/route.ts
  - app/api/admin/flows/[id]/steps/reorder/route.ts
  - app/api/admin/flows/[id]/steps/[stepId]/branches/route.ts
  - app/api/admin/flows/[id]/duplicate/route.ts
  - lib/flows/admin-auth.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Admin can create a flow via POST /api/admin/flows and receive the created flow back"
    - "Admin can list flows via GET /api/admin/flows with optional status filter"
    - "Admin can get a flow with all its steps and branches via GET /api/admin/flows/[id]"
    - "Admin can update a flow via PATCH /api/admin/flows/[id]"
    - "Admin can delete a flow via DELETE /api/admin/flows/[id]"
    - "Admin can CRUD steps within a flow via /api/admin/flows/[id]/steps routes"
    - "Saving branches that create a cycle returns HTTP 422 with cycle-detected error"
    - "Non-admin/moderator users receive 403 Forbidden on all routes"
    - "Unauthenticated requests receive 401 Unauthorized on all routes"
  artifacts:
    - path: "lib/flows/admin-auth.ts"
      provides: "Reusable admin auth guard for flow API routes"
      exports: ["requireFlowAdmin"]
    - path: "app/api/admin/flows/route.ts"
      provides: "GET (list) and POST (create) for flows"
      contains: "export async function GET"
    - path: "app/api/admin/flows/[id]/route.ts"
      provides: "GET (detail with steps), PATCH (update), DELETE for a single flow"
      contains: "export async function PATCH"
    - path: "app/api/admin/flows/[id]/steps/route.ts"
      provides: "GET (list steps) and POST (create step) for a flow"
      contains: "export async function POST"
    - path: "app/api/admin/flows/[id]/steps/[stepId]/route.ts"
      provides: "GET (detail), PATCH (update), DELETE for a single step"
      contains: "export async function DELETE"
    - path: "app/api/admin/flows/[id]/steps/reorder/route.ts"
      provides: "PUT to reorder steps within a flow"
      contains: "export async function PUT"
    - path: "app/api/admin/flows/[id]/steps/[stepId]/branches/route.ts"
      provides: "PUT to replace all branches for a step, with cycle detection"
      contains: "detectCycle"
    - path: "app/api/admin/flows/[id]/duplicate/route.ts"
      provides: "POST to duplicate a flow with all steps and branches"
      contains: "duplicateFlow"
  key_links:
    - from: "app/api/admin/flows/route.ts"
      to: "lib/flows/flow-service.ts"
      via: "import { listFlows, createFlow }"
      pattern: "import.*from.*flow-service"
    - from: "app/api/admin/flows/[id]/steps/[stepId]/branches/route.ts"
      to: "lib/flows/cycle-detection.ts"
      via: "import { detectCycle }"
      pattern: "detectCycle"
    - from: "all route files"
      to: "lib/flows/admin-auth.ts"
      via: "import { requireFlowAdmin }"
      pattern: "requireFlowAdmin"
---

<objective>
Wire the flow builder services to HTTP endpoints under /api/admin/flows/ with admin auth guard and cycle detection on branch save.

Purpose: Phase 3 admin UI needs stable API contracts to build against. These routes are the interface between the UI and the service layer created in Plan 01.
Output: 8 files — 7 route handlers + 1 shared auth guard
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/flow-builder/ROADMAP.md
@.planning/workstreams/flow-builder/STATE.md
@.planning/workstreams/flow-builder/phases/02-service-layer-admin-api-routes/02-01-SUMMARY.md

<interfaces>
<!-- Auth pattern from app/api/admin/users/bulk-delete/route.ts -->
```typescript
import 'server-only'
import { NextResponse } from 'next/server'
import { createSupabaseServerClient } from '@/lib/supabaseServer'

// Auth check pattern:
const supabase = await createSupabaseServerClient()
const { data: { user }, error: authError } = await supabase.auth.getUser()
if (authError || !user) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
const { data: profile } = await supabase.from('profiles').select('role').eq('id', user.id).single()
if (!profile || !['admin', 'moderator'].includes(profile.role)) {
  return NextResponse.json({ error: 'Forbidden — admin role required' }, { status: 403 })
}
```

<!-- Services from Plan 01 (lib/flows/) -->
flow-service.ts exports:
  listFlows(params: { status?, is_template? }) -> { data, error }
  getFlowById(id) -> { data, error }
  getFlowWithSteps(id) -> { data: FlowWithSteps | null, error }
  createFlow(input, createdBy) -> { data, error }
  updateFlow(id, input) -> { data, error }
  deleteFlow(id) -> { error }
  duplicateFlow(id, createdBy) -> { data, error }

step-service.ts exports:
  listSteps(flowId) -> { data, error }
  getStepById(id) -> { data, error }
  createStep(input) -> { data, error }
  updateStep(id, input) -> { data, error }
  deleteStep(id) -> { error }
  reorderSteps(flowId, stepIds) -> { error }
  upsertBranches(stepId, branches) -> { data, error }

cycle-detection.ts exports:
  detectCycle(steps, branches) -> { hasCycle, cyclePath? }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create admin auth guard and flow list/create routes</name>
  <files>lib/flows/admin-auth.ts, app/api/admin/flows/route.ts, app/api/admin/flows/[id]/route.ts, app/api/admin/flows/[id]/duplicate/route.ts</files>
  <read_first>
    - app/api/admin/users/bulk-delete/route.ts (auth pattern to replicate exactly)
    - lib/supabaseServer.ts (createSupabaseServerClient)
    - lib/flows/flow-service.ts (created in Plan 01 — imports to use)
    - lib/flows/types.ts (created in Plan 01 — types to reference)
  </read_first>
  <action>
    **lib/flows/admin-auth.ts** — Reusable auth guard:

    1. `import 'server-only'`
    2. Import `createSupabaseServerClient` from `@/lib/supabaseServer`.
    3. Import `NextResponse` from `next/server`.
    4. Export async function `requireFlowAdmin()` that returns `{ user, error: NextResponse | null }`:
       - Call `createSupabaseServerClient()`, then `supabase.auth.getUser()`.
       - If no user: return `{ user: null, error: NextResponse.json({ error: 'Unauthorized' }, { status: 401 }) }`.
       - Query `profiles.role` for user.id.
       - If role not in `['admin', 'moderator']`: return `{ user: null, error: NextResponse.json({ error: 'Forbidden — admin or moderator role required' }, { status: 403 }) }`.
       - Otherwise return `{ user, error: null }`.
    5. This eliminates the 8-line auth block from every route handler.

    **app/api/admin/flows/route.ts** — List and create flows:

    1. `import 'server-only'`
    2. GET handler: call `requireFlowAdmin()`, if error return it. Parse `?status=` and `?is_template=` from URL searchParams. Call `listFlows({ status, is_template })`. Return `NextResponse.json(data)` or error 500.
    3. POST handler: call `requireFlowAdmin()`. Parse body JSON. Call `createFlow(body, user.id)`. Return `NextResponse.json(data, { status: 201 })` or error 500.

    **app/api/admin/flows/[id]/route.ts** — Get, update, delete single flow:

    1. GET handler: auth guard. Extract `id` from `params` (Next.js 16 async params: `const { id } = await params`). Call `getFlowWithSteps(id)`. If data is null, return 404. Return the full FlowWithSteps object.
    2. PATCH handler: auth guard. Extract id from params. Parse body. Call `updateFlow(id, body)`. Return updated flow or error.
    3. DELETE handler: auth guard. Extract id from params. Call `deleteFlow(id)`. Return `{ success: true }` or error.

    **app/api/admin/flows/[id]/duplicate/route.ts** — Duplicate a flow:

    1. POST handler: auth guard. Extract id from params. Call `duplicateFlow(id, user.id)`. Return the new flow with status 201 or error.

    All route handlers follow existing patterns:
    - `import 'server-only'` at top
    - `NextResponse.json({ error: message }, { status: code })` for errors
    - Try/catch around service calls with 500 fallback
    - Next.js 16 async params: `const { id } = await params` (NOT destructure from function signature)
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx tsc --noEmit lib/flows/admin-auth.ts app/api/admin/flows/route.ts app/api/admin/flows/\[id\]/route.ts app/api/admin/flows/\[id\]/duplicate/route.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export async function requireFlowAdmin" lib/flows/admin-auth.ts
    - grep -q "createSupabaseServerClient" lib/flows/admin-auth.ts
    - grep -q "admin.*moderator" lib/flows/admin-auth.ts
    - grep -q "export async function GET" app/api/admin/flows/route.ts
    - grep -q "export async function POST" app/api/admin/flows/route.ts
    - grep -q "requireFlowAdmin" app/api/admin/flows/route.ts
    - grep -q "listFlows" app/api/admin/flows/route.ts
    - grep -q "createFlow" app/api/admin/flows/route.ts
    - grep -q "export async function GET" "app/api/admin/flows/[id]/route.ts"
    - grep -q "export async function PATCH" "app/api/admin/flows/[id]/route.ts"
    - grep -q "export async function DELETE" "app/api/admin/flows/[id]/route.ts"
    - grep -q "getFlowWithSteps" "app/api/admin/flows/[id]/route.ts"
    - grep -q "duplicateFlow" "app/api/admin/flows/[id]/duplicate/route.ts"
    - grep -rq "status: 401" lib/flows/admin-auth.ts
    - grep -rq "status: 403" lib/flows/admin-auth.ts
  </acceptance_criteria>
  <done>Auth guard returns 401/403 for unauthenticated/non-admin requests. Flow CRUD routes at /api/admin/flows/ handle GET (list), POST (create), GET/:id (detail with steps), PATCH/:id (update), DELETE/:id (delete), POST/:id/duplicate. All use requireFlowAdmin() and service layer functions.</done>
</task>

<task type="auto">
  <name>Task 2: Create step CRUD routes with cycle detection on branch save</name>
  <files>app/api/admin/flows/[id]/steps/route.ts, app/api/admin/flows/[id]/steps/[stepId]/route.ts, app/api/admin/flows/[id]/steps/reorder/route.ts, app/api/admin/flows/[id]/steps/[stepId]/branches/route.ts</files>
  <read_first>
    - lib/flows/admin-auth.ts (just created in Task 1)
    - lib/flows/step-service.ts (created in Plan 01)
    - lib/flows/cycle-detection.ts (created in Plan 01)
    - lib/flows/flow-service.ts (getFlowWithSteps for cycle detection context)
    - app/api/admin/flows/[id]/route.ts (just created in Task 1 — pattern reference)
  </read_first>
  <action>
    **app/api/admin/flows/[id]/steps/route.ts** — List and create steps:

    1. GET handler: auth guard. Extract `id` from async params (this is the flow_id). Call `listSteps(id)`. Return data or error.
    2. POST handler: auth guard. Extract `id` from async params. Parse body. Call `createStep({ ...body, flow_id: id })`. Return created step with status 201 or error.

    **app/api/admin/flows/[id]/steps/[stepId]/route.ts** — Get, update, delete single step:

    1. Extract both `id` and `stepId` from async params: `const { id, stepId } = await params`.
    2. GET: auth guard. Call `getStepById(stepId)`. Return step with branches or 404.
    3. PATCH: auth guard. Parse body. Call `updateStep(stepId, body)`. Return updated step or error.
    4. DELETE: auth guard. Call `deleteStep(stepId)`. Return `{ success: true }` or error.

    **app/api/admin/flows/[id]/steps/reorder/route.ts** — Reorder steps:

    1. PUT handler: auth guard. Extract `id` from async params. Parse body expecting `{ stepIds: string[] }`. Validate stepIds is a non-empty array. Call `reorderSteps(id, body.stepIds)`. Return `{ success: true }` or error.

    **app/api/admin/flows/[id]/steps/[stepId]/branches/route.ts** — Replace branches with cycle detection:

    1. PUT handler: auth guard. Extract `id` (flow_id) and `stepId` from async params. Parse body expecting `{ branches: UpsertBranchInput[] }`.
    2. **CRITICAL — Cycle detection before save:**
       - Call `getFlowWithSteps(id)` to get the full flow graph.
       - Build a merged branch list: take all existing branches from all steps, remove branches belonging to `stepId`, add the new branches from request body.
       - Call `detectCycle(flow.steps, mergedBranches)`.
       - If `hasCycle` is true, return `NextResponse.json({ error: 'Cycle detected in branch graph', cyclePath: result.cyclePath }, { status: 422 })`.
    3. If no cycle, call `upsertBranches(stepId, body.branches)`. Return the saved branches or error.
    4. This is the key validation from Success Criteria #2 — HTTP 422 on cycle detection.
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx tsc --noEmit app/api/admin/flows/\[id\]/steps/route.ts app/api/admin/flows/\[id\]/steps/\[stepId\]/route.ts app/api/admin/flows/\[id\]/steps/reorder/route.ts app/api/admin/flows/\[id\]/steps/\[stepId\]/branches/route.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export async function GET" "app/api/admin/flows/[id]/steps/route.ts"
    - grep -q "export async function POST" "app/api/admin/flows/[id]/steps/route.ts"
    - grep -q "listSteps" "app/api/admin/flows/[id]/steps/route.ts"
    - grep -q "createStep" "app/api/admin/flows/[id]/steps/route.ts"
    - grep -q "export async function GET" "app/api/admin/flows/[id]/steps/[stepId]/route.ts"
    - grep -q "export async function PATCH" "app/api/admin/flows/[id]/steps/[stepId]/route.ts"
    - grep -q "export async function DELETE" "app/api/admin/flows/[id]/steps/[stepId]/route.ts"
    - grep -q "export async function PUT" "app/api/admin/flows/[id]/steps/reorder/route.ts"
    - grep -q "reorderSteps" "app/api/admin/flows/[id]/steps/reorder/route.ts"
    - grep -q "export async function PUT" "app/api/admin/flows/[id]/steps/[stepId]/branches/route.ts"
    - grep -q "detectCycle" "app/api/admin/flows/[id]/steps/[stepId]/branches/route.ts"
    - grep -q "422" "app/api/admin/flows/[id]/steps/[stepId]/branches/route.ts"
    - grep -q "upsertBranches" "app/api/admin/flows/[id]/steps/[stepId]/branches/route.ts"
    - grep -q "requireFlowAdmin" "app/api/admin/flows/[id]/steps/route.ts"
    - grep -q "requireFlowAdmin" "app/api/admin/flows/[id]/steps/[stepId]/branches/route.ts"
  </acceptance_criteria>
  <done>Step CRUD routes handle list, create, get, update, delete, reorder. Branch save route runs DFS cycle detection before persisting and returns HTTP 422 with cyclePath if cycles found. All routes use requireFlowAdmin() guard. All files pass TypeScript compilation.</done>
</task>

</tasks>

<verification>
1. All route files compile: `npx tsc --noEmit` on all 8 new files
2. Auth guard pattern: every route file imports and calls requireFlowAdmin()
3. Cycle detection: branches route returns 422 when cycle detected
4. HTTP methods match REST conventions: GET for reads, POST for creates, PATCH for updates, DELETE for deletes, PUT for replace-all operations
5. No route exposes conditions JSONB to non-admin consumers (these are admin-only routes, safe)
</verification>

<success_criteria>
- 7 route handler files exist under app/api/admin/flows/
- lib/flows/admin-auth.ts exports requireFlowAdmin() returning 401 or 403 for unauthorized access
- GET /api/admin/flows returns flow list, POST creates a flow
- GET /api/admin/flows/[id] returns flow with all steps and branches
- PATCH /api/admin/flows/[id] updates flow fields
- DELETE /api/admin/flows/[id] deletes flow (CASCADE handles children)
- POST /api/admin/flows/[id]/duplicate creates a copy with all steps/branches
- Step CRUD routes handle list, create, get, update, delete under /api/admin/flows/[id]/steps/
- PUT /api/admin/flows/[id]/steps/reorder accepts stepIds array
- PUT /api/admin/flows/[id]/steps/[stepId]/branches runs cycle detection and returns 422 on cycle
- All routes pass TypeScript compilation
</success_criteria>

<output>
After completion, create `.planning/workstreams/flow-builder/phases/02-service-layer-admin-api-routes/02-02-SUMMARY.md`
</output>
