---
phase: 03-admin-flow-builder-ui
plan: "04"
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - app/admin/flows/components/editor/StepActionsEditor.tsx
  - app/admin/flows/components/editor/FlowSettingsPanel.tsx
  - app/admin/flows/components/editor/ConditionsBuilder.tsx
  - app/admin/flows/components/editor/FlowPreviewModal.tsx
  - app/admin/flows/components/editor/FlowEditorShell.tsx
  - app/admin/flows/components/editor/ElementPropertiesPanel.tsx
  - lib/flows/editor-store.ts
autonomous: false
requirements: [ADMIN-12, ADMIN-13, ADMIN-14, ADMIN-15]

must_haves:
  truths:
    - "Admin can configure step-level actions from a list of 8 action types"
    - "Admin can configure flow settings (display type, modal options, trigger, frequency) in a collapsible panel"
    - "Admin can build conditions using chip-based AND logic with 6 condition types"
    - "Admin can preview the flow as a user would see it with a PREVIEW MODE watermark"
  artifacts:
    - path: "app/admin/flows/components/editor/StepActionsEditor.tsx"
      provides: "Step actions configuration UI"
      exports: ["default"]
    - path: "app/admin/flows/components/editor/FlowSettingsPanel.tsx"
      provides: "Flow-level settings collapsible panel"
      exports: ["default"]
    - path: "app/admin/flows/components/editor/ConditionsBuilder.tsx"
      provides: "Chip-based condition builder with AND logic"
      exports: ["default"]
    - path: "app/admin/flows/components/editor/FlowPreviewModal.tsx"
      provides: "In-editor preview overlay"
      exports: ["default"]
  key_links:
    - from: "FlowSettingsPanel.tsx"
      to: "/api/admin/flows/[id]"
      via: "PATCH flow settings"
      pattern: "api/admin/flows"
    - from: "ConditionsBuilder.tsx"
      to: "lib/flows/editor-store.ts"
      via: "useEditorStore to update flow.conditions"
      pattern: "useEditorStore"
    - from: "FlowPreviewModal.tsx"
      to: "lib/flows/editor-store.ts"
      via: "reads flow and steps from store for preview rendering"
      pattern: "useEditorStore"
---

<objective>
Add step actions, flow settings with conditions builder, and preview mode to complete the flow editor.

Purpose: Admins need to configure what happens after each step (actions), when a flow appears (conditions/triggers), and verify the flow looks correct (preview). This completes all admin flow builder UI requirements.
Output: Fully functional flow editor with all ADMIN requirements satisfied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/flow-builder/ROADMAP.md
@.planning/workstreams/flow-builder/phases/03-admin-flow-builder-ui/03-CONTEXT.md
@.planning/workstreams/flow-builder/phases/03-admin-flow-builder-ui/03-03-SUMMARY.md

<interfaces>
<!-- From lib/flows/types.ts -->

```typescript
export type FlowDisplayType = 'modal' | 'fullscreen' | 'top_banner' | 'bottom_banner' | 'notification';
export type FlowTriggerType = 'login' | 'manual' | 'page_load';
export type FlowFrequency = 'once' | 'every_login' | 'every_session' | 'custom';

export type FlowConditionType =
  | 'role' | 'onboarding_status' | 'has_profile_picture'
  | 'subscription_status' | 'birthday' | 'flow_completed';

export interface FlowCondition {
  type: FlowConditionType;
  operator: string;
  value: string | string[] | boolean | number;
}

export interface Flow {
  // ... all fields including:
  display_type: FlowDisplayType;
  modal_dismissible: boolean | null;
  modal_backdrop: 'blur' | 'dark' | 'none' | null;
  trigger_type: FlowTriggerType;
  trigger_delay_seconds: number | null;
  frequency: FlowFrequency;
  conditions: FlowCondition[];
}
```

Step actions (from REQUIREMENTS.md ADMIN-12):
```
save_to_profile, send_email, kit_tag, stripe_checkout, redirect, trigger_flow, success_popup, mark_complete
```

Condition builder values (from CONTEXT.md):
```
role: ['student', 'teacher', 'wellness_practitioner', 'admin']
onboarding_status: ['complete', 'incomplete'] (operator: 'is')
has_profile_picture: [true, false] (operator: 'is')
subscription_status: ['active', 'trialing', 'canceled', 'none']
birthday: date value (operator: 'is_set' / 'is_not_set' / 'is_today')
flow_completed: flow_id (operator: 'has_completed' / 'has_not_completed')
```

API:
```
PATCH /api/admin/flows/[id] — Update flow settings (display_type, trigger_type, frequency, conditions, etc.)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Step actions editor and flow settings panel with conditions builder</name>
  <files>
    app/admin/flows/components/editor/StepActionsEditor.tsx,
    app/admin/flows/components/editor/FlowSettingsPanel.tsx,
    app/admin/flows/components/editor/ConditionsBuilder.tsx,
    app/admin/flows/components/editor/FlowEditorShell.tsx,
    app/admin/flows/components/editor/ElementPropertiesPanel.tsx,
    lib/flows/editor-store.ts
  </files>
  <read_first>
    lib/flows/editor-store.ts,
    lib/flows/types.ts,
    app/admin/flows/components/editor/FlowEditorShell.tsx,
    app/admin/flows/components/editor/ElementPropertiesPanel.tsx
  </read_first>
  <action>
1. Create StepActionsEditor.tsx ('use client'):
   - Shown in the right panel below ElementPropertiesPanel (or as a separate section when no element is selected, accessible via a "Step Actions" header)
   - Note: step-level actions are not part of the current database schema (flow_steps has no actions column). For now, store actions as metadata in a new `actions` JSONB field concept. Since we cannot modify the schema here, store step actions in the editor store as a temporary map: `stepActions: Record<string, StepAction[]>`.
   - Define StepAction type locally:
     ```typescript
     type StepActionType = 'save_to_profile' | 'send_email' | 'kit_tag' | 'stripe_checkout' | 'redirect' | 'trigger_flow' | 'success_popup' | 'mark_complete';
     interface StepAction { type: StepActionType; config: Record<string, unknown> }
     ```
   - UI: "Step Actions" section with "Add Action" dropdown button
   - For each configured action, show a card with:
     - Action type badge (colored)
     - Type-specific config fields:
       - save_to_profile: auto-populated from profileMappings in store (read-only summary showing which elements map to which fields)
       - send_email: subject (text), body template (textarea)
       - kit_tag: tag name (text input)
       - stripe_checkout: price_id (text input), success_url (text input)
       - redirect: url (text input)
       - trigger_flow: flow selector (text input for flow_id — could be a dropdown in future)
       - success_popup: title (text), message (textarea)
       - mark_complete: no config needed (just a toggle)
     - Delete action button (X)
   - Changes update store.stepActions[stepId] — these will be persisted when the step actions column is added (or stored as part of step metadata in a future schema migration). For now, this is a UI scaffold.

2. Create FlowSettingsPanel.tsx ('use client'):
   - Collapsible panel at top of editor (per CONTEXT.md decision: "collapsible panel at top of editor — saves vertical space")
   - Toggle: clickable header "Flow Settings" with chevron icon that collapses/expands
   - Add state to editor store: `settingsPanelOpen: boolean; toggleSettingsPanel: () => void`
   - Settings fields (all read from store.flow, update via store.updateFlow):
     - Display Type: radio group or select with all 5 FlowDisplayType values, each with icon
     - When display_type === 'modal':
       - Dismissible toggle (modal_dismissible)
       - Backdrop style select: blur, dark, none (modal_backdrop)
     - Trigger Type: select with FlowTriggerType values (login, manual, page_load)
     - When trigger_type === 'page_load':
       - Delay seconds: number input (trigger_delay_seconds)
     - Frequency: select with FlowFrequency values
     - Status: select with FlowStatus values (draft, active, paused, archived)
   - Conditions section: render ConditionsBuilder component below the other settings
   - "Save Settings" button: PATCH /api/admin/flows/[flowId] with all changed fields from store.flow
   - OR: auto-save on field change with debounce (consistent with element auto-save)

3. Create ConditionsBuilder.tsx ('use client'):
   - Props: { conditions: FlowCondition[]; onChange: (conditions: FlowCondition[]) => void }
   - UI: chip-based builder (per CONTEXT.md: "chip-based builder with AND logic")
   - Current conditions shown as chips: each chip shows "[type] [operator] [value]" with X to remove
     - Example chips: "Role is Student", "Onboarding is Complete", "Has Profile Picture"
     - Chip styling: bg-blue-50 text-blue-700 border border-blue-200 rounded-full px-3 py-1 text-sm, X button on right
   - "AND" text between chips (text-xs text-slate-400 uppercase)
   - "Add Condition" button opens a small form/popover:
     - Step 1: Select condition type from dropdown (6 types)
     - Step 2: Based on type, show operator and value inputs:
       - role: operator="is"|"is_not", value=multi-select from ['student', 'teacher', 'wellness_practitioner', 'admin', 'moderator']
       - onboarding_status: operator="is", value=select ['complete', 'incomplete']
       - has_profile_picture: operator="is", value=select [true, false]
       - subscription_status: operator="is"|"is_not", value=select ['active', 'trialing', 'canceled', 'none']
       - birthday: operator=select ['is_set', 'is_not_set', 'is_today'], no value input needed
       - flow_completed: operator=select ['has_completed', 'has_not_completed'], value=text input for flow_id (or flow name autocomplete in future)
     - "Add" button appends condition to array and calls onChange
   - All conditions are ANDed together (displayed explicitly between chips)

4. Wire into FlowEditorShell.tsx:
   - FlowSettingsPanel renders above the three-panel grid (collapsible, per CONTEXT.md)
   - When collapsed: single row showing "Flow Settings" + current display type + trigger as summary
   - StepActionsEditor renders in the right panel below element properties (or as its own tab)
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - test -f app/admin/flows/components/editor/StepActionsEditor.tsx (actions editor exists)
    - grep -q "save_to_profile\|send_email\|kit_tag\|stripe_checkout" app/admin/flows/components/editor/StepActionsEditor.tsx (all 8 action types listed)
    - test -f app/admin/flows/components/editor/FlowSettingsPanel.tsx (settings panel exists)
    - grep -q "display_type\|trigger_type\|frequency" app/admin/flows/components/editor/FlowSettingsPanel.tsx (flow settings fields)
    - grep -q "modal_dismissible\|modal_backdrop" app/admin/flows/components/editor/FlowSettingsPanel.tsx (modal-specific settings)
    - test -f app/admin/flows/components/editor/ConditionsBuilder.tsx (conditions builder exists)
    - grep -q "role\|onboarding_status\|has_profile_picture\|subscription_status\|birthday\|flow_completed" app/admin/flows/components/editor/ConditionsBuilder.tsx (all 6 condition types)
    - grep -q "FlowSettingsPanel" app/admin/flows/components/editor/FlowEditorShell.tsx (settings panel wired)
  </acceptance_criteria>
  <done>
    Admin can configure 8 action types per step. Can expand flow settings panel to change display type (with modal-specific options), trigger, frequency. Can build conditions using chip-based AND logic with all 6 condition types. All settings save to API.
  </done>
</task>

<task type="auto">
  <name>Task 2: Flow preview mode as in-editor modal overlay</name>
  <files>
    app/admin/flows/components/editor/FlowPreviewModal.tsx,
    app/admin/flows/components/editor/FlowEditorShell.tsx,
    lib/flows/editor-store.ts
  </files>
  <read_first>
    lib/flows/editor-store.ts,
    lib/flows/types.ts,
    app/admin/flows/components/editor/FlowEditorShell.tsx
  </read_first>
  <action>
1. Add preview state to editor store:
   - `isPreviewOpen: boolean`
   - `previewStepIndex: number` (0-based, for navigating through steps)
   - `openPreview: () => void` — sets isPreviewOpen=true, previewStepIndex=0
   - `closePreview: () => void`
   - `previewNext: () => void` — increments previewStepIndex (bounded by steps.length)
   - `previewBack: () => void` — decrements previewStepIndex (min 0)

2. Create FlowPreviewModal.tsx ('use client'):
   - Reads isPreviewOpen, previewStepIndex, flow, steps from useEditorStore()
   - Full-screen overlay (fixed inset-0 z-50 bg-black/60 flex items-center justify-center)
   - "PREVIEW MODE" watermark: absolute positioned, rotated text, semi-transparent (text-6xl font-bold text-white/10 rotate-[-30deg] pointer-events-none) — per CONTEXT.md decision
   - Render based on flow.display_type:
     - **modal**: centered card (max-w-lg w-full bg-white rounded-2xl shadow-2xl p-6 max-h-[80vh] overflow-y-auto)
       - If modal_dismissible: show X close button top-right
       - If modal_backdrop === 'blur': backdrop-blur-sm on overlay
       - If modal_backdrop === 'dark': bg-black/70 on overlay
     - **fullscreen**: full viewport (inset-0 bg-white), no dismiss
     - **top_banner**: fixed top-0 left-0 right-0 bar (h-16 bg-primary text-white flex items-center px-6)
     - **bottom_banner**: fixed bottom-0 left-0 right-0 bar (h-16 bg-primary text-white flex items-center px-6)
     - **notification**: fixed top-4 right-4 card (w-80 bg-white rounded-xl shadow-xl p-4 border)
   - Step content rendering (for modal/fullscreen which are multi-step):
     - Show current step title
     - Render each element in the step as a preview:
       - info_text: paragraph text
       - short_text: disabled text input with label and placeholder
       - long_text: disabled textarea with label
       - single_choice: pill buttons (Typeform-style) — flex-wrap gap-2, each option as rounded-full px-4 py-2 border border-slate-300 text-sm hover:border-primary hover:bg-primary/5 cursor-pointer
       - multi_choice: same as single_choice but allows multiple selected
       - dropdown: disabled select element
       - image_upload: upload placeholder box (dashed border, upload icon)
       - image: img tag with src and alt
       - video: simple iframe or video embed placeholder
     - Progress bar at top: w-full h-1 bg-slate-200 with filled portion (bg-primary) based on (previewStepIndex + 1) / steps.length
     - Navigation: "Back" button (disabled on first step), "Next" button (or "Complete" on last step)
     - No data is actually saved — this is purely visual preview
   - Banner/notification preview:
     - Show the first step's first element content as the banner text
     - CTA button text from first element label or "View"
     - Close button (X)
   - Close preview: "Exit Preview" button fixed top-right, or Escape key

3. Wire into FlowEditorShell.tsx:
   - "Preview" button in the top bar (next to save status indicator)
   - Calls store.openPreview()
   - FlowPreviewModal renders conditionally when isPreviewOpen
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - test -f app/admin/flows/components/editor/FlowPreviewModal.tsx (preview modal exists)
    - grep -q "PREVIEW MODE\|PREVIEW" app/admin/flows/components/editor/FlowPreviewModal.tsx (watermark present)
    - grep -q "modal\|fullscreen\|top_banner\|bottom_banner\|notification" app/admin/flows/components/editor/FlowPreviewModal.tsx (all 5 display types handled)
    - grep -q "previewNext\|previewBack\|previewStepIndex" app/admin/flows/components/editor/FlowPreviewModal.tsx (step navigation)
    - grep -q "isPreviewOpen\|openPreview" lib/flows/editor-store.ts (preview state in store)
    - grep -q "Preview\|preview" app/admin/flows/components/editor/FlowEditorShell.tsx (preview button in editor)
  </acceptance_criteria>
  <done>
    Admin clicks "Preview" in the editor top bar. Sees the flow rendered as a user would see it, in the correct display type (modal, fullscreen, banner, notification). Can navigate through steps with back/next. "PREVIEW MODE" watermark visible. No data saved. Can exit preview to return to editor.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete flow builder UI</name>
  <what-built>Complete admin flow builder UI: flow list page with tabs and drag-reorder, three-panel editor with step management, 9 element types, property editing, profile field mapping, branching, step actions, flow settings with conditions builder, and preview mode.</what-built>
  <how-to-verify>
    1. Navigate to /admin — verify "Flows" appears in sidebar between Chatbot and API Keys
    2. Click "Flows" — verify tabbed list page loads (Active, Draft, Paused, Archived, Templates tabs)
    3. Click "Create Flow" — verify modal opens, fill in name "Test Flow", select Modal display type, click Create
    4. Verify new flow appears in Draft tab with status badge and display type icon
    5. Click "Edit" on the new flow — verify three-panel editor opens
    6. Click "Add Step" in left sidebar — verify step appears
    7. Click "Add Element" on canvas — verify 9-type popover grid appears
    8. Add a "Short Text" element — verify it appears on canvas
    9. Click the element — verify right panel shows properties (label, key, required, help_text)
    10. Add a "Single Choice" element with options — verify options editor works
    11. Enable branching on the single_choice — verify target step dropdowns appear per option
    12. Expand "Flow Settings" — verify display type, trigger, frequency, conditions fields
    13. Add a condition (e.g., Role is Student) — verify chip appears with X to remove
    14. Click "Preview" — verify flow renders as modal with PREVIEW MODE watermark
    15. Navigate through preview steps with back/next — verify it works
    16. Exit preview — verify return to editor
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit`
2. All 15 ADMIN requirements verifiable through the UI
3. Flow list: tabs, drag-reorder, CRUD, stats
4. Editor: three-panel, step management, element management
5. Properties: all element types, profile mapping, branching
6. Settings: display type, trigger, frequency, conditions
7. Preview: all 5 display types, step navigation, watermark
</verification>

<success_criteria>
- Step actions editor shows all 8 action types with type-specific config
- Flow settings panel is collapsible with display type, modal options, trigger, frequency
- Conditions builder uses chip-based AND logic with all 6 condition types
- Preview mode renders flow in correct display type with PREVIEW MODE watermark
- Preview supports step navigation (back/next) with progress bar
- Human verification confirms all 15 ADMIN requirements are satisfied
</success_criteria>

<output>
After completion, create `.planning/workstreams/flow-builder/phases/03-admin-flow-builder-ui/03-04-SUMMARY.md`
</output>
