---
phase: 20-fix-broken-admin-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/admin/shop/orders/page.tsx
  - app/admin/shop/orders/OrdersPagination.tsx
  - app/admin/shop/analytics/page.tsx
  - app/admin/audit-log/page.tsx
autonomous: true
requirements: [FIX-02, FIX-03, FIX-04, FIX-05]

must_haves:
  truths:
    - "/admin/shop/orders loads without server-side exceptions and shows order rows or empty state"
    - "/admin/shop/analytics loads without server-side exceptions and shows metric cards or empty state"
    - "/admin/audit-log loads without server-side exceptions and shows log entries or empty state"
    - "/admin/shop/products has a '+ Create Product' button (already present — must remain)"
  artifacts:
    - path: "app/admin/shop/orders/OrdersPagination.tsx"
      provides: "Pagination component for orders page that navigates to /admin/shop/orders"
    - path: "app/admin/shop/orders/page.tsx"
      provides: "Orders page using OrdersPagination instead of AdminUsersPagination"
    - path: "app/admin/shop/analytics/page.tsx"
      provides: "Analytics page with all data fetches wrapped in try/catch, shows empty state on error"
    - path: "app/admin/audit-log/page.tsx"
      provides: "Audit log page using getSupabaseService (service role) instead of anon client"
  key_links:
    - from: "app/admin/shop/orders/page.tsx"
      to: "app/admin/shop/orders/OrdersPagination.tsx"
      via: "import and render with page/totalPages/pageSize props"
      pattern: "OrdersPagination"
    - from: "app/admin/audit-log/page.tsx"
      to: "lib/supabase/service.ts"
      via: "getSupabaseService() call inside try/catch"
      pattern: "getSupabaseService"
---

<objective>
Fix three crashing admin pages so they load without server-side exceptions. All three have specific bugs:
1. Orders page embeds `AdminUsersPagination` which hardcodes navigation to `/admin/users` — replace with a correct `OrdersPagination` component.
2. Analytics page crashes; add explicit null-guards on all `Promise.all` result destructuring and ensure error state renders correctly even if `fetchError` is set during the JSX rendering phase.
3. Audit log page uses `createSupabaseServerClient` (anon + user session, subject to RLS) — switch to `getSupabaseService()` (service role) so the query always executes. Also the `ip_address` inet column must be cast to string to avoid runtime serialization errors.

Note on FIX-05: `/admin/shop/products/page.tsx` already has a "+ Create Product" button at line 205–213. No change needed for that file — confirm and skip.

Purpose: Unblock admin workflow — these pages are part of the core admin shell and currently crash on load.
Output: Three fixed page files + one new OrdersPagination component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/stripe-admin/ROADMAP.md
@.planning/workstreams/stripe-admin/STATE.md

<interfaces>
<!-- Key signatures the executor needs. Do not explore codebase — use these directly. -->

From lib/supabase/service.ts:
```typescript
// Service role client — bypasses RLS. Lazy singleton, throws if SUPABASE_SERVICE_ROLE_KEY missing.
export function getSupabaseService(): SupabaseClient<Database>
```

From lib/supabaseServer.ts:
```typescript
// Anon/session client — respects RLS. Requires user session cookie.
export async function createSupabaseServerClient(): Promise<SupabaseClient>
```

From app/admin/users/AdminUsersPagination.tsx (DO NOT use in orders — it hardcodes /admin/users):
```typescript
// navigate() calls router.replace(`/admin/users?${params}`) — WRONG for orders page
export default function AdminUsersPagination(props: { page, pageSize, totalPages, totalCount, displayedCount })
```

From app/admin/audit-log/AuditLogPagination.tsx (reference for pagination pattern):
```typescript
// navigate() calls router.replace(`/admin/audit-log?${params}`)
export default function AuditLogPagination(props: { page, pageSize, totalPages, totalCount, displayedCount })
```

stripe_orders table columns (from supabase/migrations/20260340_stripe_tables.sql):
- id, stripe_id, stripe_customer_id, stripe_price_id, stripe_product_id
- user_id, amount_total, type, status, currency
- subscription_status text, cancel_at_period_end boolean, current_period_end timestamptz, canceled_at timestamptz
- metadata jsonb, created_at timestamptz

audit_log table columns (from supabase/migrations/20260344_audit_log.sql):
- id uuid, category text, action text, severity text
- actor_id uuid, actor_name text, actor_role text
- target_type text, target_id text, target_label text
- description text, metadata jsonb
- ip_address inet  ← Postgres returns this as string, but cast needed to avoid type errors
- created_at timestamptz

audit_log RLS: only admins/moderators can read — anon client with no session returns empty/error.
Service role client bypasses RLS → always works from server code.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix orders page — replace AdminUsersPagination with OrdersPagination</name>
  <files>app/admin/shop/orders/OrdersPagination.tsx, app/admin/shop/orders/page.tsx</files>
  <read_first>
    - app/admin/shop/orders/page.tsx (full file — understand how AdminUsersPagination is called)
    - app/admin/audit-log/AuditLogPagination.tsx (use as template for the new component)
    - app/admin/users/AdminUsersPagination.tsx (understand what is broken — navigates to /admin/users)
  </read_first>
  <action>
    Step 1 — Create `app/admin/shop/orders/OrdersPagination.tsx`.
    Copy the structure of `AuditLogPagination.tsx` exactly, but:
    - Change the route from `/admin/audit-log` to `/admin/shop/orders`
    - Change the label from "entries" to "orders" in the count display
    - Keep the same Props interface: `{ page, pageSize, totalPages, totalCount, displayedCount }`
    - Keep `'use client'` directive
    - Keep useRouter, useSearchParams, useTransition pattern

    Step 2 — Edit `app/admin/shop/orders/page.tsx`:
    - Remove: `import AdminUsersPagination from '@/app/admin/users/AdminUsersPagination'`
    - Add: `import OrdersPagination from './OrdersPagination'`
    - Replace: `<AdminUsersPagination page={page} pageSize={pageSize} totalPages={totalPages} totalCount={totalCount} displayedCount={displayedCount} />`
    - With: `<OrdersPagination page={page} pageSize={pageSize} totalPages={totalPages} totalCount={totalCount} displayedCount={displayedCount} />`
    - The Suspense wrapper stays as-is.

    No other changes to page.tsx — the try/catch and data fetching are already correct.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | grep -E "orders/page|OrdersPagination" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `app/admin/shop/orders/OrdersPagination.tsx` exists with `'use client'` directive
    - grep "admin/shop/orders" app/admin/shop/orders/OrdersPagination.tsx returns a match (navigate targets correct route)
    - grep "AdminUsersPagination" app/admin/shop/orders/page.tsx returns no matches
    - grep "OrdersPagination" app/admin/shop/orders/page.tsx returns a match
  </acceptance_criteria>
  <done>Orders page imports OrdersPagination instead of AdminUsersPagination. Pagination navigates to /admin/shop/orders.</done>
</task>

<task type="auto">
  <name>Task 2: Fix audit-log page — switch to service role client</name>
  <files>app/admin/audit-log/page.tsx</files>
  <read_first>
    - app/admin/audit-log/page.tsx (full file — understand current anon client usage and ip_address handling)
    - lib/supabase/service.ts (understand getSupabaseService signature)
  </read_first>
  <action>
    The audit-log page currently uses `createSupabaseServerClient()` (anon client). The `audit_log` table
    has RLS that only allows admins/moderators to read. Using the anon client can fail if the session cookie
    isn't available in certain server rendering contexts. Switch to `getSupabaseService()` so the query
    always succeeds server-side.

    Changes to `app/admin/audit-log/page.tsx`:

    1. Remove import: `import { createSupabaseServerClient } from '@/lib/supabaseServer'`
    2. Add import: `import { getSupabaseService } from '@/lib/supabase/service'`
    3. Inside the try block, change:
       `const supabase = await createSupabaseServerClient()`
       to:
       `const supabase = getSupabaseService()`
       (note: no `await` — getSupabaseService is synchronous)

    4. The `ip_address` column is Postgres `inet` type. Supabase returns it as a string. The `AuditRow`
       type already declares `ip_address: string | null` which is correct. No cast change needed.

    5. The existing error handling (try/catch + `if (dbError)` check) is already correct — keep as-is.

    6. No other changes. The rest of the page is well-structured.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | grep "audit-log/page" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "createSupabaseServerClient" app/admin/audit-log/page.tsx returns no matches
    - grep "getSupabaseService" app/admin/audit-log/page.tsx returns a match
    - grep "await getSupabaseService" app/admin/audit-log/page.tsx returns no matches (it's synchronous, no await)
  </acceptance_criteria>
  <done>Audit log page uses service role client. RLS is bypassed server-side. Page loads without exceptions.</done>
</task>

<task type="auto">
  <name>Task 3: Diagnose and fix analytics page crash</name>
  <files>app/admin/shop/analytics/page.tsx</files>
  <read_first>
    - app/admin/shop/analytics/page.tsx (full file — find crash root cause)
    - lib/analytics/metrics.ts (verify function signatures match what page passes)
    - app/admin/shop/analytics/AnalyticsCharts.tsx (check for recharts issues)
  </read_first>
  <action>
    The analytics page appears to crash with Vercel digest 2903588106. Read the page carefully.

    Known issue candidates (check each in order):

    A. `Promise.all` destructuring — if any of the four queries throws (not just returns error),
       the catch block handles it. But check: does `ordersRes`, `profilesRes`, etc. throw on
       schema mismatch? The `stripe_orders`, `profiles`, `stripe_prices`, `schools` tables all exist
       in `types/supabase.ts`, so this should be fine.

    B. `computeFunnelMetrics` or `computeRevenueMetrics` — check function signatures in
       `lib/analytics/metrics.ts` match what page passes. If there's a type/argument mismatch
       that causes a runtime error OUTSIDE the try/catch block (e.g. in the CSV data construction
       at lines 144-168), the page will crash.

    C. The CSV data and render code at lines 144-168 runs OUTSIDE the try/catch. If `funnel` or
       `revenue` have any undefined/null values (e.g. `funnel.conversionRate` is NaN), operations
       like `.toFixed()` will throw. This is the most likely crash: if `computeFunnelMetrics` returns
       NaN for `conversionRate`, then `(funnel.conversionRate * 100).toFixed(1)` throws at line 148,
       which is outside the try/catch.

    Fix: Wrap the CSV data construction in safe null-coalescing:
    - Line 148: `(funnel.conversionRate * 100).toFixed(1) + '%'`
      → `((funnel.conversionRate ?? 0) * 100).toFixed(1) + '%'`
    - Line 208-209: `(funnel.conversionRate * 100).toFixed(1) + '%'`
      → `((funnel.conversionRate ?? 0) * 100).toFixed(1) + '%'`
    - Lines 159-162: `revenue.arrTotal.toFixed(2)` etc. → `(revenue.arrTotal ?? 0).toFixed(2)` etc.
    - Lines 248-269: Similar `.toLocaleString()` calls → add `?? 0` guards

    Also add a guard: if the try/catch catches an error, the `funnelCsvData` and `revenueCsvData`
    construction (lines 144-168) still runs with the default zero values — which is safe.

    Apply the minimal targeted fix: add `?? 0` null-coalescing to every `.toFixed()` and
    `.toLocaleString()` call on `funnel.*` and `revenue.*` values in the page.

    If after reading the file the crash is clearly something else (different import error, missing
    component, etc.), fix that instead and document the actual root cause in the task output.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | grep "analytics/page" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "conversionRate \* 100" app/admin/shop/analytics/page.tsx returns no matches (replaced with null-safe version)
    - grep "conversionRate ?? 0" app/admin/shop/analytics/page.tsx returns at least 1 match
    - npx tsc --noEmit exits without errors in analytics/page.tsx
  </acceptance_criteria>
  <done>Analytics page has null-safe arithmetic on all funnel/revenue values. Page renders without crashing even when data is zero or metrics return edge-case values.</done>
</task>

</tasks>

<verification>
After all three tasks complete:

1. Orders: `grep "OrdersPagination" app/admin/shop/orders/page.tsx` — must return a match
2. Orders: `grep "AdminUsersPagination" app/admin/shop/orders/page.tsx` — must return NO match
3. Audit log: `grep "getSupabaseService" app/admin/audit-log/page.tsx` — must return a match
4. Audit log: `grep "createSupabaseServerClient" app/admin/audit-log/page.tsx` — must return NO match
5. Analytics: `grep "conversionRate ?? 0" app/admin/shop/analytics/page.tsx` — must return a match
6. Products page: `grep "Create Product" app/admin/shop/products/page.tsx` — must return a match (already exists, no change)
7. TypeScript: `npx tsc --noEmit` runs cleanly with no errors in the modified files
</verification>

<success_criteria>
- All four requirements addressed: FIX-02 (orders), FIX-03 (analytics), FIX-04 (audit-log), FIX-05 (products button already exists — confirmed)
- OrdersPagination.tsx exists and navigates to /admin/shop/orders
- audit-log/page.tsx uses getSupabaseService (service role, bypasses RLS)
- analytics/page.tsx has null-safe arithmetic preventing NaN crashes
- TypeScript compiles cleanly on modified files
</success_criteria>

<output>
After completion, create `.planning/workstreams/stripe-admin/phases/20-fix-broken-admin-pages/20-01-SUMMARY.md`
</output>
