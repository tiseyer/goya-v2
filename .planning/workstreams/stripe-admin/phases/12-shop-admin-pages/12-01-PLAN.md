---
phase: 12-shop-admin-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/admin/shop/products/actions.ts
  - app/admin/shop/products/actions.test.ts
  - app/admin/shop/products/page.tsx
  - app/admin/shop/products/ProductsTable.tsx
  - app/admin/shop/products/ProductsFilters.tsx
autonomous: true
requirements: [PROD-01, PROD-02, PROD-03, PROD-04, PROD-11]

must_haves:
  truths:
    - "Admin sees all products in a table with Name, Price, Type, Status pill, Sales count columns"
    - "Admin can click a Status pill to toggle Published/Draft inline"
    - "Admin can select multiple products and bulk change status or bulk delete"
    - "Admin can drag-and-drop reorder products and the new order persists to products.priority"
    - "Admin can soft-delete a product and it remains visible with a Deleted status pill"
  artifacts:
    - path: "app/admin/shop/products/actions.ts"
      provides: "Server Actions for toggleStatus, bulkAction, reorderProducts, softDelete"
      exports: ["toggleProductStatus", "bulkProductAction", "reorderProducts", "softDeleteProduct"]
    - path: "app/admin/shop/products/actions.test.ts"
      provides: "Unit tests for reorder, visibility, archivePrice stubs"
      contains: "describe"
    - path: "app/admin/shop/products/page.tsx"
      provides: "Server Component products list page"
      contains: "export default async function"
    - path: "app/admin/shop/products/ProductsTable.tsx"
      provides: "Client Component with dnd-kit sortable table"
      contains: "use client"
    - path: "app/admin/shop/products/ProductsFilters.tsx"
      provides: "Client Component filter bar"
      contains: "use client"
  key_links:
    - from: "app/admin/shop/products/page.tsx"
      to: "supabase"
      via: "createSupabaseServerClient query on products LEFT JOIN stripe_products LEFT JOIN stripe_prices"
      pattern: "from.*products"
    - from: "app/admin/shop/products/ProductsTable.tsx"
      to: "app/admin/shop/products/actions.ts"
      via: "Server Action calls for toggle, bulk, reorder, delete"
      pattern: "import.*actions"
    - from: "app/admin/shop/products/ProductsTable.tsx"
      to: "@dnd-kit/sortable"
      via: "DndContext + SortableContext + useSortable for drag-and-drop"
      pattern: "DndContext"
---

<objective>
Build the Products list page under /admin/shop/products with a sortable table, inline status toggle, bulk actions, and drag-and-drop reorder.

Purpose: Admins need to view, manage status, reorder, and bulk-manage all products from a single table view.
Output: Products list page with Server Actions, dnd-kit drag-and-drop, and unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/stripe-admin/ROADMAP.md
@.planning/workstreams/stripe-admin/STATE.md
@.planning/workstreams/stripe-admin/phases/12-shop-admin-pages/12-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From lib/stripe/client.ts:
```typescript
import 'server-only'
export function getStripe(): Stripe
```

From lib/supabase/service.ts:
```typescript
export function getSupabaseService(): SupabaseClient
```

From lib/supabaseServer.ts:
```typescript
export async function createSupabaseServerClient(): Promise<SupabaseClient>
```

GOYA `products` table columns (GOYA-owned):
- id (uuid PK), slug, name, full_name, category, price_display, price_cents, image_path, description, features (jsonb), requires_any_of (text[]), hidden_if_has_any (text[]), has_variants, variants (jsonb), priority (integer), is_active (boolean), stripe_product_id (text nullable), created_at, updated_at

`stripe_products` table columns (Stripe-owned):
- id (uuid PK), stripe_id (text UNIQUE), name, description, active (boolean), images (text[]), metadata (jsonb), created_at, updated_at

`stripe_prices` table columns:
- id (uuid PK), stripe_id (text UNIQUE), stripe_product_id (text), currency, unit_amount (integer), type (one_time|recurring), interval, active (boolean), created_at, updated_at

`stripe_orders` table (for sales count):
- stripe_product_id (text) — count WHERE stripe_product_id matches

Admin Users page pattern (from app/admin/users/page.tsx):
- Server Component with searchParams, createSupabaseServerClient(), filter query, passes data to Client Component
- Table wrapper: `<div className="bg-white rounded-xl border border-[#E5E7EB] shadow-sm overflow-hidden">`
- Header row: `<tr className="text-left text-[11px] font-semibold text-[#6B7280] uppercase tracking-wider border-b border-[#E5E7EB] bg-slate-50">`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install dnd-kit and create Products Server Actions with tests</name>
  <files>
    app/admin/shop/products/actions.ts,
    app/admin/shop/products/actions.test.ts
  </files>
  <read_first>
    lib/stripe/client.ts,
    lib/supabase/service.ts,
    lib/stripe/handlers/product.ts,
    app/admin/users/page.tsx
  </read_first>
  <behavior>
    - toggleProductStatus(productId, stripeProductId, newIsActive): updates products.is_active via getSupabaseService(), calls getStripe().products.update(stripeProductId, { active: newIsActive }), calls revalidatePath('/admin/shop/products')
    - bulkProductAction(ids[], action): when action='delete' calls softDeleteProduct for each; when action='draft' sets is_active=false for each; when action='publish' sets is_active=true for each
    - reorderProducts(orderedStripeProductIds[]): for each ID at index i, updates products.priority = i+1 WHERE stripe_product_id = ID via getSupabaseService(); calls revalidatePath('/admin/shop/products')
    - softDeleteProduct(productId, stripeProductId): sets products.is_active=false via getSupabaseService(), calls getStripe().products.update(stripeProductId, { active: false }), calls revalidatePath('/admin/shop/products')
  </behavior>
  <action>
    1. Run `npm install @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities` in the project root.

    2. Create `app/admin/shop/products/actions.ts` with 'use server' and 'server-only' imports. Implement four exported async functions:

    ```
    toggleProductStatus(productId: string, stripeProductId: string, newIsActive: boolean)
    ```
    - `getSupabaseService().from('products').update({ is_active: newIsActive }).eq('id', productId)`
    - `getStripe().products.update(stripeProductId, { active: newIsActive })`
    - `revalidatePath('/admin/shop/products')`

    ```
    bulkProductAction(items: Array<{ productId: string; stripeProductId: string }>, action: 'publish' | 'draft' | 'delete')
    ```
    - For 'publish': update each products.is_active = true, stripe.products.update active: true
    - For 'draft': update each products.is_active = false, stripe.products.update active: true (stays in Stripe, just hidden in GOYA)
    - For 'delete': call softDeleteProduct for each item

    ```
    reorderProducts(orderedStripeProductIds: string[])
    ```
    - Promise.all mapping each ID to `getSupabaseService().from('products').update({ priority: index + 1 }).eq('stripe_product_id', id)`
    - `revalidatePath('/admin/shop/products')`

    ```
    softDeleteProduct(productId: string, stripeProductId: string)
    ```
    - `getSupabaseService().from('products').update({ is_active: false }).eq('id', productId)`
    - `getStripe().products.update(stripeProductId, { active: false })`
    - `revalidatePath('/admin/shop/products')`

    3. Create `app/admin/shop/products/actions.test.ts` with Vitest. Mock `getStripe` from `@/lib/stripe/client` and `getSupabaseService` from `@/lib/supabase/service`. Mock `revalidatePath` from `next/cache`.

    Test cases:
    - `describe('reorderProducts')`: call with ['sp_1', 'sp_2', 'sp_3'], verify supabase .update({ priority: 1 }).eq('stripe_product_id', 'sp_1') was called, and priority 2 for sp_2, priority 3 for sp_3
    - `describe('toggleProductStatus')`: call with (id, stripeId, true), verify supabase updates is_active=true AND stripe.products.update called with { active: true }
    - `describe('softDeleteProduct')`: verify is_active set to false AND stripe.products.update called with { active: false }
    - `describe('bulkProductAction')`: verify 'publish' sets is_active=true for all items

    IMPORTANT: Write-partitioning — GOYA-owned columns (is_active, priority) go to `products` table via getSupabaseService(). Stripe active state goes to Stripe API via getStripe(). Never write GOYA-owned columns to stripe_products table.
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx vitest run app/admin/shop/products/actions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - app/admin/shop/products/actions.ts contains `'use server'` on line 1
    - app/admin/shop/products/actions.ts contains `import 'server-only'`
    - app/admin/shop/products/actions.ts contains `export async function toggleProductStatus(`
    - app/admin/shop/products/actions.ts contains `export async function bulkProductAction(`
    - app/admin/shop/products/actions.ts contains `export async function reorderProducts(`
    - app/admin/shop/products/actions.ts contains `export async function softDeleteProduct(`
    - app/admin/shop/products/actions.ts contains `.from('products')` (writes to GOYA table, NOT stripe_products)
    - app/admin/shop/products/actions.test.ts contains `describe('reorderProducts'`
    - app/admin/shop/products/actions.test.ts contains `describe('toggleProductStatus'`
    - package.json contains `@dnd-kit/core`
    - npx vitest run app/admin/shop/products/actions.test.ts exits 0
  </acceptance_criteria>
  <done>All four Server Actions exist with server-only guard, write-partitioning is correct (GOYA columns to products table, Stripe calls for active state), and all tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Build Products list page with dnd-kit sortable table, status pills, bulk actions, and filters</name>
  <files>
    app/admin/shop/products/page.tsx,
    app/admin/shop/products/ProductsTable.tsx,
    app/admin/shop/products/ProductsFilters.tsx
  </files>
  <read_first>
    app/admin/users/page.tsx,
    app/admin/users/AdminUsersTable.tsx,
    app/admin/users/AdminUsersFilters.tsx,
    app/admin/shop/products/actions.ts
  </read_first>
  <action>
    1. Create `app/admin/shop/products/page.tsx` as a Server Component following the exact pattern from app/admin/users/page.tsx:
    - Import `createSupabaseServerClient` from `@/lib/supabaseServer`
    - Accept `searchParams: Promise<{ [key: string]: string | string[] | undefined }>`
    - Parse `search`, `status` (all/published/draft/deleted), `type` (all/one_time/recurring), `sort`, `page`, `pageSize` from searchParams
    - Query: Fetch from `products` table with `select('id, name, slug, price_cents, price_display, image_path, is_active, priority, stripe_product_id, category')` ordered by `priority ASC NULLS LAST`
    - For each product row that has a stripe_product_id, fetch matching stripe_products row to get `active` status and stripe_prices to get price/type info
    - Approach: Do TWO queries — one for products, one for stripe_products + stripe_prices joined. Merge in JS by stripe_product_id.
    - For sales count: Query `stripe_orders` grouped by `stripe_product_id` using `.select('stripe_product_id')` — but for simplicity in Phase 12, show count from a separate query: `supabase.from('stripe_orders').select('stripe_product_id', { count: 'exact' }).eq('stripe_product_id', spId)` per product. OR better: fetch all stripe_orders, group in JS. Use the simpler approach.
    - Actually simplest: fetch `stripe_orders` with `.select('stripe_product_id')` then count in JS with a Map.
    - Apply search filter: `.or('name.ilike.%${search}%,slug.ilike.%${search}%')` if search param exists
    - Derive status for each product: if stripe_products.active=false -> 'Deleted'; if products.is_active=false -> 'Draft'; else 'Published'
    - Apply status filter client-side after derivation (or pre-filter: status='deleted' -> where stripe_product active=false; status='draft' -> where is_active=false AND stripe active=true; status='published' -> where is_active=true AND stripe active=true)
    - Paginate with `.range(from, to)`
    - Render layout: `<div className="p-6 lg:p-8">`, header with "Products" title + count, Suspense-wrapped filters, then ProductsTable, then pagination

    2. Create `app/admin/shop/products/ProductsTable.tsx` as a Client Component ('use client'):
    - Define type `ProductRow = { id: string; name: string; slug: string; priceCents: number; priceDisplay: string; imagePath: string | null; isActive: boolean; priority: number | null; stripeProductId: string | null; stripeActive: boolean | null; priceType: 'one_time' | 'recurring' | null; salesCount: number; derivedStatus: 'Published' | 'Draft' | 'Deleted' }`
    - Props: `{ initialProducts: ProductRow[] }`
    - State: `products` (from initialProducts), `selected` (Set of IDs), `selectAll` (boolean)
    - Wrap table in DndContext + SortableContext from @dnd-kit:
      ```
      import { DndContext, closestCenter, KeyboardSensor, PointerSensor, useSensor, useSensors, DragEndEvent } from '@dnd-kit/core'
      import { SortableContext, sortableKeyboardCoordinates, verticalListSortingStrategy, useSortable, arrayMove } from '@dnd-kit/sortable'
      import { CSS } from '@dnd-kit/utilities'
      ```
    - Each row is a SortableProductRow component using `useSortable({ id: product.id })`
    - handleDragEnd: arrayMove products, then call `reorderProducts(newOrder.map(p => p.stripeProductId).filter(Boolean))` from actions.ts
    - Columns: Drag handle (grip icon) | Checkbox | Name (with small image thumbnail) | Price (formatted from priceCents) | Type pill ('One-time' or 'Recurring') | Status pill (Published=green, Draft=yellow, Deleted=red) | Sales count
    - Status pill onClick: if not Deleted, call `toggleProductStatus(product.id, product.stripeProductId, !product.isActive)` with optimistic update
    - Bulk action bar: appears when selected.size > 0, shows "N selected" + buttons: "Publish", "Draft", "Delete" — calls `bulkProductAction`
    - Soft-delete button per row (trash icon in actions column) calls `softDeleteProduct`
    - Table wrapper: `<div className="bg-white rounded-xl border border-[#E5E7EB] shadow-sm overflow-hidden">`
    - Header row: `<tr className="text-left text-[11px] font-semibold text-[#6B7280] uppercase tracking-wider border-b border-[#E5E7EB] bg-slate-50">`
    - Empty state: same pattern as AdminUsersTable (icon + "No products found" + "Try adjusting your filters.")

    3. Create `app/admin/shop/products/ProductsFilters.tsx` as a Client Component ('use client'):
    - Follow exact pattern from app/admin/users/AdminUsersFilters.tsx
    - Filters: search input (debounced, updates URL param `search`), status dropdown (All/Published/Draft/Deleted), type dropdown (All/One-time/Recurring), sort dropdown (Priority/Name A-Z/Name Z-A/Newest/Oldest)
    - Use `useRouter`, `useSearchParams`, `useTransition` from next/navigation and react
    - updateParam helper that sets param, resets page to 1, calls `router.replace`

    Status pill colors:
    - Published: `bg-emerald-100 text-emerald-700`
    - Draft: `bg-amber-100 text-amber-700`
    - Deleted: `bg-red-100 text-red-700`

    Type pill colors:
    - One-time: `bg-slate-100 text-slate-600`
    - Recurring: `bg-blue-100 text-blue-700`
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx tsc --noEmit 2>&1 | tail -20 && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - app/admin/shop/products/page.tsx contains `export default async function`
    - app/admin/shop/products/page.tsx contains `createSupabaseServerClient`
    - app/admin/shop/products/page.tsx contains `.from('products')`
    - app/admin/shop/products/ProductsTable.tsx contains `'use client'`
    - app/admin/shop/products/ProductsTable.tsx contains `DndContext`
    - app/admin/shop/products/ProductsTable.tsx contains `SortableContext`
    - app/admin/shop/products/ProductsTable.tsx contains `useSortable`
    - app/admin/shop/products/ProductsTable.tsx contains `import { CSS } from '@dnd-kit/utilities'`
    - app/admin/shop/products/ProductsTable.tsx contains `arrayMove`
    - app/admin/shop/products/ProductsTable.tsx contains `toggleProductStatus`
    - app/admin/shop/products/ProductsTable.tsx contains `bulkProductAction`
    - app/admin/shop/products/ProductsTable.tsx contains `reorderProducts`
    - app/admin/shop/products/ProductsFilters.tsx contains `'use client'`
    - app/admin/shop/products/ProductsFilters.tsx contains `useSearchParams`
    - npx next build completes without errors
  </acceptance_criteria>
  <done>Products list page renders at /admin/shop/products with dnd-kit sortable table showing all products, inline status pill toggle, bulk action bar, drag-and-drop reorder, and URL-based filters. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `npx vitest run app/admin/shop/products/actions.test.ts` passes (all Server Action tests green)
2. `npx next build` succeeds without errors
3. Products page renders with table, filters, and dnd-kit drag handles
4. Status pills show correct derived status (Published/Draft/Deleted)
</verification>

<success_criteria>
- All products visible in table with correct columns (PROD-01)
- Status pill toggles Published/Draft on click (PROD-02)
- Bulk select + action bar works (PROD-03)
- Drag-and-drop reorder persists to products.priority (PROD-04)
- Soft-delete sets Deleted status, row remains visible (PROD-11)
</success_criteria>

<output>
After completion, create `.planning/workstreams/stripe-admin/phases/12-shop-admin-pages/12-01-SUMMARY.md`
</output>
