---
phase: 01-database-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260376_media_folders_bucket_is_system.sql
  - types/supabase.ts
autonomous: true
requirements: [SCHEMA-01, SCHEMA-02, SCHEMA-03]

must_haves:
  truths:
    - "media_folders table has a bucket column with default 'media'"
    - "media_folders table has an is_system boolean column defaulting to false"
    - "TypeScript types include bucket and is_system on media_folders"
  artifacts:
    - path: "supabase/migrations/20260376_media_folders_bucket_is_system.sql"
      provides: "ALTER TABLE adding is_system column and bucket default"
      contains: "is_system"
    - path: "types/supabase.ts"
      provides: "Regenerated Supabase types"
      contains: "is_system"
  key_links:
    - from: "supabase/migrations/20260376_media_folders_bucket_is_system.sql"
      to: "types/supabase.ts"
      via: "supabase gen types reads remote schema after migration applied"
      pattern: "is_system.*boolean"
---

<objective>
Add is_system column and bucket default to media_folders, push migration to remote Supabase, regenerate TypeScript types.

Purpose: Phase 2 (sidebar UI + query logic) needs bucket defaults and is_system to distinguish system folders from user-created folders.
Output: Migration file applied to remote, updated types/supabase.ts committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/media-library-restructure/PROJECT.md
@.planning/workstreams/media-library-restructure/ROADMAP.md
@.planning/workstreams/media-library-restructure/STATE.md
@supabase/migrations/20260374_media_library_schema.sql

Note: media_folders already has `bucket text NOT NULL` (no default). This migration adds the default and the new is_system column.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create and apply migration for is_system column + bucket default</name>
  <files>supabase/migrations/20260376_media_folders_bucket_is_system.sql</files>
  <action>
Create migration file `supabase/migrations/20260376_media_folders_bucket_is_system.sql` with:

1. ALTER media_folders to add `is_system boolean NOT NULL DEFAULT false` (per SCHEMA-02)
2. ALTER media_folders to set DEFAULT 'media' on the existing `bucket` column (per SCHEMA-01) — the column already exists as `text NOT NULL` from migration 20260374, but has no default

The migration should be idempotent-safe. Do NOT recreate the table or drop anything.

After creating the file, push to remote:
```
npx supabase db push
```

Per project memory: always run `npx supabase db push` after creating a migration file.
  </action>
  <verify>
    <automated>npx supabase db push --dry-run 2>&1 | tail -5</automated>
  </verify>
  <done>Migration applied to remote Supabase. media_folders has is_system (boolean, default false) and bucket (default 'media').</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate Supabase TypeScript types</name>
  <files>types/supabase.ts</files>
  <action>
Regenerate types from remote schema (per SCHEMA-03):

```
npx supabase gen types typescript --project-id snddprncgilpctgvjukr --schema public > types/supabase.ts
```

After regeneration, verify the media_folders type in the output includes both `bucket` and `is_system` fields. The Row type should show `is_system: boolean` and `bucket: string`.
  </action>
  <verify>
    <automated>grep -A 15 "media_folders:" types/supabase.ts | grep -E "(is_system|bucket)"</automated>
  </verify>
  <done>types/supabase.ts contains media_folders with is_system: boolean and bucket: string fields in Row, Insert, and Update types.</done>
</task>

</tasks>

<verification>
1. Migration file exists at supabase/migrations/20260376_media_folders_bucket_is_system.sql
2. Remote DB has is_system column: verify via types regen showing the field
3. types/supabase.ts media_folders Row includes `is_system: boolean` and `bucket: string`
</verification>

<success_criteria>
- media_folders.is_system column exists (boolean, default false) — SCHEMA-02
- media_folders.bucket has default 'media' — SCHEMA-01
- types/supabase.ts regenerated with both columns — SCHEMA-03
- Existing folders unaffected (all default to bucket='media', is_system=false via column defaults)
</success_criteria>

<output>
After completion, create `.planning/workstreams/media-library-restructure/phases/01-database-schema/01-01-SUMMARY.md`
</output>
