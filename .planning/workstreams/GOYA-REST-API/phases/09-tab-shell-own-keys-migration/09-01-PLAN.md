---
phase: 09-tab-shell-own-keys-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/admin/api-keys/page.tsx
  - app/admin/api-keys/OwnKeysTab.tsx
  - app/admin/api-keys/SecretsPlaceholder.tsx
  - app/admin/api-keys/EndpointsPlaceholder.tsx
autonomous: false
requirements:
  - TABS-01
  - TABS-02
  - KEYS-01
  - KEYS-02

must_haves:
  truths:
    - "Admin can click between Own Keys, Third Party Keys, and Endpoints tabs and each tab renders without error"
    - "Tab navigation uses the same design tokens, spacing, and interaction patterns as the inbox tabs"
    - "All existing API key create, list, and revoke functionality works identically inside the Own Keys tab"
    - "No regression: existing API keys created before migration remain valid and visible"
  artifacts:
    - path: "app/admin/api-keys/page.tsx"
      provides: "Three-tab shell with URL-based switching via searchParams"
      contains: "searchParams"
    - path: "app/admin/api-keys/OwnKeysTab.tsx"
      provides: "Wrapper component rendering ApiKeysTable with header and create count"
      contains: "ApiKeysTable"
    - path: "app/admin/api-keys/SecretsPlaceholder.tsx"
      provides: "Placeholder tab content for Phase 10"
      contains: "Third Party Keys"
    - path: "app/admin/api-keys/EndpointsPlaceholder.tsx"
      provides: "Placeholder tab content for Phase 11"
      contains: "Endpoints"
  key_links:
    - from: "app/admin/api-keys/page.tsx"
      to: "app/admin/api-keys/OwnKeysTab.tsx"
      via: "conditional render based on activeTab"
      pattern: "activeTab.*keys.*OwnKeysTab"
    - from: "app/admin/api-keys/OwnKeysTab.tsx"
      to: "app/admin/api-keys/ApiKeysTable.tsx"
      via: "import and render with initialKeys prop"
      pattern: "ApiKeysTable.*initialKeys"
    - from: "app/admin/api-keys/page.tsx"
      to: "searchParams"
      via: "URL-based tab state (?tab=keys|secrets|endpoints)"
      pattern: "tab.*keys.*secrets.*endpoints"
---

<objective>
Convert the flat `/admin/api-keys` page into a three-tab interface (Own Keys, Third Party Keys, Endpoints) and move existing API key management into the Own Keys tab with zero functional regression.

Purpose: Unlocks the tab shell that Phase 10 (Secrets) and Phase 11 (Endpoints) will populate with real content.
Output: Three-tab admin page with working Own Keys tab and placeholder content for the other two tabs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/GOYA-REST-API/ROADMAP.md
@.planning/workstreams/GOYA-REST-API/STATE.md
@.planning/workstreams/GOYA-REST-API/phases/09-tab-shell-own-keys-migration/09-CONTEXT.md

@app/admin/api-keys/page.tsx
@app/admin/api-keys/ApiKeysTable.tsx
@app/admin/api-keys/actions.ts
@app/admin/inbox/page.tsx

<interfaces>
<!-- Tab pattern reference from inbox/page.tsx (lines 72-108) -->
<!-- The tab bar uses:
  - border-b border-slate-200 container
  - Link elements with searchParams-based href
  - Active: text-[#00B5A3] border-b-2 border-[#00B5A3]
  - Inactive: text-slate-500 hover:text-slate-700 border-b-2 border-transparent
  - Classes: relative px-5 py-3 text-sm font-semibold -mb-px transition-colors
-->

<!-- From ApiKeysTable.tsx -->
```typescript
// Props interface
export default function ApiKeysTable({ initialKeys }: { initialKeys: ApiKeyRow[] })

// Import from lib
import type { ApiKeyRow, ApiKeyPermission } from '@/lib/api/types'
```

<!-- From page.tsx (current) -->
```typescript
// Data fetching pattern
const { data: keys, error } = await (getSupabaseService() as any)
  .from('api_keys')
  .select('*')
  .order('created_at', { ascending: false })

const apiKeys = (keys ?? []) as ApiKeyRow[]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tab shell page and placeholder components</name>
  <files>
    app/admin/api-keys/page.tsx
    app/admin/api-keys/SecretsPlaceholder.tsx
    app/admin/api-keys/EndpointsPlaceholder.tsx
    app/admin/api-keys/OwnKeysTab.tsx
  </files>
  <read_first>
    app/admin/inbox/page.tsx
    app/admin/api-keys/page.tsx
    app/admin/api-keys/ApiKeysTable.tsx
  </read_first>
  <action>
    1. Create `app/admin/api-keys/OwnKeysTab.tsx` — a simple wrapper component that receives `initialKeys: ApiKeyRow[]` as a prop and renders the existing header (title "API Keys", count subtitle) plus `<ApiKeysTable initialKeys={initialKeys} />`. This is a server component that imports ApiKeysTable. Move the header JSX (the flex container with h1 "API Keys" and the count paragraph) from the current page.tsx into this component. Per KEYS-01, keep all existing functionality intact.

    2. Create `app/admin/api-keys/SecretsPlaceholder.tsx` — a simple component that renders a centered empty-state box with text "Third Party Keys" as heading and "Manage third-party API secrets. Coming in a future update." as description. Use the same empty-state styling as the ApiKeysTable empty state: `bg-white rounded-xl border border-[#E5E7EB] p-12 text-center`. Include a lock icon (SVG) in slate-300 above the text.

    3. Create `app/admin/api-keys/EndpointsPlaceholder.tsx` — same pattern as SecretsPlaceholder but with text "API Endpoints" as heading and "Browse and search all API endpoints. Coming in a future update." as description. Use a document/list icon (SVG) in slate-300.

    4. Rewrite `app/admin/api-keys/page.tsx` to implement the three-tab shell. Per TABS-01, the page must:
       - Accept `searchParams: Promise<{ tab?: string }>` as prop (Next.js 15 async pattern, per TABS-02 matching inbox/page.tsx)
       - Extract `tab` from `await searchParams`
       - Derive `activeTab`: if tab === 'secrets' then 'secrets', if tab === 'endpoints' then 'endpoints', else 'keys' (default)
       - Keep the existing data fetch for api_keys (only needed for the keys tab, but fetch unconditionally to keep it simple since it's fast)
       - Render the page header: `<h1>API Settings</h1>` with subtitle "Manage API keys, secrets, and endpoint documentation"
       - Render a tab bar matching the inbox pattern exactly (per TABS-02):
         - Container: `<div className="border-b border-slate-200"><div className="flex items-center gap-0">`
         - Three `<Link>` elements for tabs:
           - "Own Keys" -> href="/admin/api-keys?tab=keys"
           - "Third Party Keys" -> href="/admin/api-keys?tab=secrets"
           - "Endpoints" -> href="/admin/api-keys?tab=endpoints"
         - Active tab classes: `text-[#00B5A3] border-b-2 border-[#00B5A3]`
         - Inactive tab classes: `text-slate-500 hover:text-slate-700 border-b-2 border-transparent`
         - Shared classes: `relative px-5 py-3 text-sm font-semibold -mb-px transition-colors`
       - Conditionally render tab content:
         - `activeTab === 'keys'` -> `<OwnKeysTab initialKeys={apiKeys} />`
         - `activeTab === 'secrets'` -> `<SecretsPlaceholder />`
         - `activeTab === 'endpoints'` -> `<EndpointsPlaceholder />`
       - Keep the error boundary rendering (if api_keys fetch fails, show error div)
       - Import Link from 'next/link'
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "searchParams" app/admin/api-keys/page.tsx (URL-based tab switching)
    - grep -q "activeTab" app/admin/api-keys/page.tsx (tab state derivation)
    - grep -q "OwnKeysTab" app/admin/api-keys/page.tsx (own keys tab imported)
    - grep -q "SecretsPlaceholder" app/admin/api-keys/page.tsx (secrets placeholder imported)
    - grep -q "EndpointsPlaceholder" app/admin/api-keys/page.tsx (endpoints placeholder imported)
    - grep -q "tab=keys" app/admin/api-keys/page.tsx (own keys tab link)
    - grep -q "tab=secrets" app/admin/api-keys/page.tsx (secrets tab link)
    - grep -q "tab=endpoints" app/admin/api-keys/page.tsx (endpoints tab link)
    - grep -q "border-\[#00B5A3\]" app/admin/api-keys/page.tsx (active tab styling matches inbox)
    - grep -q "ApiKeysTable" app/admin/api-keys/OwnKeysTab.tsx (renders existing table)
    - grep -q "initialKeys" app/admin/api-keys/OwnKeysTab.tsx (passes keys prop)
    - grep -q "Third Party Keys" app/admin/api-keys/SecretsPlaceholder.tsx (placeholder text)
    - grep -q "API Endpoints" app/admin/api-keys/EndpointsPlaceholder.tsx (placeholder text)
    - grep -q "text-slate-500" app/admin/api-keys/page.tsx (inactive tab styling)
  </acceptance_criteria>
  <done>
    - Three-tab page renders at /admin/api-keys with Own Keys as default tab
    - Tab bar visually matches the inbox tab pattern (same colors, spacing, hover states)
    - OwnKeysTab renders ApiKeysTable with the fetched keys data
    - SecretsPlaceholder and EndpointsPlaceholder show clean empty states
    - Build succeeds with no errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify tab shell and Own Keys functionality</name>
  <files>app/admin/api-keys/page.tsx</files>
  <read_first>app/admin/api-keys/page.tsx</read_first>
  <action>
    Human verifies the three-tab interface works correctly at /admin/api-keys. All automated work is complete in Task 1. This checkpoint confirms visual correctness and functional regression-free behavior.
  </action>
  <what-built>
    Three-tab interface at /admin/api-keys with Own Keys tab containing all existing API key management (create, list, revoke), plus placeholder tabs for Third Party Keys and Endpoints.
  </what-built>
  <how-to-verify>
    1. Navigate to /admin/api-keys — should load with Own Keys tab active (teal underline)
    2. Verify all existing API keys are visible in the table
    3. Click "Third Party Keys" tab — should show placeholder content, URL changes to ?tab=secrets
    4. Click "Endpoints" tab — should show placeholder content, URL changes to ?tab=endpoints
    5. Click "Own Keys" tab — should return to API keys table, URL changes to ?tab=keys
    6. Test creating a new API key — should work identically as before (form appears, key generated, banner shown)
    7. Test revoking an API key — status should change to "Revoked"
    8. Verify tab styling matches the inbox page tabs (same teal active color, same spacing)
  </how-to-verify>
  <verify>
    <automated>echo "Human verification checkpoint — no automated check"</automated>
  </verify>
  <done>User confirms all three tabs render correctly, Own Keys functionality is regression-free, and tab styling matches inbox pattern</done>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- Build passes: `npx next build` completes without errors
- Tab navigation works via URL params (?tab=keys, ?tab=secrets, ?tab=endpoints)
- Default tab (no param) shows Own Keys
- Existing API key CRUD is fully functional within the Own Keys tab
- No visual regression in API key management interface
</verification>

<success_criteria>
- Admin can click between all three tabs without errors (TABS-01)
- Tab bar matches inbox design pattern exactly (TABS-02)
- API key create/list/revoke works within Own Keys tab (KEYS-01)
- Previously created API keys are visible and functional (KEYS-02)
</success_criteria>

<output>
After completion, create `.planning/workstreams/GOYA-REST-API/phases/09-tab-shell-own-keys-migration/09-01-SUMMARY.md`
</output>
