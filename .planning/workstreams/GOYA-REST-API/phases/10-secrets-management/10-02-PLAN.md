---
phase: 10-secrets-management
plan: "02"
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - app/admin/api-keys/SecretsTab.tsx
  - app/admin/api-keys/SecretModal.tsx
  - app/admin/api-keys/page.tsx
  - app/admin/api-keys/SecretsPlaceholder.tsx
autonomous: false
requirements: [SECR-01, SECR-02, SECR-03, SECR-04, SECR-05, SECR-06, SECR-11]

must_haves:
  truths:
    - "Admin sees secrets table with Name, Category, Description, Last Updated columns — no raw values visible"
    - "Admin can create a new secret via modal with name, value, category dropdown, and description"
    - "Admin can edit an existing secret with pre-filled name/description/category and masked value field"
    - "Admin can delete a secret after confirming a dialog"
    - "Admin can filter secrets by category (Auth, Analytics, Payments, AI, Other)"
    - "Admin can search secrets by name and results update immediately"
    - "Pre-populated placeholder entries exist for known third-party keys (Google OAuth, GA4, Clarity, Meta Pixel, Anthropic)"
  artifacts:
    - path: "app/admin/api-keys/SecretsTab.tsx"
      provides: "Secrets list table with category filter and search"
      contains: "'use client'"
    - path: "app/admin/api-keys/SecretModal.tsx"
      provides: "Create/edit modal for secrets"
      contains: "'use client'"
    - path: "app/admin/api-keys/page.tsx"
      provides: "Updated tab shell rendering SecretsTab instead of placeholder"
      contains: "SecretsTab"
  key_links:
    - from: "app/admin/api-keys/SecretsTab.tsx"
      to: "app/admin/api-keys/secrets-actions.ts"
      via: "import server actions"
      pattern: "import.*from.*secrets-actions"
    - from: "app/admin/api-keys/SecretModal.tsx"
      to: "app/admin/api-keys/secrets-actions.ts"
      via: "import createSecret/updateSecret/getSecret"
      pattern: "import.*from.*secrets-actions"
    - from: "app/admin/api-keys/page.tsx"
      to: "app/admin/api-keys/SecretsTab.tsx"
      via: "renders SecretsTab component"
      pattern: "<SecretsTab"
---

<objective>
Build the full secrets management UI: table with category filter and search, create/edit modal, delete confirmation, and pre-populated category entries. Replace SecretsPlaceholder with the real SecretsTab component.

Purpose: Delivers the admin-facing secrets management interface (SECR-01 through SECR-06, SECR-11).
Output: SecretsTab.tsx, SecretModal.tsx, updated page.tsx, deleted SecretsPlaceholder.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/GOYA-REST-API/ROADMAP.md
@.planning/workstreams/GOYA-REST-API/STATE.md
@.planning/workstreams/GOYA-REST-API/phases/10-secrets-management/10-CONTEXT.md
@.planning/workstreams/GOYA-REST-API/phases/10-secrets-management/10-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output — server actions the UI consumes -->

From app/admin/api-keys/secrets-actions.ts:
```typescript
export type SecretCategory = 'Auth' | 'Analytics' | 'Payments' | 'AI' | 'Other'
export type SecretListItem = {
  id: string
  key_name: string
  description: string
  category: SecretCategory
  updated_at: string
}

export async function listSecrets(): Promise<{ success: true; secrets: SecretListItem[] } | { success: false; error: string }>
export async function createSecret(name: string, value: string, category: SecretCategory, description: string): Promise<{ success: true } | { success: false; error: string }>
export async function getSecret(id: string): Promise<{ success: true; secret: SecretListItem & { value: string } } | { success: false; error: string }>
export async function updateSecret(id: string, fields: { key_name?: string; value?: string; category?: SecretCategory; description?: string }): Promise<{ success: true } | { success: false; error: string }>
export async function deleteSecret(id: string): Promise<{ success: true } | { success: false; error: string }>
```

From app/admin/api-keys/ApiKeysTable.tsx (UI pattern reference):
- 'use client' component with useState for local state
- Inline create form toggle pattern
- Optimistic UI updates (filter local state after action)
- Table styling: bg-white rounded-xl border border-[#E5E7EB] shadow-sm
- Header row: text-[11px] font-semibold text-[#6B7280] uppercase tracking-wider bg-slate-50
- Body rows: hover:bg-slate-50 transition-colors
- Color tokens: #1B3A5C (heading), #374151 (body text), #6B7280 (muted), #00B5A3 (accent), #E5E7EB (border)

From app/admin/api-keys/page.tsx:
- Tab shell with activeTab === 'secrets' rendering secrets component
- Currently renders <SecretsPlaceholder />
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SecretsTab and SecretModal components</name>
  <files>app/admin/api-keys/SecretsTab.tsx, app/admin/api-keys/SecretModal.tsx, app/admin/api-keys/page.tsx, app/admin/api-keys/SecretsPlaceholder.tsx</files>
  <read_first>
    - app/admin/api-keys/ApiKeysTable.tsx (UI pattern — match table styling, color tokens, interaction patterns exactly)
    - app/admin/api-keys/page.tsx (tab shell — update import from SecretsPlaceholder to SecretsTab)
    - app/admin/api-keys/secrets-actions.ts (Plan 01 output — server action signatures to call)
    - app/admin/api-keys/SecretsPlaceholder.tsx (will be deleted)
  </read_first>
  <action>
    **Create `app/admin/api-keys/SecretModal.tsx`** (per SECR-02, SECR-03):
    - 'use client' component
    - Props: `{ mode: 'create' | 'edit'; secret?: SecretListItem; onClose: () => void; onSaved: () => void }`
    - Renders a modal overlay (dark backdrop + centered white card, same pattern as other admin modals in codebase — use fixed inset-0 z-50 bg-black/50 backdrop)
    - Form fields:
      - Name: text input, required, pre-filled in edit mode
      - Value: password-type input. In create mode: required, empty. In edit mode: empty with placeholder "Leave blank to keep current value"
      - Category: select dropdown with options Auth, Analytics, Payments, AI, Other. Pre-filled in edit mode
      - Description: textarea, optional, pre-filled in edit mode
    - On edit open: call `getSecret(secret.id)` to load existing data (fetches decrypted value but does NOT pre-fill value field — value stays masked/empty per SECR-03)
    - Submit handler: calls createSecret or updateSecret depending on mode
    - Loading state during submit, error display on failure
    - On success: call onSaved() callback (which triggers re-fetch in parent)
    - Style buttons matching ApiKeysTable: primary bg-[#1B3A5C] hover:bg-[#142d47], cancel border border-[#E5E7EB]

    **Create `app/admin/api-keys/SecretsTab.tsx`** (per SECR-01, SECR-04, SECR-05, SECR-06, SECR-11):
    - 'use client' component
    - Props: `{ initialSecrets: SecretListItem[] }`
    - Local state: secrets array, searchQuery string, selectedCategory (SecretCategory | 'All'), modalState (null | {mode: 'create'} | {mode: 'edit', secret: SecretListItem}), deleteConfirmId (string | null)
    - Initialize secrets from initialSecrets prop

    **Category filter bar** (per SECR-05):
    - Row of pill buttons: All, Auth, Analytics, Payments, AI, Other
    - Active pill: bg-[#1B3A5C] text-white; inactive: border border-[#E5E7EB] text-[#374151] hover:bg-slate-50
    - Filtering is client-side on the local secrets array

    **Search input** (per SECR-06):
    - Text input with search icon, placeholder "Search by name..."
    - Filters secrets where key_name includes searchQuery (case-insensitive)
    - Combined with category filter (both active simultaneously)

    **Secrets table** (per SECR-01):
    - Match ApiKeysTable styling exactly (same table wrapper, header, row classes)
    - Columns: Name (font-medium text-[#1B3A5C]), Category (colored badge — Auth=blue, Analytics=green, Payments=amber, AI=purple, Other=slate), Description (text-[#6B7280] truncated), Last Updated (relative format), Actions
    - Category badge colors: Auth: bg-blue-100 text-blue-700, Analytics: bg-emerald-100 text-emerald-700, Payments: bg-amber-100 text-amber-700, AI: bg-purple-100 text-purple-700, Other: bg-slate-100 text-slate-700
    - Actions column: Edit button (text-[#1B3A5C]) and Delete button (text-red-600)
    - Empty state when no secrets match filters

    **"Add Secret" button:**
    - Top-right, same style as ApiKeysTable "Create Key" button
    - Opens SecretModal in create mode

    **Delete confirmation** (per SECR-04):
    - Inline confirmation within the table row or a simple confirm dialog
    - Pattern: when delete clicked, set deleteConfirmId to secret.id, show "Are you sure?" with Cancel/Delete buttons in the actions cell
    - On confirm: call deleteSecret(id), optimistically remove from local array

    **Data refresh pattern:**
    - After any mutation (create/edit/delete), call listSecrets() to refresh local state (revalidatePath handles server cache, but client state needs manual refresh)
    - This matches the page revalidation pattern from actions.ts

    **Pre-populated entries** (per SECR-11):
    - This is handled by the executor creating initial seed entries via createSecret calls AFTER the migration is applied
    - Add a comment in SecretsTab.tsx noting that seed data is expected to exist
    - Actually: create a `seedSecrets` server action in secrets-actions.ts that checks if admin_secrets is empty and if so inserts placeholder entries for:
      - Auth: GOOGLE_CLIENT_ID (desc: "Google OAuth client ID"), GOOGLE_CLIENT_SECRET (desc: "Google OAuth client secret"), APPLE_SERVICE_ID (desc: "Apple Sign-In service ID"), APPLE_PRIVATE_KEY (desc: "Apple Sign-In private key")
      - Analytics: GA4_MEASUREMENT_ID (desc: "Google Analytics 4 measurement ID"), CLARITY_PROJECT_ID (desc: "Microsoft Clarity project ID"), META_PIXEL_ID (desc: "Meta/Facebook Pixel ID")
      - AI: ANTHROPIC_API_KEY (desc: "Anthropic API key for Claude")
    - These are placeholders with empty encrypted values (encrypt('') or a placeholder string like 'REPLACE_ME')
    - The seed function should be called from page.tsx on initial render (or from SecretsTab on mount via useEffect) — prefer calling from page.tsx server component before passing data to client
    - NOTE: Payments category deliberately has no seeds — CONTEXT.md says "Note in UI that Stripe keys remain in .env"
    - Add a small info note at the bottom of the table: "Stripe keys (STRIPE_SECRET_KEY, etc.) are managed via environment variables, not this secrets manager."

    **Update `app/admin/api-keys/page.tsx`:**
    - Replace `import SecretsPlaceholder from './SecretsPlaceholder'` with `import SecretsTab from './SecretsTab'`
    - When activeTab === 'secrets': fetch secrets via listSecrets() server action (or direct Supabase query matching existing pattern), pass as initialSecrets prop to SecretsTab
    - Also call seedSecrets() before fetching list (no-op if already seeded)
    - Remove SecretsPlaceholder import

    **Delete `app/admin/api-keys/SecretsPlaceholder.tsx`** (replaced by real UI).
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "'use client'" app/admin/api-keys/SecretsTab.tsx
    - grep -q "'use client'" app/admin/api-keys/SecretModal.tsx
    - grep -q "import.*from.*secrets-actions" app/admin/api-keys/SecretsTab.tsx
    - grep -q "listSecrets\|createSecret\|deleteSecret" app/admin/api-keys/SecretsTab.tsx
    - grep -q "SecretModal" app/admin/api-keys/SecretsTab.tsx
    - grep -q "category" app/admin/api-keys/SecretsTab.tsx
    - grep -q "search" app/admin/api-keys/SecretsTab.tsx
    - grep -q "SecretsTab" app/admin/api-keys/page.tsx
    - grep -qv "SecretsPlaceholder" app/admin/api-keys/page.tsx
    - test ! -f app/admin/api-keys/SecretsPlaceholder.tsx
    - grep -q "seedSecrets\|seed" app/admin/api-keys/secrets-actions.ts
    - grep -q "Stripe" app/admin/api-keys/SecretsTab.tsx
  </acceptance_criteria>
  <done>SecretsTab renders table with category filter, search, create/edit modal, and delete confirmation. page.tsx renders SecretsTab instead of placeholder. SecretsPlaceholder.tsx is deleted. Seed data populates on first visit. Build passes with no errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification</name>
  <what-built>Complete secrets management UI in the Third Party Keys tab: table, category filter, search, create/edit modals, delete confirmation, and pre-populated placeholder entries.</what-built>
  <how-to-verify>
    1. Navigate to `/admin/api-keys?tab=secrets`
    2. Verify pre-populated placeholder entries are visible (Google OAuth, GA4, Clarity, Meta Pixel, Anthropic keys)
    3. Verify table shows Name, Category, Description, Last Updated columns — NO raw values visible
    4. Test category filter: click "Auth" pill, verify only Auth secrets shown; click "All" to reset
    5. Test search: type "google" in search box, verify only Google-related secrets shown
    6. Click "Add Secret" — verify modal opens with name, value (password input), category dropdown, description fields
    7. Create a test secret (name: "Test Key", value: "abc123", category: Other, description: "test")
    8. Verify new secret appears in table with correct category badge
    9. Click "Edit" on the test secret — verify name/category/description pre-filled, value field empty/masked
    10. Update the description and submit — verify change reflected in table
    11. Click "Delete" on the test secret — verify confirmation prompt appears
    12. Confirm delete — verify secret removed from table
    13. Verify Stripe info note is visible at bottom of table
    14. Verify Own Keys and Endpoints tabs still work (no regression)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Secrets table renders with correct columns and styling matching ApiKeysTable
- Category filter pills work client-side (no page reload)
- Search filters by name in real-time
- Create modal accepts name, value, category, description
- Edit modal pre-fills metadata, keeps value masked
- Delete shows confirmation before removing
- Pre-populated placeholder entries exist for known third-party keys
- Stripe note visible in UI
- Build passes with no errors
- No regression on Own Keys or Endpoints tabs
</verification>

<success_criteria>
- Admin can view, create, edit, delete, filter, and search secrets from the Third Party Keys tab
- Raw secret values are never visible in the table list
- Category filter and search work simultaneously
- Pre-populated entries guide admin to known integration keys
- All SECR-01 through SECR-06 and SECR-11 requirements are satisfied
</success_criteria>

<output>
After completion, create `.planning/workstreams/GOYA-REST-API/phases/10-secrets-management/10-02-SUMMARY.md`
</output>
