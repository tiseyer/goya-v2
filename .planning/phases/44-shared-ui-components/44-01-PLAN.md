---
phase: 44-shared-ui-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - app/dashboard/components/HorizontalCarousel.tsx
  - app/dashboard/components/DashboardGreeting.tsx
  - app/dashboard/components/PrimaryActionCard.tsx
  - app/dashboard/components/ProfileCompletionCard.tsx
  - app/dashboard/components/StatHero.tsx
autonomous: true
requirements: [INFRA-04, COMP-01, COMP-02, COMP-03, COMP-04, DES-01, DES-02]

must_haves:
  truths:
    - "HorizontalCarousel scrolls horizontally with snap on mobile and drag on desktop, with hidden scrollbar"
    - "DashboardGreeting shows time-based salutation, first name, and role badge with role CSS variable color"
    - "ProfileCompletionCard renders progress bar, checklist with deep links, and hides at 100%"
    - "StatHero shows large metric number or dash for null values"
    - "PrimaryActionCard displays headline, value line, and CTA button"
  artifacts:
    - path: "app/dashboard/components/HorizontalCarousel.tsx"
      provides: "Horizontal carousel with embla-carousel-react for desktop drag, CSS snap for mobile"
      exports: ["HorizontalCarousel"]
    - path: "app/dashboard/components/DashboardGreeting.tsx"
      provides: "Time-of-day greeting with role badge"
      exports: ["DashboardGreeting"]
    - path: "app/dashboard/components/ProfileCompletionCard.tsx"
      provides: "Profile completion card with progress bar and checklist"
      exports: ["ProfileCompletionCard"]
    - path: "app/dashboard/components/StatHero.tsx"
      provides: "Single metric display tile"
      exports: ["StatHero"]
    - path: "app/dashboard/components/PrimaryActionCard.tsx"
      provides: "CTA card with headline and value line"
      exports: ["PrimaryActionCard"]
  key_links:
    - from: "app/dashboard/components/HorizontalCarousel.tsx"
      to: "embla-carousel-react"
      via: "useEmblaCarousel hook"
      pattern: "useEmblaCarousel"
    - from: "app/dashboard/components/ProfileCompletionCard.tsx"
      to: "lib/dashboard/profileCompletion.ts"
      via: "ProfileCompletionResult type import"
      pattern: "ProfileCompletionResult"
    - from: "app/globals.css"
      to: "HorizontalCarousel.tsx"
      via: "@utility no-scrollbar class"
      pattern: "no-scrollbar"
---

<objective>
Create the infrastructure-level shared components for the dashboard redesign: HorizontalCarousel (with embla-carousel-react for desktop drag and CSS snap for mobile), DashboardGreeting, PrimaryActionCard, ProfileCompletionCard, and StatHero. Install embla-carousel-react and add the @utility no-scrollbar rule to globals.css.

Purpose: These components are consumed by all four role layouts in Phases 45-46. Building them first as isolated, reusable pieces avoids duplication and ensures visual consistency.
Output: 5 new component files + globals.css update + embla-carousel-react installed
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-shared-ui-components/44-CONTEXT.md
@.planning/research/STACK.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From app/dashboard/components/types.ts:
```typescript
export interface DashboardProfile {
  id: string
  full_name: string | null
  username: string | null
  avatar_url: string | null
  role: string | null
  bio: string | null
  location: string | null
  website: string | null
  instagram: string | null
  youtube: string | null
  teaching_styles: string[] | null
  principal_trainer_school_id: string | null
  member_type: string | null
}
```

From lib/dashboard/profileCompletion.ts:
```typescript
export type ProfileCompletionResult = {
  score: number
  missing: Array<{ key: string; label: string; href: string }>
  complete: string[]
}
```

From app/dashboard/components/utils.tsx:
```typescript
export function getTimeOfDay(): string  // returns 'morning' | 'afternoon' | 'evening'
```

From app/components/ui/Card.tsx:
```typescript
export type CardVariant = 'default' | 'elevated' | 'flat' | 'outlined';
export default function Card({ variant, padding, className, children, ...props }: CardProps): JSX.Element
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install embla-carousel-react + add @utility no-scrollbar + create HorizontalCarousel and DashboardGreeting</name>
  <files>app/globals.css, app/dashboard/components/HorizontalCarousel.tsx, app/dashboard/components/DashboardGreeting.tsx</files>
  <read_first>
    - app/globals.css (to find insertion point for @utility)
    - app/dashboard/components/utils.tsx (getTimeOfDay helper)
    - app/dashboard/components/types.ts (DashboardProfile type)
    - app/components/ui/Card.tsx (Card component pattern)
    - .planning/research/STACK.md (embla setup instructions, @utility syntax)
  </read_first>
  <action>
    1. Run `npm install embla-carousel-react` to install embla + its peer dep (per STACK.md, pin ^8).

    2. Add `@utility no-scrollbar` to app/globals.css at the END of the file (after all existing rules). Use the exact Tailwind CSS 4 syntax per STATE.md decision:
       ```css
       @utility no-scrollbar {
         scrollbar-width: none;
         -ms-overflow-style: none;
         &::-webkit-scrollbar { display: none; }
       }
       ```
       Do NOT install tailwind-scrollbar-hide — it is broken under Tailwind v4 (per STATE.md decision).

    3. Create `app/dashboard/components/HorizontalCarousel.tsx` as a 'use client' component (per INFRA-04):
       - Props: `title: string`, `showAllHref?: string`, `showAllLabel?: string` (default "Show all"), `children: ReactNode`, `emptyState?: ReactNode`, `loading?: boolean`, `className?: string`
       - Header row: `title` as text-2xl font-bold text-slate-900, and "Show all →" link (text-sm text-[var(--goya-primary)] hover:underline) right-aligned when showAllHref provided (per DES-03)
       - Track: a div with classes `flex overflow-x-auto snap-x snap-mandatory gap-4 no-scrollbar scroll-smooth pb-2`
       - Use `useEmblaCarousel` hook from embla-carousel-react on the track ref for desktop drag-to-scroll. Options: `{ dragFree: true, containScroll: 'trimSnaps' }`. The embla ref goes on the overflow container.
       - NO arrow buttons (per CONTEXT.md decision: "No carousel arrow buttons — swipe/scroll only")
       - When `loading` is true, render 3 skeleton placeholder cards: `w-[280px] h-48 bg-slate-100 rounded-2xl animate-pulse shrink-0 snap-start`
       - When `children` is falsy/empty AND `emptyState` is provided, render emptyState full-width instead of the track
       - Section wrapper: `space-y-4` for gap between header and track (per DES-01 Apple aesthetic)

    4. Create `app/dashboard/components/DashboardGreeting.tsx` as a 'use client' component (per COMP-01):
       - Props: `firstName: string`, `role: string`, `subtitle?: string`
       - Import and use `getTimeOfDay()` from `./utils`
       - Render: "Good {timeOfDay}, {firstName}." as text-2xl font-bold text-slate-900 (per DES-01)
       - Below the greeting, if subtitle provided, render it as text-base text-slate-500
       - Role badge: a pill span with text-xs font-medium px-2.5 py-0.5 rounded-full. Use the `roleBadgeColor` mapping:
         - student: bg-blue-100 text-blue-700
         - teacher: bg-emerald-100 text-emerald-700
         - wellness_practitioner: bg-purple-100 text-purple-700
         - school: bg-amber-100 text-amber-700
         - default: bg-slate-100 text-slate-700
       - Role label text: capitalize role, replace underscores with spaces ("wellness_practitioner" -> "Wellness Practitioner")
       - Layout: flex items-center gap-3 for greeting text + badge on same line
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "embla-carousel-react" package.json
    - grep -q "@utility no-scrollbar" app/globals.css
    - grep -q "useEmblaCarousel" app/dashboard/components/HorizontalCarousel.tsx
    - grep -q "snap-x" app/dashboard/components/HorizontalCarousel.tsx
    - grep -q "getTimeOfDay" app/dashboard/components/DashboardGreeting.tsx
    - grep -q "rounded-full" app/dashboard/components/DashboardGreeting.tsx
  </acceptance_criteria>
  <done>embla-carousel-react installed, @utility no-scrollbar in globals.css, HorizontalCarousel renders with embla drag + CSS snap + skeleton loading + empty state, DashboardGreeting renders time-based salutation with role badge pill</done>
</task>

<task type="auto">
  <name>Task 2: Create PrimaryActionCard, ProfileCompletionCard, and StatHero</name>
  <files>app/dashboard/components/PrimaryActionCard.tsx, app/dashboard/components/ProfileCompletionCard.tsx, app/dashboard/components/StatHero.tsx</files>
  <read_first>
    - app/components/ui/Card.tsx (Card variant/padding pattern)
    - lib/dashboard/profileCompletion.ts (ProfileCompletionResult type, WEIGHTED_FIELDS)
    - app/dashboard/components/types.ts (DashboardProfile)
    - .planning/phases/44-shared-ui-components/44-CONTEXT.md (design specs)
  </read_first>
  <action>
    1. Create `app/dashboard/components/PrimaryActionCard.tsx` (per COMP-04):
       - Props: `headline: string`, `description?: string`, `ctaLabel: string`, `ctaHref: string`, `className?: string`
       - Uses the existing Card component from `@/app/components/ui/Card` with variant="default" padding="lg"
       - headline: text-lg font-bold text-slate-900
       - description (value line, Hormozi principle): text-sm text-slate-500 mt-1
       - CTA button: an `<a>` styled as block mt-4 text-center py-2.5 px-4 rounded-xl bg-[var(--goya-primary)] text-white font-medium text-sm hover:opacity-90 transition-opacity
       - Full-width by default, can be constrained by parent grid

    2. Create `app/dashboard/components/ProfileCompletionCard.tsx` (per COMP-02):
       - Props: `completion: ProfileCompletionResult`, `className?: string`
       - Do NOT render the card at all if completion.score >= 100 (return null)
       - Uses Card from `@/app/components/ui/Card` with variant="outlined" padding="lg"
       - Headline: "Complete your profile" as text-lg font-bold text-slate-900
       - Progress bar container: h-1.5 w-full rounded-full bg-slate-100 mt-3
       - Progress bar fill: h-1.5 rounded-full bg-[var(--goya-primary)] transition-[width] duration-500 ease-out, width set via inline style `{{ width: `${completion.score}%` }}`
       - Percentage label: text-sm font-medium text-slate-500 mt-1, showing "{score}% complete"
       - Checklist: ul with mt-4 space-y-2. Each item in completion.missing rendered as:
         - `<li>` with `<a href={item.href}>` styled as flex items-center gap-2 text-sm text-[var(--goya-primary)] hover:underline
         - Prefix each with a circle-outline icon (use Lucide `Circle` icon, size 14, text-slate-300)
         - Text: item.label (e.g., "Profile photo", "Bio", "Location")
       - Import `Circle` from lucide-react for the checklist icon

    3. Create `app/dashboard/components/StatHero.tsx` (per COMP-03):
       - Props: `label: string`, `value: number | null | undefined`, `suffix?: string`, `className?: string`
       - Renders a single metric tile. Use a plain div (not Card component) with classes: text-center p-4
       - Value display: text-4xl font-bold text-slate-900. When value is null or undefined, display "\u2014" (em dash). Never display "0" for null — only display 0 when value is explicitly 0 (per CONTEXT.md decision: StatHero static, no count-up).
       - If suffix provided and value is not null, append suffix after the number (e.g., "12%")
       - Label: text-sm text-slate-500 mt-1
       - Export a companion `StatHeroGrid` component that wraps children in `grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-4`
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ctaHref" app/dashboard/components/PrimaryActionCard.tsx
    - grep -q "completion.score" app/dashboard/components/ProfileCompletionCard.tsx
    - grep -q "h-1.5" app/dashboard/components/ProfileCompletionCard.tsx
    - grep -q "completion.missing" app/dashboard/components/ProfileCompletionCard.tsx
    - grep -rq '\\u2014\|"\u2014"\|em dash\|"\—"' app/dashboard/components/StatHero.tsx || grep -q '"\u2014"' app/dashboard/components/StatHero.tsx
    - grep -q "text-4xl" app/dashboard/components/StatHero.tsx
    - grep -q "StatHeroGrid" app/dashboard/components/StatHero.tsx
  </acceptance_criteria>
  <done>PrimaryActionCard renders headline + value description + CTA link, ProfileCompletionCard shows progress bar (h-1.5) + checklist with deep links + returns null at 100%, StatHero displays text-4xl metric with em dash for null + StatHeroGrid layout helper</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` completes without module-not-found errors
3. All 5 component files exist in app/dashboard/components/
4. embla-carousel-react appears in package.json dependencies
5. @utility no-scrollbar exists in app/globals.css
</verification>

<success_criteria>
- HorizontalCarousel uses embla-carousel-react for desktop drag + CSS snap-x for mobile + no-scrollbar utility + skeleton loading state + empty state + "Show all" link
- DashboardGreeting renders time-of-day greeting with first name and colored role badge pill
- PrimaryActionCard renders headline, optional description, and CTA link button
- ProfileCompletionCard renders progress bar (h-1.5), percentage, checklist with deep links, and returns null when score is 100
- StatHero shows text-4xl number with em dash for null, plus StatHeroGrid grid helper
- tsc --noEmit passes, npm run build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/44-shared-ui-components/44-01-SUMMARY.md`
</output>
