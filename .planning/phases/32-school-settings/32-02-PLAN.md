---
phase: 32-school-settings
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - app/schools/[slug]/settings/page.tsx
  - app/schools/[slug]/settings/online-presence/page.tsx
  - app/schools/[slug]/settings/teaching/page.tsx
  - app/schools/[slug]/settings/location/page.tsx
autonomous: true
requirements: [SET-03, SET-04, SET-05, SET-06]

must_haves:
  truths:
    - "General page shows editable name, slug, short bio, full bio, and established year pre-filled from database"
    - "Changing name or slug on General page shows a warning about triggering re-review"
    - "Online Presence page edits website, social links, and video intro"
    - "Teaching Info page edits practice styles, programs, delivery format, lineage, and languages"
    - "Location page uses Google Places autocomplete to set address"
    - "Each page has an explicit Save button that calls the corresponding server action"
  artifacts:
    - path: "app/schools/[slug]/settings/page.tsx"
      provides: "General settings section"
      min_lines: 50
    - path: "app/schools/[slug]/settings/online-presence/page.tsx"
      provides: "Online Presence settings section"
      min_lines: 40
    - path: "app/schools/[slug]/settings/teaching/page.tsx"
      provides: "Teaching Info settings section"
      min_lines: 50
    - path: "app/schools/[slug]/settings/location/page.tsx"
      provides: "Location settings section with Google Places"
      min_lines: 40
  key_links:
    - from: "app/schools/[slug]/settings/page.tsx"
      to: "actions.ts updateGeneral"
      via: "import and call on form submit"
      pattern: "updateGeneral"
    - from: "app/schools/[slug]/settings/online-presence/page.tsx"
      to: "actions.ts updateOnlinePresence"
      via: "import and call on form submit"
      pattern: "updateOnlinePresence"
    - from: "app/schools/[slug]/settings/teaching/page.tsx"
      to: "actions.ts updateTeachingInfo"
      via: "import and call on form submit"
      pattern: "updateTeachingInfo"
    - from: "app/schools/[slug]/settings/location/page.tsx"
      to: "actions.ts updateLocation"
      via: "import and call on form submit"
      pattern: "updateLocation"
---

<objective>
Build the first 4 settings section pages: General, Online Presence, Teaching Info, and Location. Each page loads school data, pre-fills forms, and saves via server actions from Plan 01.

Purpose: These are the core informational sections that school owners edit most frequently.
Output: 4 working settings pages with save functionality.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/32-school-settings/32-CONTEXT.md
@.planning/phases/32-school-settings/32-01-SUMMARY.md

@app/schools/[slug]/onboarding/OnboardingWizard.tsx
@app/schools/[id]/settings/SchoolSettingsClient.tsx

<interfaces>
<!-- Server actions from Plan 01 -->
From app/schools/[slug]/settings/actions.ts (created in 32-01):
```typescript
export async function updateGeneral(schoolSlug: string, data: {
  name: string; slug: string; short_bio: string; bio: string; established_year: number;
}): Promise<{ success: true } | { error: string }>;

export async function updateOnlinePresence(schoolSlug: string, data: {
  website?: string; instagram?: string; facebook?: string; tiktok?: string; youtube?: string;
  video_platform?: 'youtube' | 'vimeo'; video_url?: string;
}): Promise<{ success: true } | { error: string }>;

export async function updateTeachingInfo(schoolSlug: string, data: {
  practice_styles: string[]; programs_offered: string[]; course_delivery_format: 'in_person' | 'online' | 'hybrid';
  lineage: string; languages: string[];
}): Promise<{ success: true } | { error: string }>;

export async function updateLocation(schoolSlug: string, data: {
  location_address: string; location_city: string; location_country: string;
  location_lat: number; location_lng: number; location_place_id: string;
}): Promise<{ success: true } | { error: string }>;
```

From app/schools/[slug]/settings/layout.tsx (created in 32-01):
- Layout fetches full school data and passes to children
- Each page receives school data via server component query (params.slug available)

From app/schools/[slug]/onboarding/OnboardingWizard.tsx:
- Google Places autocomplete pattern using dynamic script loading in useEffect
- Practice styles, programs, languages as tag/chip selectors
- Lineage as comma-separated string input rendered as tag chips
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: General and Online Presence settings pages</name>
  <files>
    app/schools/[slug]/settings/page.tsx,
    app/schools/[slug]/settings/online-presence/page.tsx
  </files>
  <read_first>
    app/schools/[id]/settings/SchoolSettingsClient.tsx,
    app/schools/[slug]/settings/actions.ts,
    app/schools/[slug]/settings/layout.tsx,
    app/schools/[slug]/onboarding/OnboardingWizard.tsx
  </read_first>
  <acceptance_criteria>
    - General page: server component that fetches school, renders client form with name, slug, short_bio, bio, established_year
    - Re-review warning shown when name or slug field value differs from original
    - Online Presence page: client form with website, instagram, facebook, tiktok, youtube, video_platform, video_url
    - Both pages use explicit "Save" button calling the respective server action
    - Toast feedback on success/error
    - Styling matches existing SchoolSettingsClient card pattern (white bg, rounded-xl, border, shadow-sm)
  </acceptance_criteria>
  <action>
    **General page (page.tsx) — SET-03:**

    Structure: Server component wrapper + client component.

    Server component (page.tsx):
    - Await params for slug (Next.js 15 async params)
    - Fetch school by slug using createSupabaseServerClient: `supabase.from('schools').select('*').eq('slug', slug).single()`
    - If no school, redirect to /dashboard
    - Render GeneralSettingsClient with school data

    Client component (can be inline or separate file — prefer inline for simplicity):
    - State for: name, slug, short_bio, bio, established_year (pre-filled from school prop)
    - Track original name and slug to detect changes for re-review warning
    - If name !== originalName || slug !== originalSlug: show amber warning box: "Changing the school name or URL slug will trigger a re-review of your school."
    - Slug field: text input with URL preview (goya.community/schools/[slug]), auto-generate from name on change (using same slugify util from registration), allow manual edit
    - Short bio: textarea with char counter (max 250)
    - Bio: textarea with char counter (min 1000, max 5000)
    - Established year: number input (1900 to current year)
    - Save button calls updateGeneral server action
    - Show success toast or error message on result
    - Use same card styling as SchoolSettingsClient: white bg, rounded-xl border, px-6 py-5

    **Online Presence page (online-presence/page.tsx) — SET-04:**

    Same server/client split pattern.

    Client form fields:
    - Website URL input
    - Instagram URL input
    - Facebook URL input
    - TikTok URL input
    - YouTube URL input
    - Video platform toggle (YouTube/Vimeo) + video URL input (optional section)
    - Save button calls updateOnlinePresence server action
    - Toast feedback
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    General page edits name, slug, short bio, bio, established year with re-review warning. Online Presence page edits website, social links, and video intro. Both save via server actions with toast feedback.
  </done>
</task>

<task type="auto">
  <name>Task 2: Teaching Info and Location settings pages</name>
  <files>
    app/schools/[slug]/settings/teaching/page.tsx,
    app/schools/[slug]/settings/location/page.tsx
  </files>
  <read_first>
    app/schools/[slug]/onboarding/OnboardingWizard.tsx,
    app/schools/[slug]/settings/actions.ts
  </read_first>
  <acceptance_criteria>
    - Teaching page: practice styles multi-select (max 5), programs multi-select, delivery format radio, lineage text, languages multi-select (max 3)
    - Location page: Google Places autocomplete input that populates address, city, country, lat, lng, place_id
    - Both save via server actions with toast feedback
    - Styling consistent with General and Online Presence pages
  </acceptance_criteria>
  <action>
    **Teaching Info page (teaching/page.tsx) — SET-05:**

    Server/client split. Fetch school by slug for pre-fill.

    Client form fields (reuse patterns from OnboardingWizard.tsx):
    - Practice styles: checkbox/chip multi-select from predefined list (Ashtanga, Vinyasa, Hatha, Yin, Kundalini, Restorative, Power, Bikram/Hot, Iyengar, Jivamukti, Anusara, Forrest, Sivananda, Other). Max 5 selections.
    - Programs offered: checkbox/chip multi-select (Teacher Training 200hr, Teacher Training 300hr, Teacher Training 500hr, Workshops, Retreats, Kids Yoga, Prenatal, Therapeutic, Corporate, Online Courses, Other)
    - Course delivery format: radio buttons (In Person, Online, Hybrid)
    - Lineage: text input (stored as comma-separated string, display as tag chips like onboarding)
    - Languages: chip multi-select (English, Spanish, French, German, Portuguese, Hindi, Mandarin, Japanese, Other). Max 3.
    - Save button calls updateTeachingInfo
    - Toast feedback

    **Location page (location/page.tsx) — SET-06:**

    Server/client split. Fetch school by slug for pre-fill.

    Client component:
    - Google Places autocomplete input (reuse the same dynamic loading pattern from OnboardingWizard):
      1. Load Google Maps Places script dynamically in useEffect using NEXT_PUBLIC_GOOGLE_MAPS_API_KEY
      2. Initialize Autocomplete on the input element
      3. On place_changed: extract address_components, geometry.location, place_id
      4. Map to: location_address (formatted_address), location_city, location_country, location_lat, location_lng, location_place_id
    - Show current location if already set (formatted address display)
    - Allow clearing location
    - Save button calls updateLocation
    - Toast feedback
    - Type window as any to avoid global Window conflicts (same pattern as Phase 31 decision)
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    Teaching Info page has multi-select chips for styles/programs/languages, radio for delivery format, and lineage input. Location page has working Google Places autocomplete. Both save via server actions.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- All 4 pages exist and import their respective server actions
- General page has re-review warning when name/slug changes
- Location page loads Google Places API dynamically
</verification>

<success_criteria>
- General settings page edits name, slug, bio fields with re-review warning on name/slug change
- Online Presence page edits website, social links, and video
- Teaching Info page edits styles, programs, format, lineage, languages with chip selectors
- Location page uses Google Places autocomplete
- All pages save via server actions and show toast feedback
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/32-school-settings/32-02-SUMMARY.md`
</output>
