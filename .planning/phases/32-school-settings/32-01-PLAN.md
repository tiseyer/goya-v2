---
phase: 32-school-settings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/Header.tsx
  - app/schools/[slug]/settings/layout.tsx
  - app/schools/[slug]/settings/components/SchoolSettingsShell.tsx
  - app/schools/[slug]/settings/actions.ts
autonomous: true
requirements: [SET-01, SET-02, SET-11]

must_haves:
  truths:
    - "School owners see 'School Settings' link in the header dropdown between Settings and Admin Settings"
    - "Non-owners and non-teachers do NOT see a School Settings link"
    - "Visiting /schools/[slug]/settings shows a collapsible sidebar shell with 8 navigation sections"
    - "A pending_review status banner appears at the top of the settings area"
    - "Server actions exist for updating each school settings section with owner auth"
  artifacts:
    - path: "app/schools/[slug]/settings/components/SchoolSettingsShell.tsx"
      provides: "Collapsible sidebar shell matching SettingsShell pattern"
      min_lines: 100
    - path: "app/schools/[slug]/settings/layout.tsx"
      provides: "Server layout with auth + owner guard"
      exports: ["default"]
    - path: "app/schools/[slug]/settings/actions.ts"
      provides: "Server actions for all settings sections"
      exports: ["updateGeneral", "updateOnlinePresence", "updateTeachingInfo", "updateLocation"]
  key_links:
    - from: "app/components/Header.tsx"
      to: "/schools/[slug]/settings"
      via: "Link with school slug from profiles query"
      pattern: "schools.*slug.*settings"
    - from: "app/schools/[slug]/settings/layout.tsx"
      to: "SchoolSettingsShell"
      via: "import and render with school data"
      pattern: "SchoolSettingsShell"
---

<objective>
Create the school settings infrastructure: header dropdown link, settings shell with collapsible sidebar, layout with auth guard, and server actions for all settings sections.

Purpose: Establishes the navigation entry point and structural foundation that all settings section pages (Plans 02 and 03) build upon.
Output: Working shell at /schools/[slug]/settings with sidebar, status banner, and server actions ready for consumption.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-school-settings/32-CONTEXT.md

@app/settings/components/SettingsShell.tsx
@app/settings/layout.tsx
@app/components/Header.tsx
@app/schools/[slug]/onboarding/actions.ts
@app/schools/[id]/settings/page.tsx
@app/schools/[id]/settings/SchoolSettingsClient.tsx

<interfaces>
<!-- Existing SettingsShell pattern to replicate -->
From app/settings/components/SettingsShell.tsx:
- NavItem interface: { href, label, paths (SVG path data), roles? }
- Collapsible sidebar with localStorage persistence ('settings-sidebar-collapsed')
- Uses usePathname() for active state detection
- Sidebar width: 248px expanded, 64px collapsed
- Classes: sticky top-16 h-[calc(100vh-4rem)] bg-white border-r

From app/settings/layout.tsx:
- Server component with createSupabaseServerClient
- Auth check (redirect to /sign-in)
- Profile role fetch
- Passes userRole to shell

From app/components/Header.tsx (lines 826-828):
- Currently fetches school by owner_id: `supabase.from('schools').select('id').eq('owner_id', data.user!.id)`
- Uses schoolId state for link href: `/schools/${schoolId}/settings`
- Needs to change to fetch slug instead of id

From app/schools/[slug]/onboarding/actions.ts:
- getOwnedSchool helper pattern: accepts slug, validates owner, returns { school, userId } | { error }
- Uses createSupabaseServerActionClient() and (supabase as any) casts for new tables
- allowCompleted parameter controls whether completed schools can be accessed
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Header dropdown link + SchoolSettingsShell + layout</name>
  <files>
    app/components/Header.tsx,
    app/schools/[slug]/settings/components/SchoolSettingsShell.tsx,
    app/schools/[slug]/settings/layout.tsx
  </files>
  <read_first>
    app/settings/components/SettingsShell.tsx,
    app/settings/layout.tsx,
    app/components/Header.tsx
  </read_first>
  <acceptance_criteria>
    - Header fetches school slug (not id) and links to /schools/[slug]/settings
    - SchoolSettingsShell has 8 nav items matching SettingsShell pattern exactly
    - Layout authenticates user, verifies school ownership, passes school data to shell
    - Status banner shown when school.status === 'pending_review'
  </acceptance_criteria>
  <action>
    **Header.tsx changes (per SET-01):**
    1. Change the school query from `select('id')` to `select('slug')` at lines ~827 and ~843
    2. Rename state from `schoolId` to `schoolSlug` (or keep schoolId and just change query — simpler: change to `select('id, slug')` and store slug)
    3. Update the Link href from `/schools/${schoolId}/settings` to `/schools/${schoolSlug}/settings`
    4. The "School Settings" link is already conditionally rendered for teachers with a school — this is correct per SET-01

    **SchoolSettingsShell.tsx (per SET-02):**
    Create as a 'use client' component matching SettingsShell.tsx exactly in structure:
    - NAV_ITEMS array with 8 entries, each with href, label, and SVG paths:
      1. General — `/schools/[slug]/settings` (gear icon, same as SettingsShell General)
      2. Online Presence — `/schools/[slug]/settings/online-presence` (globe icon)
      3. Teaching Info — `/schools/[slug]/settings/teaching` (academic cap icon)
      4. Location — `/schools/[slug]/settings/location` (location marker icon)
      5. Faculty — `/schools/[slug]/settings/faculty` (users icon)
      6. Designations — `/schools/[slug]/settings/designations` (badge/shield icon)
      7. Documents — `/schools/[slug]/settings/documents` (document icon)
      8. Subscription — `/schools/[slug]/settings/subscription` (credit card icon)
    - Accept props: `{ children, schoolSlug, schoolStatus }` — schoolSlug used to construct hrefs dynamically
    - Collapsible sidebar with localStorage key 'school-settings-sidebar-collapsed'
    - Sidebar header shows "School Settings" text when expanded
    - Active state detection via usePathname() — exact match for General, startsWith for others
    - Same styling, dimensions, and transitions as SettingsShell

    **Status banner (per SET-11):**
    Inside SchoolSettingsShell, above {children} in the main content area:
    - If schoolStatus === 'pending_review': render amber/yellow banner with text "Your school is currently under review. This can take up to 1 week."
    - Style matching the existing submission banner pattern in SchoolSettingsClient.tsx (amber-50 bg, amber-200 border, rounded-xl)

    **layout.tsx:**
    Server component following admin/layout.tsx and settings/layout.tsx patterns:
    1. createSupabaseServerClient() for auth
    2. Get user, redirect to /sign-in if not authenticated
    3. Await params to get slug (Next.js 15 async params)
    4. Query school by slug: `supabase.from('schools').select('*').eq('slug', slug).single()`
    5. Query profile for role check
    6. Guard: if school.owner_id !== user.id AND role is not admin/moderator, redirect to /dashboard
    7. Render `<SchoolSettingsShell schoolSlug={slug} schoolStatus={school.status}>{children}</SchoolSettingsShell>`
    8. Pass full school data as a prop so child pages can access it (or use a separate query per page — prefer passing school to avoid N+1)

    Note: The old `app/schools/[id]/settings/` directory can remain for now — it uses a different route param ([id] vs [slug]) so there is no conflict.
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    Header links to /schools/[slug]/settings using slug. SchoolSettingsShell renders 8-item collapsible sidebar. Layout guards auth + ownership. Status banner visible for pending_review schools.
  </done>
</task>

<task type="auto">
  <name>Task 2: Server actions for all settings sections</name>
  <files>
    app/schools/[slug]/settings/actions.ts
  </files>
  <read_first>
    app/schools/[slug]/onboarding/actions.ts
  </read_first>
  <acceptance_criteria>
    - 'use server' file with actions for: updateGeneral, updateOnlinePresence, updateTeachingInfo, updateLocation
    - Each action authenticates user and verifies school ownership via slug
    - updateGeneral sets status to 'pending_review' when name or slug changes
    - All actions return { success: true } | { error: string }
    - Validation matches onboarding patterns (char limits, required fields)
  </acceptance_criteria>
  <action>
    Create app/schools/[slug]/settings/actions.ts as a 'use server' module.

    **Helper — reuse getOwnedSchool pattern from onboarding/actions.ts:**
    Copy the getOwnedSchool function but ALWAYS set allowCompleted=true (settings are post-onboarding). Also select more fields: id, owner_id, name, slug, status.

    **updateGeneral(schoolSlug, data: { name, slug, short_bio, bio, established_year }):**
    - Validate: name 3+ chars, short_bio required and <= 250 chars, bio >= 1000 and <= 5000 chars, established_year 1900-current
    - If name !== school.name OR slug !== school.slug: set status = 'pending_review' in the update (per SET-03 re-review trigger)
    - If slug is being changed: validate slug format (lowercase, alphanumeric + hyphens), check uniqueness via separate query
    - Update schools table with all fields + updated_at

    **updateOnlinePresence(schoolSlug, data: { website?, instagram?, facebook?, tiktok?, youtube?, video_platform?, video_url? }):**
    - At least one field required (same validation as onboarding)
    - If video_platform set, video_url required
    - Update schools table

    **updateTeachingInfo(schoolSlug, data: { practice_styles[], programs_offered[], course_delivery_format, lineage, languages[] }):**
    - course_delivery_format required
    - practice_styles max 5, languages max 3
    - Update schools table

    **updateLocation(schoolSlug, data: { location_address, location_city, location_country, location_lat, location_lng, location_place_id }):**
    - Same validation as onboarding saveLocation
    - Update schools table

    All actions use createSupabaseServerActionClient() and (supabase as any) cast pattern from onboarding/actions.ts.
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    Server actions file exports updateGeneral (with re-review logic), updateOnlinePresence, updateTeachingInfo, updateLocation. All authenticate, validate, and return typed results.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- SchoolSettingsShell.tsx exists with 8 nav items
- layout.tsx has auth guard and ownership check
- actions.ts exports 4 update functions
- Header.tsx fetches slug and links to /schools/[slug]/settings
</verification>

<success_criteria>
- School owners see "School Settings" in header dropdown linking to /schools/[slug]/settings
- The settings shell renders with a collapsible 8-item sidebar
- A pending_review status banner is visible when applicable
- Server actions are ready for consumption by section pages
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/32-school-settings/32-01-SUMMARY.md`
</output>
