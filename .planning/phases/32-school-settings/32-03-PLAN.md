---
phase: 32-school-settings
plan: 03
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - app/schools/[slug]/settings/faculty/page.tsx
  - app/schools/[slug]/settings/designations/page.tsx
  - app/schools/[slug]/settings/documents/page.tsx
  - app/schools/[slug]/settings/subscription/page.tsx
autonomous: true
requirements: [SET-07, SET-08, SET-09, SET-10]

must_haves:
  truths:
    - "Faculty page lists current faculty members with positions and allows adding/removing members"
    - "Designations page shows active designations with status badges"
    - "Documents page shows uploaded verification documents per designation with re-upload capability"
    - "Subscription page shows billing status and links to Stripe Customer Portal"
  artifacts:
    - path: "app/schools/[slug]/settings/faculty/page.tsx"
      provides: "Faculty management page"
      min_lines: 50
    - path: "app/schools/[slug]/settings/designations/page.tsx"
      provides: "Designations view page"
      min_lines: 30
    - path: "app/schools/[slug]/settings/documents/page.tsx"
      provides: "Documents management page"
      min_lines: 50
    - path: "app/schools/[slug]/settings/subscription/page.tsx"
      provides: "Subscription status page"
      min_lines: 30
  key_links:
    - from: "app/schools/[slug]/settings/faculty/page.tsx"
      to: "onboarding/actions.ts"
      via: "saveFacultyMember, removeFacultyMember, inviteFacultyByEmail"
      pattern: "saveFacultyMember|removeFacultyMember"
    - from: "app/schools/[slug]/settings/documents/page.tsx"
      to: "onboarding/actions.ts"
      via: "uploadDocument, deleteDocument"
      pattern: "uploadDocument|deleteDocument"
    - from: "app/schools/[slug]/settings/subscription/page.tsx"
      to: "Stripe Customer Portal"
      via: "billingPortal link or server action"
      pattern: "billing.*portal|stripe"
---

<objective>
Build the remaining 4 settings section pages: Faculty, Designations, Documents, and Subscription. Faculty and Documents reuse onboarding server actions. Designations is read-only view. Subscription shows billing status with Stripe portal link.

Purpose: Completes the full settings area with all 8 sections functional.
Output: 4 working settings pages completing the school settings feature.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/32-school-settings/32-CONTEXT.md
@.planning/phases/32-school-settings/32-01-SUMMARY.md

@app/schools/[slug]/onboarding/actions.ts
@app/schools/[slug]/onboarding/OnboardingWizard.tsx
@app/settings/subscriptions/PortalButton.tsx
@app/settings/subscriptions/page.tsx

<interfaces>
<!-- Onboarding actions to reuse for Faculty and Documents -->
From app/schools/[slug]/onboarding/actions.ts:
```typescript
// Faculty
export async function saveFacultyMember(schoolSlug: string, data: { profile_id: string; position: string }): Promise<{ success: true } | { error: string }>;
export async function removeFacultyMember(schoolSlug: string, facultyId: string): Promise<{ success: true } | { error: string }>;
export async function inviteFacultyByEmail(schoolSlug: string, data: { email: string; position: string }): Promise<{ success: true } | { error: string }>;

// Documents
export async function uploadDocument(formData: FormData): Promise<{ success: true; document: { id: string; file_name: string; file_url: string } } | { error: string }>;
export async function deleteDocument(schoolSlug: string, documentId: string): Promise<{ success: true } | { error: string }>;
```

IMPORTANT: These onboarding actions use getOwnedSchool with allowCompleted=false by default. The settings pages need post-onboarding access. Two options:
1. Modify getOwnedSchool in onboarding/actions.ts to default allowCompleted=true (risky — affects onboarding)
2. Create wrapper actions in settings/actions.ts that duplicate the pattern with allowCompleted=true
3. Best: Add new settings-specific faculty/document actions in settings/actions.ts that use the same DB operations but always allow completed schools

From app/settings/subscriptions/PortalButton.tsx:
- Pattern for Stripe Customer Portal redirect button
- Likely uses a server action to create portal session URL

Database tables:
- school_faculty: id, school_id, profile_id, invited_email, invite_token, position, status, is_principal_trainer
- school_designations: id, school_id, designation_type, stripe_subscription_id, stripe_payment_intent_id, status
- school_verification_documents: id, school_id, designation_id, document_type, file_url, file_name, file_size, status
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Faculty and Designations settings pages</name>
  <files>
    app/schools/[slug]/settings/faculty/page.tsx,
    app/schools/[slug]/settings/designations/page.tsx,
    app/schools/[slug]/settings/actions.ts
  </files>
  <read_first>
    app/schools/[slug]/onboarding/actions.ts,
    app/schools/[slug]/onboarding/OnboardingWizard.tsx,
    app/schools/[slug]/settings/actions.ts
  </read_first>
  <acceptance_criteria>
    - Faculty page lists current members with name, position, and status
    - Faculty page has "Add Member" flow: search GOYA members or invite by email
    - Faculty page allows removing members
    - Designations page lists active designations with type, status badge, and subscription info
    - Server actions for faculty CRUD work on completed schools (not blocked by onboarding guard)
  </acceptance_criteria>
  <action>
    **Add faculty actions to settings/actions.ts:**
    Since onboarding/actions.ts getOwnedSchool blocks completed schools by default, add settings-specific faculty actions to the existing settings/actions.ts (created in Plan 01). These use the same getOwnedSchool helper already in settings/actions.ts (which always allows completed schools):

    - saveFacultyMember(schoolSlug, data: { profile_id, position }) — same logic as onboarding version
    - removeFacultyMember(schoolSlug, facultyId) — same logic
    - inviteFacultyByEmail(schoolSlug, data: { email, position }) — same logic
    - searchMembers(query: string) — search profiles by name/email for faculty picker (same as onboarding API or server action)

    **Faculty page (faculty/page.tsx) — SET-07:**

    Server component fetches school + existing faculty:
    ```
    supabase.from('school_faculty').select('*, profiles(id, first_name, last_name, avatar_url, email)').eq('school_id', school.id)
    ```

    Client component:
    - List of current faculty members as cards/rows: avatar, name, position badge, status indicator, remove button
    - "Add Faculty Member" button opens a section/modal with:
      1. Search input that searches GOYA members by name (calls searchMembers action)
      2. Results list with "Add" button per result
      3. Position select dropdown (Instructor, Senior Instructor, Lead Teacher, Assistant, etc.)
      4. OR: "Invite by Email" tab with email input + position select
    - Remove: confirmation dialog, calls removeFacultyMember
    - Toast feedback on all actions
    - Reuse patterns from OnboardingWizard faculty step (step 8)

    **Designations page (designations/page.tsx) — SET-08:**

    Server component fetches school + designations:
    ```
    supabase.from('school_designations').select('*').eq('school_id', school.id)
    ```

    Render as cards:
    - Each designation card shows: designation_type (formatted label), status badge (active/pending/etc.), subscription info if available
    - Status badges: 'active' = green, 'pending' = amber, 'cancelled' = red
    - Note at bottom: "To add new designations, contact support@goya.community" (per deferred decision — no checkout flow yet)
    - This is primarily a read-only view for now
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    Faculty page lists members with add/remove functionality. Designations page shows active designations with status badges. Faculty server actions work on completed schools.
  </done>
</task>

<task type="auto">
  <name>Task 2: Documents and Subscription settings pages</name>
  <files>
    app/schools/[slug]/settings/documents/page.tsx,
    app/schools/[slug]/settings/subscription/page.tsx,
    app/schools/[slug]/settings/actions.ts
  </files>
  <read_first>
    app/schools/[slug]/onboarding/actions.ts,
    app/settings/subscriptions/PortalButton.tsx,
    app/settings/subscriptions/page.tsx,
    app/schools/[slug]/settings/actions.ts
  </read_first>
  <acceptance_criteria>
    - Documents page lists uploaded documents grouped by designation
    - Documents page allows re-uploading (delete old + upload new) for each document type
    - Subscription page shows school billing status and active subscriptions
    - Subscription page has "Manage Billing" button linking to Stripe Customer Portal
    - Document upload/delete actions work on completed schools
  </acceptance_criteria>
  <action>
    **Add document actions to settings/actions.ts:**
    Add settings-specific document actions (same as onboarding but without the completed-school guard):

    - uploadDocument(formData: FormData) — same logic as onboarding version but uses settings getOwnedSchool (allows completed)
    - deleteDocument(schoolSlug, documentId) — same logic

    Also add a Stripe portal action:
    - createBillingPortalSession(schoolSlug) — creates a Stripe billing portal session for the school's Stripe customer. Look at existing PortalButton.tsx pattern for how the portal session is created. Returns { url: string } | { error: string }.

    **Documents page (documents/page.tsx) — SET-09:**

    Server component fetches:
    - School by slug
    - Designations: `school_designations.select('*').eq('school_id', school.id)`
    - Documents: `school_verification_documents.select('*').eq('school_id', school.id)`

    Client component:
    - Group documents by designation (designation_id)
    - For each designation section:
      - Designation type header
      - List of document types (business_registration, qualification_certificate, insurance)
      - For each document type:
        - If uploaded: show file name, upload date, status badge (pending/approved/rejected), "Re-upload" button
        - If not uploaded: show upload dropzone/button
    - Re-upload flow: delete existing document via deleteDocument, then upload new via uploadDocument
    - Use file input with FormData (same pattern as onboarding uploadDocument)
    - Toast feedback on upload/delete

    **Subscription page (subscription/page.tsx) — SET-10:**

    Server component fetches:
    - School by slug
    - Designations with Stripe subscription info: `school_designations.select('*').eq('school_id', school.id)`

    Render:
    - School subscription status card:
      - "School Subscription" header
      - List each designation with its subscription status
      - If stripe_subscription_id exists: show "Active" badge, link to manage
    - "Manage Billing" button that calls createBillingPortalSession and redirects to the portal URL
    - If no Stripe customer: show informational message
    - Style: match existing subscription page card pattern from app/settings/subscriptions/
    - This is primarily read-only with the portal link being the main action
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    Documents page shows documents grouped by designation with re-upload capability. Subscription page shows billing status with Stripe Customer Portal link. All 8 settings sections are now functional.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- All 4 pages exist under /schools/[slug]/settings/
- Faculty page can add and remove members
- Documents page can re-upload files
- Subscription page has portal link
- Combined with Plan 02, all 8 sidebar sections have working pages
</verification>

<success_criteria>
- Faculty page manages members with search, add, remove functionality
- Designations page shows active designations (read-only with status badges)
- Documents page shows uploads grouped by designation with re-upload capability
- Subscription page shows billing status and Stripe portal link
- All 8 school settings sections are complete and accessible from the sidebar
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/32-school-settings/32-03-SUMMARY.md`
</output>
