---
phase: 14-ai-backend-streaming-chat-widget
plan: "04"
type: execute
wave: 3
depends_on: ["14-03"]
files_modified: []
autonomous: false
requirements: [CHAT-01, CHAT-02, CHAT-03, CHAT-04, CHAT-05, CHAT-06, CHAT-07, CHAT-08, CHAT-09, CHAT-10, CHAT-11, CHAT-12, AI-01, AI-04, AI-05, AI-08, AI-09, AI-10, AI-11, INFRA-07]

must_haves:
  truths:
    - "End-to-end chat works: user sends message, receives streamed AI response"
    - "Widget visible on public pages, hidden on admin"
    - "Guest and logged-in sessions both function"
    - "Escalation flow creates ticket and shows message"
    - "Rate limiting enforced"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete chat widget and AI backend integration. Test all 6 success criteria from the ROADMAP phase definition.

Purpose: Confirm the entire Phase 14 works end-to-end before moving to Phase 15.
Output: Human approval or issue list for gap closure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-ai-backend-streaming-chat-widget/14-CONTEXT.md
@.planning/phases/14-ai-backend-streaming-chat-widget/14-UI-SPEC.md
@.planning/phases/14-ai-backend-streaming-chat-widget/14-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify complete chat widget and AI backend</name>
  <files>None — verification only</files>
  <action>
    Present the verification checklist to the user and wait for approval. Start the dev server if not running (`npm run dev`). Guide the user through each test scenario below.
  </action>
  <what-built>
    Complete AI-powered chat widget with:
    - Floating button on public pages, hidden on /admin/*
    - 380x560px panel (desktop) / fullscreen (mobile) with Mattea header
    - Real-time streaming AI responses from configured OpenAI/Anthropic provider
    - FAQ knowledge base injected as system prompt context
    - Guest sessions via cookie, logged-in sessions via user ID
    - Conversation persistence and restore across page navigation
    - Escalation detection (keywords + repeat questions) creating support tickets
    - Rate limiting at 20 messages per session per hour
    - New Chat / Delete History functionality
  </what-built>
  <how-to-verify>
    **Pre-requisite:** Ensure an AI provider key is configured in /admin/api-keys (Third Party Keys) and selected in /admin/chatbot Configuration tab. Chatbot must be set to Active.

    **1. Widget visibility (CHAT-01, CHAT-12):**
    - Visit the homepage — floating chat button should appear at bottom-right
    - Visit /admin/chatbot — floating button should NOT appear
    - Set chatbot to Inactive in /admin/chatbot config — refresh homepage, button should disappear
    - Re-enable Active for remaining tests

    **2. Panel dimensions and header (CHAT-02, CHAT-03):**
    - Click floating button — panel opens with slide-up animation
    - Desktop: panel is ~380x560px, positioned bottom-right
    - Mobile (resize to <768px): panel goes fullscreen
    - Header shows: Mattea avatar (or placeholder), "Mattea" name, green online dot
    - Three buttons visible: rotate (new chat), trash (delete), X (close)

    **3. Message flow (CHAT-04, CHAT-06):**
    - Fresh chat shows greeting: "Namaste! I'm Mattea, your GOYA guide. How can I help you today?"
    - Type "What is GOYA?" and press Enter
    - User bubble appears right-aligned immediately
    - Typing indicator (3 dots) appears while waiting
    - Streamed response appears token by token on the left with Mattea avatar
    - Streaming cursor blinks during response, disappears when done

    **4. Session persistence (CHAT-07, CHAT-08, CHAT-09):**
    - While logged in: send a few messages, close panel, reopen — history restored
    - Navigate to another page, come back — history still there
    - Log out, open chat as guest — should start fresh
    - As guest: send messages, close and reopen — history restored via cookie

    **5. New Chat and Delete (CHAT-05):**
    - Click rotate icon (New Chat) — messages clear, greeting reappears
    - Send a message, then click trash icon — inline confirm appears
    - Click "Yes, delete" — messages clear, fresh session

    **6. Escalation (CHAT-10, CHAT-11):**
    - Type "I want to talk to a human" — should see escalation bubble with 48-hour message
    - Input should be disabled after escalation
    - Check /admin — a support ticket should have been created (visible in Phase 15, but can verify via DB)

    **7. Rate limiting (AI-10):**
    - Send 20+ messages rapidly in the same session — after limit, should see rate-limit bubble
    - Input disabled until window resets

    **8. AI integration (AI-01, AI-04, AI-05):**
    - Ask about a topic covered in FAQ items — response should incorporate FAQ knowledge
    - Verify response is contextually relevant, not generic

    Report any issues found.
  </how-to-verify>
  <verify>Human confirms all 8 test scenarios pass or reports issues</verify>
  <done>User types "approved" confirming all Phase 14 success criteria are met</done>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
Human verification of all ROADMAP success criteria for Phase 14.
</verification>

<success_criteria>
All 6 ROADMAP success criteria pass:
1. Floating chat button visible on public pages, opens correct dimensions
2. Logged-in user: send, receive streamed response, history persists across refresh
3. Guest user: anonymous session via cookie, prior messages visible on return
4. Escalation after 2 failures or explicit request, support ticket created
5. Widget hidden when is_active is false
6. Rate limiting at 20 messages/session/hour, Node.js runtime
</success_criteria>

<output>
After completion, create `.planning/phases/14-ai-backend-streaming-chat-widget/14-04-SUMMARY.md`
</output>
