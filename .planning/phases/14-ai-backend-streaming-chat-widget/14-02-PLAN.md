---
phase: 14-ai-backend-streaming-chat-widget
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/chat/ChatWidget.tsx
  - app/components/chat/FloatingButton.tsx
  - app/components/chat/ChatPanel.tsx
  - app/components/chat/ChatHeader.tsx
  - app/components/chat/MessageList.tsx
  - app/components/chat/MessageBubble.tsx
  - app/components/chat/ChatInput.tsx
  - app/components/chat/TypingIndicator.tsx
  - app/layout.tsx
autonomous: true
requirements: [CHAT-01, CHAT-02, CHAT-03, CHAT-04, CHAT-05, CHAT-06, CHAT-12]

must_haves:
  truths:
    - "Floating chat button visible on all public pages, hidden on /admin/*"
    - "Chat panel opens as 380x560px on desktop, fullscreen on mobile"
    - "Header shows Mattea avatar, name, online indicator, New Chat, Delete, Close buttons"
    - "Message bubbles: user right-aligned, Mattea left-aligned with avatar"
    - "Opening greeting shown on fresh session"
    - "Widget renders null when chatbot is_active is false"
  artifacts:
    - path: "app/components/chat/ChatWidget.tsx"
      provides: "Root widget component with config fetch and conditional render"
    - path: "app/components/chat/FloatingButton.tsx"
      provides: "56x56px floating button bottom-right"
    - path: "app/components/chat/ChatPanel.tsx"
      provides: "380x560 panel / mobile fullscreen container"
    - path: "app/components/chat/ChatHeader.tsx"
      provides: "Header with avatar, name, action buttons"
    - path: "app/components/chat/MessageList.tsx"
      provides: "Scrollable message list with auto-scroll"
    - path: "app/components/chat/MessageBubble.tsx"
      provides: "User and assistant bubble variants"
    - path: "app/components/chat/ChatInput.tsx"
      provides: "Text input with send button"
    - path: "app/layout.tsx"
      provides: "ChatWidget mounted conditionally"
  key_links:
    - from: "app/layout.tsx"
      to: "app/components/chat/ChatWidget.tsx"
      via: "dynamic import with conditional pathname check"
      pattern: "ChatWidget"
    - from: "app/components/chat/ChatWidget.tsx"
      to: "/api/chatbot/config"
      via: "fetch on mount to check is_active"
      pattern: "api/chatbot/config"
---

<objective>
Build all chat widget UI components with static/mock interactions. The widget mounts in the root layout, checks the config endpoint for is_active, and renders the full visual interface with bubbles, input, header, and animations.

Purpose: Create the complete visual surface users will interact with. Wiring to the real streaming backend happens in Plan 03.
Output: 8 component files + layout.tsx modification. Widget is visually complete but sends messages to a local state mock (no API calls to /api/chatbot/message yet).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-ai-backend-streaming-chat-widget/14-CONTEXT.md
@.planning/phases/14-ai-backend-streaming-chat-widget/14-UI-SPEC.md

<interfaces>
<!-- UI-SPEC.md is the primary contract. Key dimensions extracted here for quick reference. -->

Layout mount point (app/layout.tsx lines 82-108):
```tsx
// ChatWidget will be added inside <ClientProviders> before <CookieConsent />
// pathname available via headers().get('x-pathname')
```

Existing globals.css keyframes (reuse, do NOT create new ones):
- slideUp: translateY(100%) to translateY(0)
- stepIn: opacity 0 + translateY(8px) to normal
- fadeIn: opacity 0 to 1

Color tokens from globals.css:
- var(--goya-surface) = widget background
- var(--background-secondary) = user bubble bg, header bg, input area bg
- var(--goya-primary) = floating button bg, send button bg, online dot
- var(--goya-border) = all borders
- var(--goya-accent) = destructive (delete button only)

Icon imports from lucide-react:
- MessageCircle (floating button)
- RotateCcw (new chat)
- Trash2 (delete history)
- X (close panel)
- ArrowUp (send button)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all chat widget components</name>
  <files>
    app/components/chat/FloatingButton.tsx
    app/components/chat/ChatHeader.tsx
    app/components/chat/MessageBubble.tsx
    app/components/chat/TypingIndicator.tsx
    app/components/chat/MessageList.tsx
    app/components/chat/ChatInput.tsx
    app/components/chat/ChatPanel.tsx
    app/components/chat/ChatWidget.tsx
  </files>
  <read_first>
    .planning/phases/14-ai-backend-streaming-chat-widget/14-UI-SPEC.md
    .planning/phases/14-ai-backend-streaming-chat-widget/14-CONTEXT.md
    app/layout.tsx
    app/globals.css
  </read_first>
  <action>
    Create all 8 files as `'use client'` components. Follow the UI-SPEC.md exactly for dimensions, colors, spacing, and copy. Reference 14-UI-SPEC.md section names below.

    **FloatingButton.tsx** (UI-SPEC: FloatingButton section):
    - Props: `{ onClick: () => void }`
    - 56x56px circle, fixed `bottom-6 right-6`, `z-50`
    - Background: `bg-[var(--goya-primary)]` with `shadow-lg`
    - Icon: `<MessageCircle size={24} className="text-white" />` from lucide-react
    - `aria-label="Open chat with Mattea"`
    - Hover: slight scale `hover:scale-105 transition-transform`

    **ChatHeader.tsx** (UI-SPEC: ChatHeader section):
    - Props: `{ avatarUrl: string | null, name: string, onNewChat: () => void, onDeleteHistory: () => void, onClose: () => void }`
    - Height 56px, `px-4`, flex row items-center, border-bottom `border-[var(--goya-border)]`
    - Background: `bg-[var(--background-secondary)]`, `rounded-t-2xl` (desktop only — pass a prop or use responsive class)
    - Left side: avatar img 36x36 rounded-full (fallback to first letter of name in a circle if no URL), name span `text-sm font-semibold`, online dot 8px `bg-[var(--goya-primary)]` rounded-full with `sr-only` "Online" text
    - Right side: three icon buttons each 32x32 with `hover:bg-[var(--goya-border)]/50 rounded-lg transition-colors`:
      - RotateCcw 16px — `aria-label="Start new chat"` `title="New chat"` — calls onNewChat
      - Trash2 16px — `aria-label="Delete chat history"` `title="Delete chat history"` — `text-[var(--goya-accent)]` — shows inline confirm on click
      - X 16px — `aria-label="Close chat"` `title="Close chat"` — calls onClose
    - Delete confirm: local useState `showDeleteConfirm`. When true, show a row below the header (or replace the button area) with: "Delete all messages? This cannot be undone." + "Yes, delete" button (red/danger small) + "Cancel" button (ghost small). "Yes, delete" calls onDeleteHistory and resets confirm. Auto-dismiss after 5 seconds.

    **MessageBubble.tsx** (UI-SPEC: MessageBubble sections):
    - Props: `{ role: 'user' | 'assistant' | 'escalation' | 'rate-limit', content: string, avatarUrl?: string | null, isStreaming?: boolean }`
    - **User bubble**: `ml-auto max-w-[75%]`, bg `var(--background-secondary)`, border `var(--goya-border)`, `rounded-xl rounded-br-sm`, `px-4 py-2`, text-sm
    - **Assistant bubble**: `mr-auto max-w-[75%]`, flex row gap-2, avatar 28x28 rounded-full shrink-0 mt-0.5, bubble bg `var(--goya-surface)` border `var(--goya-border)` `rounded-xl rounded-tl-sm` `px-4 py-2` text-sm. If isStreaming, append blinking cursor `|` in `text-[var(--goya-primary)]` with CSS animation `animate-pulse` (600ms step approximation via opacity)
    - **Escalation bubble**: Same as assistant but with `border-l-[3px] border-l-[var(--goya-primary)]` and bg `bg-[var(--goya-primary)]/5`
    - **Rate-limit bubble**: Same as assistant but with bg `bg-[var(--goya-accent)]/5` border `border-[var(--goya-accent)]/20`
    - Animation: each bubble wraps in a div with `animate-[stepIn_220ms_ease]` (reuses existing keyframe)

    **TypingIndicator.tsx** (UI-SPEC: TypingIndicator section):
    - Props: `{ avatarUrl?: string | null }`
    - Same layout as assistant bubble (left-aligned with 28px avatar)
    - Content: three 6px dots with staggered pulse animation (dot 1: delay 0, dot 2: 150ms, dot 3: 300ms). Use inline style for animation-delay. `bg-foreground/30` color. `aria-label="Mattea is typing"`

    **MessageList.tsx** (UI-SPEC: MessageList section):
    - Props: `{ messages: Array<{ id: string, role: string, content: string }>, avatarUrl: string | null, isTyping: boolean, isStreaming: boolean }`
    - Flex-col, `overflow-y-auto scrollbar-none`, `px-4 py-4`, `aria-live="polite"`, flex-1
    - Auto-scroll to bottom on new message: useEffect with ref on container, scrollTo({ top: scrollHeight, behavior: 'smooth' })
    - If messages empty (fresh session): render greeting bubble as first assistant message with content "Namaste! I'm Mattea, your GOYA guide. How can I help you today?"
    - Map messages to MessageBubble components. Last assistant message gets `isStreaming` if streaming is active.
    - Show TypingIndicator when isTyping and NOT isStreaming (typing = waiting for first token, streaming = tokens arriving)

    **ChatInput.tsx** (UI-SPEC: ChatInput section):
    - Props: `{ onSend: (message: string) => void, disabled?: boolean }`
    - Height auto (min ~60px), `px-4 py-3`, border-top `border-[var(--goya-border)]`, bg `var(--background-secondary)`
    - Textarea: single line auto-expand to max 3 rows. Use a ref to measure scrollHeight and set height dynamically (min-h-[36px] max-h-[84px]). `bg-[var(--background-tertiary)]` or fallback `bg-[var(--goya-surface)]`, border, rounded-xl, `px-3 py-2 text-sm`, placeholder "Ask Mattea anything...", `resize-none`
    - Send button: 36x36 circle, `bg-[var(--goya-primary)]`, ArrowUp icon 16px white, rounded-full. Disabled (opacity-50 cursor-not-allowed) when input empty or disabled prop true.
    - Submit on Enter (no Shift): call onSend with trimmed value, clear input. Shift+Enter: newline.
    - `aria-label="Send message"` on button

    **ChatPanel.tsx** (UI-SPEC: ChatPanel sections):
    - Props: `{ isOpen: boolean, avatarUrl: string | null, name: string, onClose: () => void }`
    - Container: fixed positioning. Desktop (`md:` prefix): `bottom-24 right-6 w-[380px] h-[560px] rounded-2xl`. Mobile (default): `inset-0 rounded-none h-[100dvh]`.
    - Border, shadow-lg, bg `var(--goya-surface)`, `z-50`
    - Animation: when isOpen transitions to true, apply `animate-[slideUp_280ms_ease-out]`. Use a local state to track mount/unmount for exit animation (opacity fade 180ms).
    - `role="dialog" aria-label="Chat with Mattea" aria-modal="true"`
    - Focus trap: on open, focus the textarea input. On Escape keydown, call onClose. Use useEffect for keydown listener.
    - Internal state management (local useState):
      - `messages: Array<{ id: string, role: 'user' | 'assistant' | 'escalation' | 'rate-limit', content: string }>`
      - `isTyping: boolean` (waiting for first token)
      - `isStreaming: boolean` (tokens arriving)
      - `isEscalated: boolean` (escalation triggered, disables input)
      - `isRateLimited: boolean` (rate limit hit, disables input)
      - `sessionId: string | null`
    - **Mock send behavior for now** (Plan 03 will replace with real API call):
      When user sends message via ChatInput.onSend:
      1. Add user message to messages array with `crypto.randomUUID()` as id
      2. Set isTyping = true
      3. After 1 second timeout, set isTyping = false, add mock assistant message "I'm Mattea! This is a placeholder response. The real AI backend will be connected soon."
      This mock allows the full UI to be verified visually before wiring.
    - **New Chat**: clear messages array, set sessionId = null, set isEscalated/isRateLimited = false
    - **Delete History**: same as New Chat (Plan 03 will add API call to delete DB records)
    - Compose: ChatHeader at top, MessageList in middle (flex-1), ChatInput at bottom. Disable ChatInput when isEscalated or isRateLimited.

    **ChatWidget.tsx** (UI-SPEC: ChatWidget root):
    - Root component that manages open/closed state
    - On mount: fetch `GET /api/chatbot/config`. If `is_active === false`, render null (CHAT-12).
    - Store `{ name, avatar_url }` from config response.
    - State: `isOpen: boolean` (default false)
    - When closed: render FloatingButton with onClick to set isOpen=true
    - When open: render FloatingButton (hidden or behind panel) + ChatPanel
    - Focus return: when panel closes, focus returns to FloatingButton (use ref)
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - 8 files created in app/components/chat/
    - All files start with 'use client'
    - FloatingButton: 56x56, fixed bottom-6 right-6, MessageCircle icon, aria-label="Open chat with Mattea"
    - ChatHeader: 56px height, avatar + name + online dot + 3 icon buttons with correct aria-labels
    - MessageBubble: handles 4 role variants (user, assistant, escalation, rate-limit) with correct alignment and colors
    - ChatPanel: 380x560 desktop, 100dvh mobile, role="dialog", Escape key closes
    - ChatInput: Enter sends, Shift+Enter newline, disabled state, placeholder "Ask Mattea anything..."
    - MessageList: auto-scroll, greeting bubble when empty, aria-live="polite"
    - ChatWidget: fetches /api/chatbot/config on mount, renders null when is_active=false
    - npx tsc --noEmit passes with zero errors
  </acceptance_criteria>
  <done>All 8 chat components render correctly with mock interactions, widget conditionally mounts</done>
</task>

<task type="auto">
  <name>Task 2: Mount ChatWidget in root layout</name>
  <files>
    app/layout.tsx
  </files>
  <read_first>
    app/layout.tsx
    app/components/chat/ChatWidget.tsx
  </read_first>
  <action>
    Modify app/layout.tsx to mount the ChatWidget conditionally.

    1. Add import at top (use next/dynamic for code splitting since this is a client component loaded in a server layout):
       ```typescript
       import dynamic from 'next/dynamic'
       const ChatWidget = dynamic(() => import('./components/chat/ChatWidget'), { ssr: false })
       ```
       Using `ssr: false` because the widget depends on client-side pathname detection and should not contribute to initial SSR payload.

    2. Determine if current page is an admin page. The layout already reads pathname via `headersList.get("x-pathname")`. Add a variable:
       ```typescript
       const isAdmin = pathname.startsWith('/admin')
       ```

    3. Insert ChatWidget inside the `<ClientProviders>` block, BEFORE the `<CookieConsent />` component:
       ```tsx
       {!isAdmin && <ChatWidget />}
       ```

    This ensures:
    - Widget is hidden on all /admin/* routes (CHAT-01)
    - Widget lazy-loads (not in main bundle)
    - Widget is inside ClientProviders so it has access to any needed context
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - app/layout.tsx imports ChatWidget via next/dynamic with ssr: false
    - ChatWidget rendered conditionally with `{!isAdmin && <ChatWidget />}`
    - ChatWidget placed inside ClientProviders, before CookieConsent
    - isAdmin derived from existing pathname variable
    - npx tsc --noEmit passes with zero errors
  </acceptance_criteria>
  <done>ChatWidget mounted in root layout, hidden on admin pages, lazy-loaded</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `ls app/components/chat/` shows all 8 component files
3. `grep "ChatWidget" app/layout.tsx` confirms mount
4. `grep "ssr: false" app/layout.tsx` confirms lazy loading
5. `grep "aria-label" app/components/chat/FloatingButton.tsx` shows "Open chat with Mattea"
6. `grep "api/chatbot/config" app/components/chat/ChatWidget.tsx` confirms config fetch
</verification>

<success_criteria>
- Floating chat button visible on public pages at bottom-right
- Clicking opens a 380x560px panel (desktop) or fullscreen (mobile)
- Header shows Mattea name, avatar placeholder, online dot, three action buttons
- Greeting bubble "Namaste! I'm Mattea..." shown on empty chat
- User can type and send a message, sees mock assistant response
- New Chat clears history, Delete shows inline confirm
- Widget hidden on /admin/* routes
- Widget renders null when chatbot config is_active is false
- All components follow UI-SPEC.md exactly for colors, spacing, typography
</success_criteria>

<output>
After completion, create `.planning/phases/14-ai-backend-streaming-chat-widget/14-02-SUMMARY.md`
</output>
