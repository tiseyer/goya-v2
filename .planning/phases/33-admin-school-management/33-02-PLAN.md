---
phase: 33-admin-school-management
plan: 02
type: execute
wave: 2
depends_on: [33-01]
files_modified:
  - app/admin/schools/[id]/page.tsx
  - app/members/[id]/page.tsx
autonomous: true
requirements: [ADM-02, ADM-05]

must_haves:
  truths:
    - "Admin can view all school data on a detail page at /admin/schools/[id]"
    - "Admin can see uploaded documents with download links on the detail page"
    - "Admin can approve or reject directly from the detail page"
    - "Member profile shows Visit School button for Principal Trainers and Faculty of approved schools"
  artifacts:
    - path: "app/admin/schools/[id]/page.tsx"
      provides: "Admin school detail/review page"
      min_lines: 100
    - path: "app/members/[id]/page.tsx"
      provides: "Updated member profile with Visit School button"
  key_links:
    - from: "app/admin/schools/[id]/page.tsx"
      to: "app/admin/schools/actions.ts"
      via: "server action import for approve/reject buttons"
      pattern: "approveSchool|rejectSchool"
    - from: "app/admin/schools/[id]/page.tsx"
      to: "supabase storage"
      via: "createSignedUrl for document downloads"
      pattern: "createSignedUrl"
    - from: "app/members/[id]/page.tsx"
      to: "schools table"
      via: "query for school by principal_trainer_school_id or faculty_school_ids"
      pattern: "principal_trainer_school_id|faculty_school_ids"
---

<objective>
Create the admin school detail/review page showing all school data with approve/reject actions, and add a "Visit School" button to member profiles for school-affiliated members.

Purpose: Admins need a comprehensive view of all school data before making approval decisions. Members affiliated with schools should have a visible link from their profile.
Output: Admin school detail page at /admin/schools/[id], updated member profile page with school button.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-admin-school-management/33-01-SUMMARY.md

@app/admin/schools/actions.ts
@app/members/[id]/page.tsx
@types/supabase.ts

<interfaces>
<!-- Server actions from Plan 33-01 -->
From app/admin/schools/actions.ts:
export async function approveSchool(schoolId: string): Promise<{ success: boolean; error?: string }>
export async function rejectSchool(schoolId: string, reason: string): Promise<{ success: boolean; error?: string }>

<!-- schools table Row type (key fields) -->
id, name, slug, status, bio, short_bio, description, city, country, established_year,
website, instagram, youtube, facebook, tiktok, video_url, video_platform,
practice_styles, programs_offered, course_delivery_format, lineage, languages,
location_address, location_city, location_country, location_lat, location_lng,
logo_url, cover_image_url, is_insured, onboarding_completed, onboarding_completed_at,
approved_at, approved_by, rejection_reason, owner_id, created_at

<!-- school_designations Row type -->
id, school_id, designation_type, status, stripe_subscription_id, signup_fee_amount, annual_fee_amount

<!-- school_faculty Row type -->
id, school_id, profile_id, position, is_principal_trainer, invited_email, status

<!-- school_verification_documents Row type -->
id, school_id, designation_id, document_type, file_url, file_name, file_size, status, uploaded_at

<!-- profiles table (relevant fields) -->
id, full_name, first_name, last_name, email, avatar_url, principal_trainer_school_id, faculty_school_ids
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create admin school detail/review page</name>
  <files>app/admin/schools/[id]/page.tsx</files>
  <read_first>
    - app/admin/schools/actions.ts (approveSchool, rejectSchool signatures)
    - app/admin/inbox/page.tsx (pattern for fetching school data with joins)
    - lib/supabase/service.ts (getSupabaseService)
    - lib/supabaseServer.ts (createSupabaseServerClient for storage signed URLs)
  </read_first>
  <action>
Create app/admin/schools/[id]/page.tsx as a server component (force-dynamic) with a client component for the action buttons.

**Data fetching (server component):**
- Use getSupabaseService() to fetch the school by ID with all columns
- Join owner:owner_id (id, full_name, email, avatar_url)
- Separately fetch school_designations where school_id matches
- Separately fetch school_faculty where school_id matches, joining profile:profile_id (id, full_name, email, avatar_url)
- Separately fetch school_verification_documents where school_id matches
- For documents: generate Supabase Storage signed URLs (1 hour expiry) using createSupabaseServerClient().storage.from('school-documents').createSignedUrl(file_url, 3600) — if file_url is a full URL, extract the path after the bucket name; if it's a relative path, use directly
- If school not found, call notFound()

**Layout — read-only review page:**
- Header: school name, status badge (reuse STATUS_STYLES pattern from inbox tab), created date, "Back to Inbox" link
- If status is pending/pending_review: show ApproveRejectBar client component at top with Approve button (green) and Reject button (red, opens inline reason input)
- If status is rejected: show rejection reason in a red-tinted alert box

**Sections (styled as white rounded-2xl cards with border-slate-100, matching admin design):**
1. **Owner** — avatar, name, email, link to /admin/users?search={owner_id}
2. **Basic Info** — name, slug, short bio, full bio, established year, insured status
3. **Online Presence** — website, instagram, youtube, facebook, tiktok, video URL
4. **Teaching Info** — practice styles (pill badges), programs offered (pill badges), delivery format, lineage, languages (pill badges)
5. **Location** — full address, city, country, lat/lng (if available)
6. **Designations** — table with designation_type, status badge, subscription ID, fees
7. **Documents** — table with document_type, file_name, file_size (formatted), status badge, download link (using signed URL, opens in new tab)
8. **Faculty** — table with name/email, position, principal trainer flag, status badge

**ApproveRejectBar client component (inline in same file or separate):**
- 'use client' component receiving schoolId prop
- Approve button calls approveSchool(schoolId) server action, shows loading state, on success shows "Approved" toast or redirect
- Reject button toggles inline input for reason, Confirm Reject calls rejectSchool(schoolId, reason)
- Use useRouter().refresh() after successful action to update the page

Use PageContainer for consistent width. Follow admin styling: text-[#1B3A5C] for headings, slate color palette.
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Page loads at /admin/schools/[id] showing all school fields in organized sections
    - Documents have signed URL download links
    - Approve/Reject buttons visible for non-approved schools
    - Rejection reason displayed when status is rejected
    - Back to Inbox link works
  </acceptance_criteria>
  <done>Admin can navigate to /admin/schools/[id], see all school data, documents with download links, designations, faculty, and approve/reject the school</done>
</task>

<task type="auto">
  <name>Task 2: Add Visit School button to member profile page</name>
  <files>app/members/[id]/page.tsx</files>
  <read_first>
    - app/members/[id]/page.tsx (full file — understand current layout and query)
    - types/supabase.ts (profiles table — principal_trainer_school_id, faculty_school_ids columns)
  </read_first>
  <action>
**Update the profile query** in app/members/[id]/page.tsx:
- Add principal_trainer_school_id and faculty_school_ids to the select statement

**Fetch school data if member is affiliated:**
- If profile.principal_trainer_school_id is set: query schools table for that ID, selecting id, name, slug, status
- Else if profile.faculty_school_ids is a non-empty array: query schools table with .in('id', faculty_school_ids), selecting id, name, slug, status
- Filter to only approved schools (status='approved')

**Add Visit School button:**
- If there is at least one approved school: render a "Visit School" link button in the right column, above or below the "GOYA Member" card
- Style: white bg card with rounded-2xl, shadow-sm, border border-slate-100 (matching other right-column cards)
- Inside: school logo or initials circle, school name, "Visit School" link pointing to /schools/[slug]
- If member is affiliated with multiple approved schools, show one button per school (rare but possible for faculty)
- Use a school building SVG icon if no logo

Per ADM-05: only show for Principal Trainers and Faculty of approved schools.
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Member profile page queries principal_trainer_school_id and faculty_school_ids
    - Visit School button appears only when member has an approved school
    - Button links to /schools/[slug]
    - Button does not appear for members with no school affiliation
    - Button does not appear for non-approved schools
  </acceptance_criteria>
  <done>Member profile shows "Visit School" button for Principal Trainers and Faculty of approved schools, linking to the school's public page</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit passes with no errors
- /admin/schools/[id] renders with all school sections
- Documents section has download links
- Member profile shows Visit School for affiliated members only
</verification>

<success_criteria>
- Admin can view complete school data on the detail page
- Admin can download uploaded documents via signed URLs
- Admin can approve/reject from the detail page
- Member profiles show Visit School button for school-affiliated members with approved schools
</success_criteria>

<output>
After completion, create `.planning/phases/33-admin-school-management/33-02-SUMMARY.md`
</output>
