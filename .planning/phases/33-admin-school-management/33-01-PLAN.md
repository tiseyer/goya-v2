---
phase: 33-admin-school-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/admin/schools/actions.ts
  - app/admin/inbox/SchoolRegistrationsTab.tsx
  - app/admin/inbox/page.tsx
autonomous: true
requirements: [ADM-01, ADM-03, ADM-04]

must_haves:
  truths:
    - "Admin inbox School Registrations tab shows designation badges per school"
    - "Approve action sets status to approved, sets approved_at and approved_by, sends approval email"
    - "Reject action requires a reason, sets status to rejected with reason, sends rejection email"
    - "View link navigates to /admin/schools/[id]"
  artifacts:
    - path: "app/admin/schools/actions.ts"
      provides: "approveSchool and rejectSchool server actions"
      exports: ["approveSchool", "rejectSchool"]
    - path: "app/admin/inbox/SchoolRegistrationsTab.tsx"
      provides: "Updated tab with designations column and server action calls"
    - path: "app/admin/inbox/page.tsx"
      provides: "Updated school query joining school_designations"
  key_links:
    - from: "app/admin/inbox/SchoolRegistrationsTab.tsx"
      to: "app/admin/schools/actions.ts"
      via: "server action import"
      pattern: "approveSchool|rejectSchool"
    - from: "app/admin/schools/actions.ts"
      to: "lib/email/send.ts"
      via: "sendEmailFromTemplate"
      pattern: "sendEmailFromTemplate.*school_approved|school_rejected"
---

<objective>
Create server actions for approve/reject workflow and update the admin inbox School Registrations tab to show designation data and use proper server actions with email notifications.

Purpose: Admins need to approve/reject schools from the inbox with email notifications. The existing tab uses direct Supabase client calls without email sending.
Output: Server actions at app/admin/schools/actions.ts, updated SchoolRegistrationsTab with designations column and server-action-based approve/reject.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@app/admin/inbox/SchoolRegistrationsTab.tsx
@app/admin/inbox/page.tsx
@lib/email/send.ts
@lib/email/defaults.ts
@types/supabase.ts

<interfaces>
<!-- Email templates already exist in lib/email/defaults.ts -->
school_approved template: variables {{firstName}}, {{schoolName}}, {{schoolUrl}}
school_rejected template: variables {{firstName}}, {{schoolName}}, {{reason}}

<!-- sendEmailFromTemplate signature from lib/email/send.ts -->
export async function sendEmailFromTemplate({
  to: string | string[],
  templateKey: string,
  variables: Record<string, string>,
}): Promise<{ success: boolean; reason?: string; error?: unknown }>

<!-- school_designations table columns -->
id, school_id, designation_type, status, stripe_subscription_id, stripe_price_id,
signup_fee_amount, annual_fee_amount, signup_fee_paid, activated_at, cancelled_at

<!-- schools table key columns for approve/reject -->
status ('pending'|'pending_review'|'approved'|'rejected'|'suspended'),
approved_at, approved_by, rejection_reason, owner_id, name, slug
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create approve/reject server actions with email</name>
  <files>app/admin/schools/actions.ts</files>
  <read_first>
    - lib/email/send.ts (sendEmailFromTemplate pattern)
    - lib/email/defaults.ts (school_approved and school_rejected template variables)
    - lib/supabase/service.ts (getSupabaseService import)
  </read_first>
  <action>
Create app/admin/schools/actions.ts with 'use server' directive containing two server actions:

1. **approveSchool(schoolId: string)**: 
   - Get admin user via createSupabaseServerClient + auth.getUser()
   - Use getSupabaseService() to update schools table: set status='approved', approved_at=new Date().toISOString(), approved_by=admin user id, rejection_reason=null
   - Fetch the school's owner profile (owner_id join to profiles for email + first_name)
   - Call sendEmailFromTemplate with templateKey='school_approved', variables: firstName (owner first name), schoolName (school name), schoolUrl (NEXT_PUBLIC_BASE_URL + '/schools/' + slug)
   - Return { success: true } or { success: false, error: string }
   - Wrap in try/catch, revalidatePath('/admin/inbox')

2. **rejectSchool(schoolId: string, reason: string)**:
   - Get admin user via createSupabaseServerClient + auth.getUser()
   - Use getSupabaseService() to update schools table: set status='rejected', rejection_reason=reason
   - Fetch the school's owner profile
   - Call sendEmailFromTemplate with templateKey='school_rejected', variables: firstName, schoolName, reason
   - Return { success: true } or { success: false, error: string }
   - Wrap in try/catch, revalidatePath('/admin/inbox')

Import createSupabaseServerClient from '@/lib/supabaseServer', getSupabaseService from '@/lib/supabase/service', sendEmailFromTemplate from '@/lib/email/send'. Use revalidatePath from 'next/cache'.
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx tsc --noEmit app/admin/schools/actions.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - approveSchool sets status='approved', approved_at, approved_by, clears rejection_reason, sends school_approved email
    - rejectSchool sets status='rejected', saves reason, sends school_rejected email
    - Both actions fetch owner profile for email variables
    - Both call revalidatePath('/admin/inbox')
  </acceptance_criteria>
  <done>Both server actions compile, use sendEmailFromTemplate with correct template keys and variables</done>
</task>

<task type="auto">
  <name>Task 2: Update inbox page query and SchoolRegistrationsTab with designations + server actions</name>
  <files>app/admin/inbox/page.tsx, app/admin/inbox/SchoolRegistrationsTab.tsx</files>
  <read_first>
    - app/admin/inbox/page.tsx (existing school query at lines 43-56)
    - app/admin/inbox/SchoolRegistrationsTab.tsx (full file — existing UI)
    - app/admin/schools/actions.ts (just created — import the actions)
  </read_first>
  <action>
**Update app/admin/inbox/page.tsx:**
- Extend the schools query to also join school_designations: add `school_designations(designation_type, status)` to the select
- Update the pending count to filter by status='pending' OR status='pending_review' (both are pre-approval states)
- Pass the designations data through to the SchoolRegistrationsTab component
- Change the school query to use supabaseService (service role) for consistency with other tabs that need cross-table joins

**Update app/admin/inbox/SchoolRegistrationsTab.tsx:**
- Update the School type to include `school_designations: { designation_type: string; status: string }[]` and add 'pending_review' to the status union
- Add a DESIGNATIONS column between LOCATION and SUBMITTED showing designation badges (small pill badges for each designation_type, e.g. "RYS 200", "RCYS 50")
- Update grid-cols from [2fr_1.5fr_1fr_1fr_1fr_auto] to [2fr_1.2fr_1.5fr_0.8fr_0.8fr_0.8fr_auto] to accommodate the new column
- Change the "View" link href from `/schools/${school.id}/settings` to `/admin/schools/${school.id}`
- Replace the direct supabase.from('schools').update() calls in handleApprove and handleReject with imports of approveSchool and rejectSchool from '@/app/admin/schools/actions'
- handleApprove: call approveSchool(schoolId), on success update local state
- handleReject: call rejectSchool(schoolId, rejectReason), on success update local state
- Keep the existing handleReset as client-side supabase call (reset is a convenience, not a formal workflow)
- Add designation badge styling: bg-purple-50 text-purple-700 border border-purple-200 rounded-full px-2 py-0.5 text-xs font-medium
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - School query joins school_designations and passes to component
    - Tab shows designation badges per school
    - View link points to /admin/schools/[id]
    - Approve/reject use server actions (not direct client Supabase calls)
    - Pending count includes both 'pending' and 'pending_review' statuses
  </acceptance_criteria>
  <done>Inbox tab displays designation badges, links to admin school detail, and approve/reject trigger server actions with email</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit passes with no errors
- app/admin/schools/actions.ts exists with approveSchool and rejectSchool exports
- SchoolRegistrationsTab shows designation column and uses server actions
</verification>

<success_criteria>
- Admin can approve a school from inbox tab — status changes, email sent
- Admin can reject a school with reason — status changes, reason saved, email sent
- Designation badges visible per school in the inbox tab
- View button links to /admin/schools/[id]
</success_criteria>

<output>
After completion, create `.planning/phases/33-admin-school-management/33-01-SUMMARY.md`
</output>
