---
phase: 36-database-migrations
plan: "03"
type: execute
wave: 1
depends_on: ["36-01", "36-02"]
files_modified:
  - supabase/migrations/20260382_course_categories_rls.sql
  - supabase/migrations/20260383_lessons_rls.sql
autonomous: true
gap_closure: true
requirements: [DB-05, DB-06, DB-07]

must_haves:
  truths:
    - "Running all 5 migration files (20260379-20260383) sequentially on a fresh database completes without errors"
    - "After sequential application, course_categories has exactly 2 RLS policies (public SELECT + admin/mod ALL)"
    - "After sequential application, lessons has exactly 3 RLS policies (admin/mod ALL, authenticated SELECT published, authenticated SELECT own)"
  artifacts:
    - path: "supabase/migrations/20260382_course_categories_rls.sql"
      provides: "Idempotent RLS for course_categories"
      contains: "DROP POLICY IF EXISTS"
    - path: "supabase/migrations/20260383_lessons_rls.sql"
      provides: "Idempotent RLS for lessons — drops coarse Plan 01 policies and creates precise Plan 02 policies"
      contains: "DROP POLICY IF EXISTS"
  key_links:
    - from: "supabase/migrations/20260382_course_categories_rls.sql"
      to: "supabase/migrations/20260379_course_categories.sql"
      via: "DROP POLICY IF EXISTS before CREATE POLICY — handles duplicate names from inline RLS in 20260379"
      pattern: "DROP POLICY IF EXISTS.*Anyone can read course categories"
    - from: "supabase/migrations/20260383_lessons_rls.sql"
      to: "supabase/migrations/20260380_lessons_table.sql"
      via: "DROP POLICY IF EXISTS before CREATE POLICY — drops coarse inline policies from 20260380 and replaces with precise policies"
      pattern: "DROP POLICY IF EXISTS.*Anyone can read published course lessons"
---

<objective>
Fix migration file idempotency gap: add DROP POLICY IF EXISTS statements to 20260382 and 20260383 so that the full migration sequence (20260379 through 20260383) can be applied to a fresh database without duplicate policy errors.

Purpose: Environment reproducibility — without this fix, any fresh db push (local dev, staging, new team member) fails with "policy already exists" errors because Plan 01 migration files (20260379, 20260380) already create inline RLS policies that Plan 02 files (20260382, 20260383) attempt to recreate.
Output: Two updated migration files that are idempotent when run after their predecessor migrations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-database-migrations/36-VERIFICATION.md
@.planning/phases/36-database-migrations/36-01-SUMMARY.md
@.planning/phases/36-database-migrations/36-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DROP POLICY IF EXISTS to course_categories RLS migration</name>
  <files>supabase/migrations/20260382_course_categories_rls.sql</files>
  <read_first>
    supabase/migrations/20260379_course_categories.sql
    supabase/migrations/20260382_course_categories_rls.sql
  </read_first>
  <action>
Edit supabase/migrations/20260382_course_categories_rls.sql to add DROP POLICY IF EXISTS statements before each CREATE POLICY.

The file currently has two CREATE POLICY statements that duplicate policies already created inline in 20260379_course_categories.sql:
- "Anyone can read course categories" (lines 31-33 of 20260379)
- "Admins and moderators can manage course categories" (lines 35-43 of 20260379)

Add these two DROP statements immediately after the ALTER TABLE ENABLE ROW LEVEL SECURITY line (line 9) and before the first CREATE POLICY:

```sql
DROP POLICY IF EXISTS "Anyone can read course categories" ON public.course_categories;
DROP POLICY IF EXISTS "Admins and moderators can manage course categories" ON public.course_categories;
```

Keep the existing CREATE POLICY statements exactly as they are. The ENABLE ROW LEVEL SECURITY is already idempotent (no-op if already enabled).
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && grep -c "DROP POLICY IF EXISTS" supabase/migrations/20260382_course_categories_rls.sql | grep -q "2" && echo "PASS: 2 DROP POLICY IF EXISTS statements found" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File contains exactly 2 "DROP POLICY IF EXISTS" statements
    - DROP for "Anyone can read course categories" ON public.course_categories appears before CREATE POLICY of same name
    - DROP for "Admins and moderators can manage course categories" ON public.course_categories appears before CREATE POLICY of same name
    - All existing CREATE POLICY statements are unchanged
  </acceptance_criteria>
  <done>20260382_course_categories_rls.sql has DROP POLICY IF EXISTS for both policy names before their CREATE POLICY statements</done>
</task>

<task type="auto">
  <name>Task 2: Add DROP POLICY IF EXISTS to lessons RLS migration</name>
  <files>supabase/migrations/20260383_lessons_rls.sql</files>
  <read_first>
    supabase/migrations/20260380_lessons_table.sql
    supabase/migrations/20260383_lessons_rls.sql
  </read_first>
  <action>
Edit supabase/migrations/20260383_lessons_rls.sql to add DROP POLICY IF EXISTS statements before each CREATE POLICY. This file must handle TWO sources of duplication:

1. Policies with the SAME NAME as 20260380 inline policies:
   - "Admins and moderators can manage lessons" (line 42-50 of 20260380 — same name, same content)

2. Policies from 20260380 that are SUPERSEDED by more precise policies in 20260383:
   - "Anyone can read published course lessons" (lines 32-40 of 20260380) — superseded by "Members can read published course lessons" (TO authenticated, with deleted_at check)
   - "Course owners can manage own course lessons" (lines 52-67 of 20260380) — superseded by "Course creators can read own course lessons" (SELECT only, TO authenticated)

Add these five DROP statements immediately after the ALTER TABLE ENABLE ROW LEVEL SECURITY line (line 10) and before the first CREATE POLICY comment block:

```sql
-- Drop coarser inline policies from 20260380_lessons_table.sql (superseded by precise policies below)
DROP POLICY IF EXISTS "Anyone can read published course lessons" ON public.lessons;
DROP POLICY IF EXISTS "Course owners can manage own course lessons" ON public.lessons;
-- Drop same-named policy from 20260380 to avoid duplicate error
DROP POLICY IF EXISTS "Admins and moderators can manage lessons" ON public.lessons;
-- Drop own policies for idempotency (safe re-run)
DROP POLICY IF EXISTS "Members can read published course lessons" ON public.lessons;
DROP POLICY IF EXISTS "Course creators can read own course lessons" ON public.lessons;
```

Keep all existing CREATE POLICY statements exactly as they are. The 5 DROP statements cover:
- 3 policies from 20260380 that would conflict (2 superseded + 1 same-name)
- 2 policies from 20260383 itself (for full idempotency if this file is re-run)
  </action>
  <verify>
    <automated>cd "/Users/tillseyer/Library/Mobile Documents/com~apple~CloudDocs/Documents/Claude/GOYA v2" && grep -c "DROP POLICY IF EXISTS" supabase/migrations/20260383_lessons_rls.sql | grep -q "5" && echo "PASS: 5 DROP POLICY IF EXISTS statements found" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File contains exactly 5 "DROP POLICY IF EXISTS" statements
    - DROP for "Anyone can read published course lessons" appears (supersedes 20260380 coarse policy)
    - DROP for "Course owners can manage own course lessons" appears (supersedes 20260380 coarse ALL policy)
    - DROP for "Admins and moderators can manage lessons" appears (same-name conflict with 20260380)
    - DROP for "Members can read published course lessons" appears (self-idempotency)
    - DROP for "Course creators can read own course lessons" appears (self-idempotency)
    - All 5 DROP statements appear BEFORE any CREATE POLICY statement
    - All existing CREATE POLICY statements are unchanged
  </acceptance_criteria>
  <done>20260383_lessons_rls.sql drops all conflicting and superseded policies before creating the precise Plan 02 policies, making the full migration sequence idempotent</done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full migration sequence would work on a fresh database:

1. `grep -c "DROP POLICY IF EXISTS" supabase/migrations/20260382_course_categories_rls.sql` returns 2
2. `grep -c "DROP POLICY IF EXISTS" supabase/migrations/20260383_lessons_rls.sql` returns 5
3. No CREATE POLICY statement in 20260382 or 20260383 appears without a preceding DROP POLICY IF EXISTS for the same policy name
4. The migration files are syntactically valid SQL (no unclosed quotes, correct semicolons)
</verification>

<success_criteria>
- The 5 migration files (20260379 through 20260383) can be applied sequentially to a fresh database without any "policy already exists" errors
- After application: course_categories has exactly 2 active RLS policies, lessons has exactly 3 active RLS policies
- The precise Plan 02 policies (TO authenticated, deleted_at check, SELECT-only for creators) are the ones that survive
- The coarser Plan 01 inline policies (public SELECT, owner ALL) are dropped by 20260383
</success_criteria>

<output>
After completion, create `.planning/phases/36-database-migrations/36-03-SUMMARY.md`
</output>
