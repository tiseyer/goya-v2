---
phase: 15-escalation-support-tickets-conversations-admin
plan: 03
type: execute
wave: 3
depends_on: ["15-01", "15-02"]
files_modified: []
autonomous: false
requirements: [ADMIN-07, ADMIN-08, ADMIN-09, ADMIN-10, ADMIN-11, SUPP-01, SUPP-02, SUPP-03, SUPP-04, SUPP-05, INFRA-06]

must_haves:
  truths:
    - "All Phase 15 features are visually correct and functionally working"
  artifacts: []
  key_links: []
---

<objective>
Human verification of all Phase 15 features: Conversations tab, Support Tickets tab, API Connections tab, and cron endpoint.

Purpose: Final checkpoint before marking v1.8 milestone complete.
Output: Human approval or issue list for fixes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-escalation-support-tickets-conversations-admin/15-01-SUMMARY.md
@.planning/phases/15-escalation-support-tickets-conversations-admin/15-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify all Phase 15 features</name>
  <files>none</files>
  <action>
Present the verification checklist to the user and wait for approval. Start the dev server if not running (`npm run dev`). No code changes — this is a human verification gate.

What was built:
1. Conversations tab on /admin/chatbot with session list, filter/search, and read-only conversation viewer
2. API Connections tab on /admin/chatbot with 4 tool toggles (FAQ locked on)
3. Support Tickets tab on /admin/inbox with status management and reply flow
4. Guest chat cleanup cron endpoint at /api/cron/chatbot-cleanup

Verification steps for the user:

**Conversations Tab:**
1. Navigate to /admin/chatbot?tab=conversations
2. Verify the table shows chat sessions (if any exist from Phase 14 testing)
3. Test the filter dropdown: All, Logged-in Users, Guests, Escalated
4. Test the search input
5. Click "View" on a conversation — verify read-only chat history appears with user/assistant bubbles
6. Click Back to return to conversation list

**API Connections Tab:**
7. Navigate to /admin/chatbot?tab=api-connections
8. Verify 4 tools shown: FAQ Knowledge Base, Events Search, Teacher Directory, Course Catalog
9. Verify FAQ toggle is checked and disabled (locked on)
10. Toggle one tool on/off — verify it persists on page refresh

**Support Tickets Tab:**
11. Navigate to /admin/inbox?tab=tickets
12. Verify Support Tickets tab appears as fourth tab with badge count for open tickets
13. Verify ticket table shows User, Issue, Created, Status, Actions
14. Click "View" on a ticket — verify conversation history + reply field appears
15. Type a reply and send — verify message appears in the conversation
16. Toggle ticket status (Open -> In Progress -> Resolved) — verify badge updates

**Cron Endpoint:**
17. Run `curl -s localhost:3000/api/cron/chatbot-cleanup -H "Authorization: Bearer test"` — verify JSON response (may show error if no CRON_SECRET set, that is fine — route existence confirmed)
  </action>
  <verify>User types "approved" or describes issues</verify>
  <done>Human approves all Phase 15 features, v1.8 milestone ready for completion</done>
</task>

</tasks>

<verification>
Human confirms all features work as described.
</verification>

<success_criteria>
Human types "approved" after verifying all 6 success criteria from the ROADMAP Phase 15 entry.
</success_criteria>

<output>
After completion, create `.planning/phases/15-escalation-support-tickets-conversations-admin/15-03-SUMMARY.md`
</output>
